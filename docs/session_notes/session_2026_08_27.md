# Session 2026-08-27 — Three checklists, seven ticks, a real two-account group session

Client `d1aecd6` (exported 12:10 UTC, gdext 5918f106); server `9b320c8` unchanged. The R720
rebooted overnight and the service came back on its own with `dev_cmds=false` — the systemd
auto-start doing its job.

---

## 1. Seven To-Do items ticked on one day's evidence

- **Per-character hotbar + spell bar** (`hotbar_per_character_checklist.md`, all rows):
  isolation both directions across Warrior/Monk, memorized bar isolated on Chumby, macro
  library isolated (a macro made on Funk never reached Zoltan).
- **Bank coin breaking, corpse coin tiers, Stack All container scope, vendor narration**
  (`coin_and_stack_checklist.md`, all rows): the banker makes change both directions; per-tier
  coin survives the corpse round-trip with identical encumbrance; Stack All never crosses
  containers and the desync canary stayed quiet; the vendor window narrates requests only.
- **Group panel stats + incoming tells** (`group_stats_and_tells_checklist.md`): the first
  two-account group session since 08-11 — account 4's character joined account 1's (gid=1),
  bars live and correctly labeled both directions, XP split `pool=315 per_member=157`,
  round-robin loot rejections enforced, tells pass both directions. Closes the two stale
  entries corrected yesterday, this time with playtest evidence.

## 2. New findings, all in the To-Do

- **Buying and granting ignore bag space** — "Your bags are full" with two empty pouches
  carried. Verified server-side: `add_item_locating` scans base slots only; the `bags` map is
  never considered; `BuyItem` and `GmGive` share the refusal. Log: three consecutive
  `BuyItem rejected — inventory full, no stack placed`.
- **Group panel bars blank until the first resource update** — rows build zeroed and the
  server only fans on change, so an idle full-HP group-mate shows empty bars.
- **Skill stamina cost invisible to group-mates** — the spend is client-local; the server's
  stamina never moves. Folded into the active-skills server-model item.
- **/r reply + TAB tell-cycling** requested (EQ muscle memory) — new client UX item.
- **PATK macro on every character** — triaged, not a bug: a June "/pet attack" library macro
  in the legacy seed file. Removed from the legacy file and all seven per-character files at
  the user's request.

## 3. Still open from the day

The 15-second cancel-and-wait re-check (cast cancel's last gate) was not covered. The
pickup-and-place row carries a note that cuts off mid-sentence ("When a player drops an item
on the ground") — needs finishing. Sparse swing-rate rejections in the log (`attacker=12`,
minutes apart) are the known damage-skill-as-plain-Attack collision with the limiter, tracked
in the active-skills item, not a new bug.

## 4. Evening round: both loose ends closed, plus a freebie

- **Cast cancel: TICKED.** The deliberate cancel-and-wait ran clean (plump, Reclaim Soul on
  FIGHTME's corpse, ESC mid-bar, 20 seconds untouched — mana never re-consumed, watched on
  both group displays). And the hit-while-casting row exercised itself: `cast interrupted by
  incoming damage caster=4 spell=Reclaim Soul` at 21:37:03, with a clean re-cast and a full
  res offer/accept after (`refund=0` is correct — a level 2 death sits under the level-5
  grace, so no XP was lost and there was nothing to refund). Every row of the checklist now
  passes.
- **The truncated note resolved:** the user wants LEFT-click to pick up a lone ground item to
  the cursor (corpses stay right-click loot). New To-Do with the design wrinkle recorded: no
  server cursor slot exists, so v1 is likely left-click auto-loot of single-stack bags through
  the same loot-rights checks.
- **An observation made and then corrected:** the log looked like char 13 earned a group XP
  share while dead (killed 21:34:01, share 21:35:30, res 21:37:33). The tester corrected it:
  FIGHTME had respawned at bind and was alive and in range for that share, taking the res
  teleport later — a sequence the log alone cannot distinguish. The follow-up code check still
  found something real: the split filters members on connected-only (`tick.rs` ~504), no
  dead-check, so a genuinely dead member would collect. Reworded as a code-confirmed,
  never-observed discuss-first To-Do.

## 5. The cursor-slot epic, slice 1: built end to end

User chose Option B ("Option B for sure... we need the corpse auto-re-equip feature"), so the
full server-side cursor slot exists as of tonight. Server `73074a8` (PD_W0027): a real
`Inventory.cursor` addressed as the location string "cursor" — the wire, the DB column and
`peek_at` all carry it with no shape changes — with the full move/merge-capped/swap law, the
`LootToCursor` intent gated exactly like the loot window plus single-stack/zero-coin/empty-hand,
atomic take, death strip, persistence, 210/210 tests. Client: cursor mirror + mouse-attached
name label, left-click ground pickup with honest pre-gates, place into base/bag slots,
equip-from-hand at the paperdoll, Drop/Trash cells accept the held item, encumbrance counts it.
gdext rebuilt and the DLL refreshed in the working tree. Design doc:
`docs/design/cursor_slot_and_reequip.md`. Playtest: `cursor_slot_checklist.md` — server restart
and client re-export must land together (protocol gate).
