# Session 2026-09-22: Machine Day — the Fresh World Goes Live

The deploy day the last two weeks built toward. Everything ran off
`machine_day_2026_09_22_checklist.md`; per-row evidence lives there.

- **Deployed + wiped**: phase 4 server live (`a34af47` boot line, dev_cmds
  false), old world snapshotted twice and retired, fresh DB from migrations.
  First citizens: account tyler_mkee, character Splenda, GM chain proven.
- **Starter book 4 for 4**: rat, wolf, and gnoll quests confirmed by lap;
  Rotfang proven in the server log, which also exercised the previously
  undocumented server-side level_req accept gate, the 300 s den respawn,
  named enrage/multipliers in production, and the bag-space `can_accept`
  pre-flight refusing a full-bags turn-in before paying the exact 48,800.
- **Find: Hadrik unreachable** (Forge 1 m away) — all 5 crafting stations +
  3 ore veins removed from world.tscn at the user's call (dead furniture
  since the online guards; To-Do notes their return with server positions).
- **Find: exported builds ignore the window-size override** — login.gd now
  sets 1280x720 centered in code at boot.
- **Find: "Chat 21"** — a veteran settings.cfg layout surviving the wipe;
  seed renamed "Chat 1" in code, operator's stale layout cleared surgically.
- **Find: idle mobs don't fight back when shot from range** — exploit-shaped
  (risk-free ranged farming). Server `3e98553`: tick_idle now acquires the
  highest-threat attacker within leash range; two tests pin both halves.
- **Filed**: in-game UI layout "pretty busted" (deferred theming pass).
- **Answered, no change**: quest XP already pays only at turn-in.
- Exports: `8eb56e7` (town declutter) then `f551dcf` (window + chat), zip
  repacked at each; `f551dcf` is the ship-to-testers build. Pending: the
  `3e98553` redeploy (provoked-mobs fix), checklist section 6 verifies.
