# Content Created — April 30, 2026

---

## World Lore — New Files

| File | Description |
|---|---|
| `docs/concepts/world/named_spawns.md` | Spawn window timing, camp conventions, loot table design |
| `docs/concepts/world/mob_trains.md` | Social aggro radius, leash distances, pulling methods |
| `docs/concepts/world/six_fingered_builders.md` | Early Architect notes (superseded by architects_master.md) |
| `docs/concepts/world/goddess_aracheth.md` | Dark Elf spider-goddess; all-female priesthood; intelligence service |
| `docs/concepts/world/bess_quest_hook.md` | Full Millhaven mystery chain; three families; the jar; five resolution paths |
| `docs/concepts/world/deity_solrath.md` | God of light/justice; Paladin/Cleric patron; the Compact |
| `docs/concepts/world/deity_erindra.md` | Nature goddess; Druid/Ranger patron; the cycle includes predation |
| `docs/concepts/world/deity_vethis.md` | God of the dead; the Ledger; resurrection window; Necromancer theology |
| `docs/concepts/world/deity_mireth.md` | Goddess of luck/trickery; the Laughing Door; the Laughing Company |
| `docs/concepts/world/deity_durrath.md` | God of the iron path; Warriors/Monks/Minotaur Shamans; practice is the prayer |
| `docs/concepts/world/dominion_war.md` | 11-year war (CR 547–558); Third Army; Dominion collapse |
| `docs/concepts/world/varek_and_the_residual.md` | 90 years of Architect proximity; certainty erosion; four resolution paths |
| `docs/concepts/world/aelindra_sealed_archive.md` | 400-year seal; sky-wrong event; the letter to "whoever finds this first, after" |
| `docs/concepts/world/dreaming_grove.md` | Zone/dungeon doc; the Warden; Library of Echoes; node survey fragment |
| `docs/concepts/world/architects_master.md` | Full network map; Calibration State; Remainder Protocol; authorization system |
| `docs/concepts/world/the_old_thing.md` | Degraded Architect remainder; the tooth; Greyveil Foundation Ring connection |
| `docs/concepts/world/languages.md` | All 15 languages; trainer locations; lore context |
| `docs/concepts/world/world_calendar.md` | 24x time ratio; months; festivals; Year 847 CR |
| `docs/concepts/world/creature_battle_remembering.md` | Three categories of staying; Ghost Tongue questline; Durrath/Vethis dispute |

---

## World Lore — Factions

| File | Description |
|---|---|
| `docs/concepts/world/faction_breach_wardens.md` | Sereth Dolan; Gate-Warden investigation; 4 standing ranks |
| `docs/concepts/world/faction_dockmasters_compact.md` | 7 seats (2 vacant); Grey Anchor; Orreth |
| `docs/concepts/world/faction_dominion_orders.md` | Sealed Record / Walking Name / Final Survey; Vault access mechanics |
| `docs/concepts/world/mystery_third_digger_chain.md` | Four-zone connected questline; cessation notice; authorization token |

---

## NPC Profiles — New Files

| File | NPC | Location |
|---|---|---|
| `docs/concepts/world/npc_varek.md` | Varek Lenn — Wizard, 133 years old | Varek's Spire |
| `docs/concepts/world/npc_nameless_commander.md` | The Nameless Commander — waiting 300 years | Ashen Marches |
| `docs/concepts/world/npc_archivist_valex.md` | Valex Dethras — Iksar Necromancer; 31 years in ruins | Ixareth-Kul |
| `docs/concepts/world/npc_director_sathren.md` | Cellen Sathren — 12% probability, proceeding | Ixareth-Kul |
| `docs/concepts/world/npc_sereth_dolan.md` | Sereth Dolan — Warden-Captain, 11 years, 3 attempts | The Breach |
| `docs/concepts/world/npc_bess_holloway.md` | Bess Holloway — innkeeper, 19 years, the jar | Millhaven |
| `docs/concepts/world/npc_silindra.md` | Silindra Vaethas — Grove-Keeper, inside twice, done | Aelindra |
| `docs/concepts/world/npc_seeker_drathis.md` | Drathis Vehn — Walking Name Bard, reading the letter | Ixareth-Kul |

---

## Zone Docs — Expanded

| File | What Was Added |
|---|---|
| `docs/concepts/world/aelindra.md` | Second dungeon (Dreaming Grove); Sealed Archive; Dark Elf infiltrators; Aveth Malorn; 3 new NPCs; level range 10→25 |
| `docs/concepts/world/ardenmoor.md` | Canopy village structure; Kaevel Undershade; Rotwood Hollow expanded; Blighted Wood connection |
| `docs/concepts/world/southeastern_marshlands.md` | Ziggurat as Architect node; night undead mechanics; Elder Reth; Ghost Tongue connection |
| `docs/concepts/world/eastern_badlands.md` | Durrath's temple; Calibration Tower; Bone Citadel expanded; 3 named NPCs |

---

## ASCII Maps — New Files

| File | Location |
|---|---|
| `docs/concepts/ascii/greyveil_city_map.md` | Greyveil — full city |
| `docs/concepts/ascii/vel_sharath_city_map.md` | Vel'Sharath — full city |
| `docs/concepts/ascii/aelindra_city_map.md` | Aelindra — full city |
| `docs/concepts/ascii/cogsworth_city_map.md` | Cogsworth — full city |
| `docs/concepts/ascii/harrowmere_city_map.md` | Harrowmere — full city |
| `docs/concepts/ascii/fae_mere.md` | Fae Mere — zone overview |
| `docs/concepts/ascii/khala_savannahs.md` | Khala Savannahs — zone overview |
| `docs/concepts/ascii/ardenmoor.md` | Ardenmoor — zone overview |
| `docs/concepts/ascii/eastern_badlands.md` | Eastern Badlands — zone overview |
| `docs/concepts/ascii/dreaming_grove.md` | The Dreaming Grove — dungeon layout |

---

## Updated

| File | Change |
|---|---|
| `docs/concepts/world/world.md` | Added 3 zones; updated Aelindra level range and description |
| `docs/concepts/ascii/README.md` | Added 5 zone maps, 1 dungeon map; fixed vah_shir filename |

---

## Code — Technical Debt Cleared

All five outstanding Technical Debt items completed in one session.

### New Autoloads

| File | Description |
|---|---|
| `autoloads/alignment.gd` | Extracted from `player_stats.gd`; owns alignment_score, alignment_tier, `alignment_changed` signal, `get_effective_class()`; `set_alignment()` always emits the signal (bug fixed in review) |
| `autoloads/warder_ai.gd` | Extracted from `pet_manager.gd`; owns warder type, retreat timer, `summon_warder`, `command_fury`, `heal_warder`, `setup_for_class`; communicates with PetManager via `warder_died` / `warder_pet_dismissed` signals |

### New HUD Component Scripts

`hud.gd` reduced from 907 → 478 lines. Each component is a self-contained Control subclass.

| File | Description |
|---|---|
| `scripts/hud_death_screen.gd` | Full-screen death overlay; subscribes to `PlayerDeath` signals |
| `scripts/hud_cast_bar.gd` | Floating cast progress bar; subscribes to `Spells` casting signals |
| `scripts/hud_buff_bar.gd` | Row of buff icons with countdown timers; subscribes to `BuffManager.buffs_changed` |
| `scripts/hud_pet_panel.gd` | Pet HP panel; subscribes to `PetManager` pet lifecycle signals |
| `scripts/hud_group_panel.gd` | Group member rows with HP/MP/STA bars; subscribes to `GroupManager`; emits `layout_changed` so hud.gd repositions the pet panel |

### Modified Files

| File | Change |
|---|---|
| `autoloads/combat.gd` | Added 8 enemy-status signals (`enemy_stunned`, `enemy_stun_wore_off`, `enemy_rooted`, `enemy_snared`, `enemy_slowed`, `enemy_mez_applied`, `enemy_mez_broke`, `enemy_charmed_attacked`) |
| `autoloads/pet_manager.gd` | Warder-specific logic removed; generic pet lifecycle only; added `get_pet_scene()` public getter; `warder_died` / `warder_pet_dismissed` signals emitted for WarderAI to subscribe to |
| `autoloads/player_stats.gd` | Alignment vars, signal, and four alignment methods removed; `apply_character()` now calls `Alignment.set_alignment()` |
| `autoloads/skills.gd` | `PlayerStats.alignment_changed` → `Alignment.alignment_changed`; `get_effective_class()` → `Alignment`; `PetManager.command_fury()` → `WarderAI.command_fury()` |
| `autoloads/spells.gd` | Same alignment re-routing; `PetManager.heal_warder()` → `WarderAI.heal_warder()`; `PlayerStats.alignment_tier` → `Alignment.alignment_tier` |
| `autoloads/crafting.gd` | `PlayerStats.alignment_tier` → `Alignment.alignment_tier` |
| `autoloads/transformations.gd` | `PlayerStats.alignment_tier` → `Alignment.alignment_tier` |
| `scripts/enemy.gd` | 8 direct `CombatLog.add_line()` calls replaced with `Combat.*` signal emissions |
| `scripts/pet.gd` | Added `hit_target` signal; emitted on each attack hit |
| `scripts/combat_log.gd` | 9 new signal subscriptions added: 8 `Combat` enemy-status signals + `PetManager.pet_info` |
| `scripts/hud.gd` | All bar styling delegated to `UITheme.style_bar()`; local `_style_bar()` removed |
| `scripts/ui_theme.gd` | Added `static func style_bar(bar, color) -> Label`; centralizes the bar-styling pattern that was duplicated across 3 HUD files |
| `project.godot` | `Alignment` and `WarderAI` registered as autoloads (after `PlayerStats` and `PetManager` respectively) |

---

## Code — Critical Hits

| File | Change |
|---|---|
| `autoloads/combat.gd` | `deal_spell_damage()` now rolls INT-based crit (0–20% chance); applies 1.5–2.0× multiplier to spell damage on crit; auto-attack and skill crits were already wired (DEX-based, 0–30%) |
| `scripts/combat_log.gd` | Added `C_CRIT` color constant and `MsgType.CRIT` enum value; crit hits now render in bright gold, distinct from normal damage gold; `add_damage_out()` routes crits to `MsgType.CRIT` |

---

## Code — Chat Log Scrollback

| File | Change |
|---|---|
| `scripts/combat_log.gd` | Added `_back_btn` overlay button anchored to the bottom of the log panel; appears when the user scrolls up and new messages arrive; clicking it snaps back to the latest message and re-enables auto-scroll; mouse wheel scrolling works natively via `ScrollContainer` (DraggablePanel does not intercept wheel events) |

---

## Code — Damage Shield

| File | Change |
|---|---|
| `scripts/spell_data.gd` | Added `damage_shield_amount: float` and `damage_shield_duration: float` fields |
| `autoloads/buff_manager.gd` | Added `_damage_shield` dict, `add_damage_shield()`, `get_damage_shield_amount()`, `get_damage_shield()`; ticked via `_tick_timed_buff()`; cleared in `clear_all()`; emits `damage_shield_applied` signal on apply |
| `autoloads/combat.gd` | `receive_player_damage()`: after evasion/sitting checks, before absorb, reads `BuffManager.get_damage_shield_amount()` and calls `attacker.take_damage()` if attacker is alive |
| `autoloads/spells.gd` | Loads `damage_shield_amount`/`damage_shield_duration` from spell dict; calls `BuffManager.add_damage_shield()` when both > 0 |
| `data/spell_definitions.gd` | Added **Thorns** (Druid — 8 nature dmg/hit, 600s, abjuration) and **Spellshield** (Enchanter — 10 arcane dmg/hit, 600s, abjuration) |

---

## Code — Elemental Resistances

| File | Change |
|---|---|
| `scripts/enemy.gd` | Added `@export_group("Resistances")` with 8 float fields (fire/ice/lightning/arcane/holy/nature/spirit/shadow, 0.0–1.0). Added `get_spell_resist(DamageType) -> float` match statement |
| `autoloads/combat.gd` | `deal_spell_damage()` now accepts `damage_type: SpellData.DamageType` (default NONE); reads `current_target.get_spell_resist()`; applies `1.0 - resist` multiplier; logs "X resists your spell." on full block |
| `autoloads/spells.gd` | Passes `spell.damage_type` to `Combat.deal_spell_damage()` |
| `scripts/enemy_spawner.gd` | Added `@export_group("Resist Overrides")` with 8 float fields (−1.0 = inherit from prefab, 0.0–1.0 = override); applied via property loop in `_apply_overrides()` |

---

## Code — Enemy Spawn System

`scripts/enemy_spawner.gd` was already complete (PackedScene + respawn_time + spawn_radius + mob config overrides). This session added resist overrides and created `scenes/enemy_spawner.tscn` so the node can be dragged into any world scene from the Godot editor.

---

## Code — Class Abilities Pass (continued from session)

### New Autoloads

| File | Description |
|---|---|
| `autoloads/bard_songs.gd` | Bard Song Twist system: single active song, 3s pulse, 3.5s linger; `activate_song()` toggles same song off; `_pulse()` applies heal/speed/mp_regen/haste/absorb each interval; registered in `project.godot` |

### New Scripts

| File | Description |
|---|---|
| `scripts/track_window.gd` | Ranger Track window (DraggablePanel subclass); lists enemies within 60m with name/level/distance; refreshes every 2s; hidden by default; opened via `/track` command; instantiated in `hud.gd._build_components()` |

### SpellData / SkillData field additions

`scripts/spell_data.gd` — 17 new `@export` fields: `move_speed_mult/duration`, `mp_regen_hps/duration`, `haste_amount/duration`, `accuracy_buff`, `crit_buff`, `stat_buff_duration`, `str/agi/int/wis/con_buff`, `max_hp/mp_buff`, `primary_stat_buff_duration`, `is_stealth`, `stealth_duration`, `is_dispel`, `silence_duration`, `is_lich_form`, `lich_mp_regen`, `mana_drain`, `is_song`

`scripts/skill_data.gd` — Extended `EffectType` enum with `FEIGN_DEATH`, `STEALTH`, `TRUESIGHT`; added `stealth_duration` field

`scripts/item_data.gd` — Added `Type.AUGMENT`, `gem_slots: int`, `socketed_augments: Array[String]`

### Modified Files

| File | Change |
|---|---|
| `autoloads/buff_manager.gd` | Added: `_speed_buff`, `_mp_regen_buff`, `_haste_buff`, `_stat_buff`, `_primary_stat_buff`, `_damage_shield`, `_stealth_remaining`, `_lich_form_active`; corresponding add/get/clear functions; `_tick_timed_buff()` helper; death+zone clear wired |
| `autoloads/spells.gd` | Loads all new SpellData fields; `_apply_spell()` handles speed/mp_regen/haste/stat/stealth/dispel/silence/lich_form/mana_drain/song; song buffs skip direct-apply (routed exclusively through BardSongs pulses) |
| `autoloads/skills.gd` | Loads stealth_duration; match cases for FEIGN_DEATH, STEALTH, TRUESIGHT; `_do_feign_death()` (80% success, iterates enemies group → LEASH); ENEMY-targeted skills break stealth |
| `autoloads/regen.gd` | Lich Form skips HP regen, adds extreme MP regen; Clarity/Breeze `mp_regen_buff` added to MP tick |
| `autoloads/combat.gd` | `_update_attack_interval()` applies haste; `calc_damage()` adds crit_bonus from stat buff; accuracy bonus reduces miss chance; added `enemy_silenced` signal |
| `autoloads/transformations.gd` | Placeholder comment for Revenant tradeskill retention |
| `scripts/enemy.gd` | Added `is_silenced`/`_silence_timer`; `silence()` emits `Combat.enemy_silenced`; `strip_one_buff()` (charm > mez > stun priority); `feign_death_deaggro()` (CHASE/ATTACK → LEASH) |
| `scripts/player.gd` | Move speed multiplied by `BuffManager.get_speed_mult()` |
| `scripts/hud_buff_bar.gd` | Added icons for: speed, haste, mp_regen, stat (accuracy/crit), stealth, lich — each with color border and countdown timer |
| `scripts/hud.gd` | Instantiates `TrackWindow`; `/track` command wires to `_track_window.toggle()` |
| `scripts/combat_log.gd` | Added `Combat.enemy_silenced` subscription |
| `data/spell_definitions.gd` | Added: Complete Heal, Resurrection (scaffolded), Torpor, Clarity, Breeze, Haste, Spirit of Wolf, Selos' Melody, Lich Form, Exsanguinate, Ensnaring Roots, Camouflage, Hunter's Eye, Spellbreak, Antimagic Ward; fixed Expose to `is_dispel:true` |
| `data/skill_definitions.gd` | Added: Hide (Rogue), Aimed Shot, Rapid Shot, Dual Flurry (Ranger), Exploit Weakness, Truesight Strike (Witch Hunter), Feign Death (Monk) |
| `project.godot` | `BardSongs` registered as autoload |

### Bugs fixed in review

| Bug | Fix |
|---|---|
| TRUESIGHT skill dealt damage twice (match case + target_type==ENEMY block) | Removed redundant damage call from TRUESIGHT match case; `pass` placeholder explains intent |
| Song buffs double-applied on cast (direct apply + BardSongs immediate pulse) | Wrapped speed/mp_regen/haste/stat buff blocks in `if not spell.is_song:` |
| `enemy.gd silence()` called `CombatLog.add_line()` directly | Replaced with `Combat.enemy_silenced.emit()`; combat_log.gd subscribes |
| `_stealth_buff_name` stored but never read | Removed; signature kept compatible via `_buff_name` discard param |
| `BardSongs` active song not cleared on death/zone change | Added `_ready()` connecting `PlayerDeath.player_died` and `ZoneLoader.zone_changed` to `_stop()` |
| `_track_window` untyped in hud.gd | Typed as `TrackWindow` |
