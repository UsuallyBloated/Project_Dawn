# Session 2026-07-31 — Phase 1 COMPLETE (login rate-limit ticked) + account-admin handoff

## Login rate-limit + auth-timing — PLAYTESTED + TICKED (the last Phase 1 gate)
`login_rate_limit_checklist.md` closed out: sections 1-4 all `[x]` (normal auth regression, login
throttle at 5/60 s, blocked-while-correct-password, window rollover, success-clears-recovery,
Register throttle, clean message text). Section 5 (auth timing) left optional — it's a tens-of-ms
difference that can't be eye-tested; unit-reasoned + code-reviewed.

**Verification beyond the tick.** `server.log` was clean (0 errors, 0 panics, only the dev-cmds
boot WARN) but showed **no auth activity at all**, and `world.db` showed **zero new accounts** for
the day. Reconciled both rather than assuming a pass:
- The empty log was *by design*: rate-limit rejections only surfaced via the generic `"auth error"`
  line at `tracing::debug`, and the server runs at INFO.
- Zero new accounts is consistent with the throttle firing: `try_acquire` runs **before**
  `create_account`, so re-using existing usernames returns `NameTaken` (no row written) while still
  consuming a slot; the 6th attempt then hits the cap. The tester's observed message text is
  literally the `RateLimited` string, which section 4 confirms rendered cleanly.

**Gap found + fixed while verifying (server `339ea97`).** A throttled IP left no trace at INFO, so a
brute-force attempt against a hosted server would be invisible in `server.log` — the operator's only
window into attacks, and Phase 2 (hosting) is next. Both throttle sites (Login + Register) now log at
INFO with the client IP and the window/cap. Wrong-password attempts stay at debug (no spam).
179 lib + 3 auth-smoke pass.

### The two bugs this playtest cycle caught (worth remembering)
1. **Loopback exemption** (`6cddff2`) — the original gate skipped 127.0.0.1 "for dev convenience",
   which made it both untestable *and* unprotected in the normal localhost setup. The first re-test
   reported "does not work" purely because of it. Dropped: a login success clears the budget, so the
   dev relog loop never accumulates anyway.
2. **Register never throttled** (`fcfdf9a`) — the launcher auto-logs-in after each Register, and a
   login success cleared the **shared** per-IP counter, so the Register budget reset every account
   (8 accounts in 30 s, no error). Root-caused from `world.db` (each account's `last_login` was ~1 s
   after `created_at`). Fixed by keying the limiter on `(IpAddr, AuthKind)`.

Both were only findable by playtest — neither unit tests nor the 3-lens adversarial review caught
them, because both hinged on real client/launcher behavior.

## Phase 1 (exploit gate) is COMPLETE — 2026-07-31, on schedule (Jul 20 to Jul 31)
All seven gates built, playtested, and ticked:
`is_gm` keystone, Respawn dead-check, attack-while-seated, CastSpell class/level, `Attack`
weapon_path, melee swing-rate limit, login rate-limit + auth-timing. Schedule + systems_overview
updated (new "Auth hardening" section documenting the design and its deliberate residuals).

Open caveat, not blocking: the swing-rate **haste** row is unit/design-covered but never eye-tested
(client max haste 0.5 == the gate's `MAX_MODELED_HASTE`, so it's safe by construction).

## Also: account-admin handoff written
User asked where to see registered accounts for housekeeping. Answer: `admin_report` already does it
(read-only console + `world_report.html`; 26 accounts / 135 characters, 120 soft-deleted). The real
gap is **write** tooling: `accounts.is_banned` is *enforced at login but unsettable* (nothing writes
it), and there's no account delete or soft-deleted-character purge. Wrote
`handoff_account_admin.md` — a self-contained plan for another session proposing one `admin_account`
ops bin (list / ban / unban / delete / purge-deleted-chars) with guardrails: `--confirm` + a preview
on destructive ops, a transactional delete that clears every referencing table (re-derived from the
migrations, and check whether SQLite FK cascade is even on), GM-account protection, and session
revocation on ban.

## Next
- **Server procs playtest** (`server_procs_checklist.md`) — needs `build.ps1` (gdext `.dll` rebuild)
  + a client re-export. The only outstanding playtest.
- Phase 2 (hosting) is the next schedule phase.
