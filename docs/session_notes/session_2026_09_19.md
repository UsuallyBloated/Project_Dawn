# Session 2026-09-19: The Trade Window Design

Phone session; design only, no code, no deploy-payload movement (server stays at
`a34af47` for the pending desk deploy).

`docs/design/trade_window.md` drafts the trade epic from the 2026-08-28 mandate
("EQ works like this"). The load-bearing choices: **escrow by locking** (offered
items never leave the owner's inventory; a per-connection locked-slot set makes
every other intent refuse them, so a crash mid-trade can lose nothing), **one
transaction to commit** (a two-connection sibling of `save_stores_atomic`, with
the corpse-loot deferred-send pattern), and **every offer edit clears both
accepts** (the anti-bait-and-switch rule). Receiver placement rides the real
placer (`can_accept` pre-check both ways, `merge_capped` stacks, bag spill).
A ten-point exploit ledger doubles as the build's test list. Wire sketch is
PD_W0028 (protocol bump, gdext rebuild, both-sides deploy). Four open calls for
the user: slots per side (proposal 4), instant-open vs accept prompt (proposal
instant, friends-scale), trade range (proposal 10 m), NPC-give slice timing
(after item-objective quests exist).

To-Do updated: the trade item now points at the design.
