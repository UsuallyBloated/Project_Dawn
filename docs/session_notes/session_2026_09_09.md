# Session 2026-09-09 — The lost marks arrive by phone, and two bugs die

Remote-only session (the user on a phone). The slice 1.5 checklist marks, stuck in an unsaved
editor tab since 08-28, landed on disk mid-session — all 14 rows PASS, including the §1
death-dupe retest on the fixed build and §4's F1-retarget row, which ticks the long-standing
`hud.gd::_tracked_target` guard item.

## The notes held two real bugs, both fixed today

- **Equip-from-hand blanked the held item.** The EquipItem handler's delta fan knew
  base/equip/bag but not "cursor", so an equip-from-hand SWAP fanned an EMPTY cursor delta:
  the client's hand went blank while the server still held the swapped-out piece, and the
  "You're already holding something." refusal the tester saw was the desync's symptom. A sweep
  found five hand-rolled delta readers with the same blind spot — three latent (partial
  drop/destroy from the hand would zero the client's remainder; eat-from-hand was refused at
  the pre-peek). All five now read through `peek_at`. Server-only; 213/213 tests.
- **Drop-confirm clicks passed through to the world.** The confirm dialogs are native popup
  Windows, invisible to the main viewport's hovered-control check, while player.gd deliberately
  polls hardware button state — so the click answering the dialog also registered as a world
  tap and instantly re-opened the prompt. The dialogs now report their closes to Inventory and
  `request_ground_drop` ignores taps inside a 400 ms echo window (which also covers a
  cancelled DELETE echoing into a drop prompt). Client-only.

## Also from the notes

"We plan on changing this, correct?" about non-empty bags refusing to move: it was not
planned; now it is — recorded as the "Full bags should move with their contents" design item
(server re-keys the bags map; EQ precedent), discuss-first.

## Deploy state

Nothing deployed from this session. When back at the machine: server redeploy (the peek_at
fix) + a client re-export (the echo guard), then the two RETEST rows on the checklist.

## The schedule/To-Do accuracy audit (the "review or 10", delivered)

Full read of both schedule copies plus a code-verified walk of the open To-Do entries.

**Stale To-Do entries found and corrected (six):** silent-refusals batch 2 shipped 08-23
(`f1aaf79`) and sat listed as open for 17 days; player inspect is built and command-triggered,
not "in progress"; the ring-reward "bad stat block" hypothesis is disproven on BOTH data
sources (`.tres` and `items.toml` each carry the AGI bonus — the fault is in an apply/display
path); the rate-limit entry never mentioned the PD_NO_RATE_LIMIT kill-switch that has been
active on the R720 for weeks; the group-bars verify step is settled (EntitySpawn carries no
resources, so the fix is a server-side one-shot on roster change); the jump entry's line refs
had drifted.

**Schedule drift found and synced:** the html header chip still said "Status · Aug 11"; the md
status row and the html item lists had drifted OPPOSITE ways again; both risk tables still
taught the retired "flaky trio" rule; and neither copy mentioned the entire Aug 28 to Sep 9
cursor-slot epic — confirming the suspicion that finding-shaped work never reaches the
schedule.

**Two decisions surfaced for the user:** (1) structural — stop carrying per-item status in the
schedule (freeze phase bullets as "the plan as drafted", one-line status rows, the To-Do as
the only live list; the html's own "one home per fact" rule, which both copies violate);
(2) the date — phase 4 is unstarted with its window ending Sep 11, so Sep 14 cannot meet the
three-hour content bar: slip, or keep the date and lower the bar. The audit item closes when
both land.

Also from the phone brainstorm: proximity-gated XP shares recorded on the dead-XP item
(mirrors the coin split's 30 m range), and the companion wish — dead players leaving NPC aggro
lists — verified ALREADY BUILT (the death sweep wipes aggro + threat and clears targets).

**Decision 2 landed same day: the target slipped.** New date 2026-10-05 — the mechanical
re-base (phase 4's full three-week window now Sep 10 to Oct 1, original slack preserved), with
the three-hour content bar standing rather than lowered. Both schedule copies updated in the
same pass. Decision 1 (strip per-item status from the schedule) remains open.

**Decision 1 landed too — the restructure is done, and the audit item is CLOSED.** Both
schedule copies' item lists are frozen as dated snapshots (banner in each), the one-line §7
status table is the schedule's only live part, Session workflow step 6 now says a tick touches
the schedules only when it closes a phase, and the phase-3 mega-row collapsed to one line.
Per-item status now has exactly one home: the To-Do. Drift by construction, not discipline.
