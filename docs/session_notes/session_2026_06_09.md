# Session 2026-06-09 — CLAUDE.md rewrite + systems overview doc

Documentation-only session. No gameplay code touched.

## What changed

- **Rewrote `CLAUDE.md`** for a smaller, more accurate session-context footprint:
  - Fixed the long-standing doc rot: engine is **Godot 4.4**, not 4.3 (flagged in
    `audit_2026_05_08.md`).
  - Added a **Commands** section covering both repos — client run/debug, and the Rust
    server (`cargo run -p projectdawn-server`, `cargo test`, `cargo build --release`,
    SQLite `world.db`).
  - Added a **two-repo architecture** section: client + authoritative server, current
    pre-alpha/auth-only state, multiplayer-only (no solo), and the canonical pointer to
    `docs/concepts/architecture/README.md` → `server/docs/server_design.md`. Listed the
    known client↔server drift (spell GDScript-vs-toml, client-local leveling rollback,
    per-client time-of-day).
  - Added a **Debugging / chasing bugs** section centered on the in-game debug console
    (`scripts/debug_console.gd`): F2 / backtick / `/console` toggle, in-memory tail of
    `DebugLog` that survives file-IO failure, and the instrument-and-watch loop.
  - Regrouped the **autoload reference** — the old table listed ~30; the project actually
    registers **49** (the entire client networking layer — `Net`, `SaveManager`, the four
    `Remote*` managers, `NetCombatBroadcaster`, `ItemRegistry` — was missing). Now a
    domain-grouped table that names `project.godot` as the source of truth.
  - **Trimmed the To-Do list to open items only.** The completed `[x]` items (≈60) were a
    growing changelog inside the context window; their "what exists" knowledge graduated
    to the systems overview doc.
  - **Loosened the comment rule.** Was "comment only for *why*"; now welcomes file-header
    and orientation comments (this is a project people read to learn from), while keeping
    the anti-rot, anti-narration, no-sprint-labels, no-commented-out-code guards.
  - **Tightened touch hygiene** with an adjacency guardrail: cleanups stay next to the
    diff; no hunting; never refactor under cover of a feature change.

- **Created `docs/concepts/architecture/systems_overview.md`** — the new home for "what
  systems exist and how they work," absorbing the graduated `[x]` knowledge, organized by
  domain (combat, spells, status effects, skills, progression, items, pets/mounts, world,
  NPCs/quests, tradeskills, UI, networking).

## Caveats

- The systems overview is a condensed map, not an exhaustive API; the code remains truth.
- `docs/concepts/architecture/README.md` (canonical server pointer) was left untouched —
  the new overview links to it rather than duplicating it.

---

# Session 2026-06-09 (cont.) — ALLY-target buff routing

Made beneficial buffs castable on a targeted group-mate over the wire. The handoff framed
this as "ALLY buffs wrongly apply to the caster," but the real state was different: **every
stat/haste/clarity/SoW/shield buff was authored `target_type: "SELF"`** on both client and
server, so they correctly self-applied — they were simply never ally-targetable. So this is
a feature (make buffs ally-castable), not a routing bugfix. The class design docs (cleric /
enchanter / druid / shaman / ranger / beast_master `.md`) explicitly tag each buff's base
rank `Self` / `Target` / `Group`; that drove the curated set.

## Curated ALLY set (10, designer-confirmed)

Flipped `SELF → ALLY` in both `data/spell_definitions.gd` and server `spells.toml`:
**Bless, Valor, Brilliance, Clarity, Strength, Gift of Insight, Spirit of the Bear,
Spirit of Wolf, Haste, Thorns.** Left `SELF` (documented self-only): Rune, Spellshield,
Breeze, Primal Bond, Hunter's Eye, the Bard songs (Twist mechanic), Lich Form. Note:
the Druid doc has base **Thorns as Self** (group version = the unimplemented capstone
"Ancient Thornwall"); the designer chose to keep Thorns ally-castable anyway. Spellshield
stays self.

## Changes

- **Server** (`world/tick.rs`): new `apply_player_spell_buffs(conn, spell, now)` applies the
  full beneficial set (HoT, MP-regen, speed, haste, damage shield, absorb, accuracy/crit,
  primary stat) to one recipient. The SELF arm (Lich toggle kept inline) and the ALLY
  player arm now both call it, so they stay in lockstep. ALLY arm fans the `BuffSnapshot`
  to the buffed peer + bystanders and logs `"ALLY buff applied"` (caster + target + spell)
  as the playtest log anchor. ALLY-pet arm still heal-only (no replicated pet buffs).
- **Client `autoloads/spells.gd`**: `_cast_target_id()` makes the broadcast target
  target-type-aware (ALLY → peer, or pet only when the spell heals, else self/0); buffs
  cast on a peer skip the local `BuffManager.add_*` via `_ally_target_is_remote()`; the
  ALLY PvP pre-check now covers buffs (not just heals); added `Spells.get_spell_by_name()`.
- **Client `autoloads/buff_manager.gd`**: `reconcile_with_server_snapshot()` is now
  **additive** — a snapshot buff with no local tracking is reconstructed from the spell
  definition (raw, server-authoritative values; snapshot `remaining` as duration), so the
  buffed ally's own `PlayerStats` + buff bar reflect it. Expiry/dispel still flow through
  the existing subtractive pass. Single-slot buffs only fill when empty (no clobber).

## Acceptance

Cleric casts Bless on a group-mate → server applies the StatBuff to the target conn +
fans `BuffSnapshot` → the group-mate's client reconstructs it (WIS goes up, bar shows
Bless) → on server expiry the snapshot drops it and the stat returns. Caster is no longer
buffed when targeting a peer.

## Verification

- `cargo build -p projectdawn-server` clean (only the pre-existing `BuffSnapshotFanOut`
  dead-code warning). Buff/heal/cast tests pass: `two_clients_buff_snapshot_fanout`,
  `lifesteal_spell_heals_caster`, `two_clients_cast_fanout`, all cast-gate tests.
- The integration suite has a **pre-existing flaky cluster** around enemy-aggro timing
  (`enemy_aggros_chases_and_attacks_player`, `pet_command_attack_locks_onto_target`,
  `charm_converts_enemy_to_pet`, `pet_attacks_owners_target`, `pet_pulls_aggro_via_threat_reaggro`).
  Proven not mine: it fails on baseline with the change stashed, the panic site varies
  (1532 vs 1555), the rate tracks machine load, and the failing assertions are 35 s
  `wait_for` enemy-aggro deadlines + a 3 s post-cast PetSpawn wait — none touch buff code.
- Client GDScript not run through Godot (not on PATH); verified by re-read. Needs a
  two-client editor playtest to confirm end to end.

## Follow-ups

- **Pet buffs**: deferred — needs replicated `active_buffs` on the server `Entity`.
- **Multiple concurrent stat buffs on one player**: client `_primary_stat_buff` /
  single-slot buckets hold one each; server allows several. Pre-existing; reconstruction
  fills only the empty slot, so a 2nd stat buff in a snapshot is shown as just the first.
- Consider a server test that casts an ALLY buff peer→peer and asserts the target's
  `BuffSnapshot` carries the buff name (extends `two_clients_buff_snapshot_fanout`).

## Playtest round 1 (2026-06-09) — two real bugs + one false alarm

First two-client run surfaced three findings; `server.log` triaged them:

- **SoW / Thorns "don't land on target" → false alarm.** Every SoW/Thorns cast logged
  `spell cast rejected — insufficient mana` (caster stuck at mp=15.0). Regen is currently
  disabled (commit 835d442), so the caster drained and couldn't cast. When mana was up,
  `ALLY buff applied … target=47` fired for Bless/Strength/Clarity/Haste — routing is fine.
  Top up mana (Test Panel Full Heal fills MP, or food/drink) to actually test SoW/Thorns.
- **"Stat buffs won't stack" → real, fixed.** Server stacks stat buffs (keyed by name —
  Bless's +WIS and Strength's +STR both land on the target conn), but the client's
  `_primary_stat_buff` was a **single slot**. Converted to a multi-slot list
  (`_primary_stat_buffs`): `add_primary_stat_buff` appends + refreshes by name,
  `_undo_one_primary_stat_buff(name)` undoes a single entry, and `_process` / reconcile /
  `clear_all` / snapshot all iterate. `hud_buff_bar.gd` now renders one icon per stat buff
  keyed `primary_stat:<name>` (mirrors the existing HoT pattern). Other buckets stay
  single-slot (mp_regen / shield / absorb are exclusive on the server too — consistent).
- **`/pvp on` blocks buffing a group-mate → real, fixed.** `combat::can_attack` is just
  `a.pvp_override_on && b.pvp_override_on` — no group check — so two grouped /pvp-on players
  read as mutually attackable and the ALLY gate rejected the beneficial cast. Added
  `GroupManager::same_group(a, b)`; the ALLY heal/buff gate (player + pet arms) now rejects
  only when `hostile && !allied`. (This was partly masked by the mana drought in the log.)

Server rebuilds clean; client refactor verified by re-read. Re-export + restart the server,
top up mana, and re-run the checklist.

## Playtest round 2 (2026-06-09) — the mana drought was a tooling bug

Round 2's "buff doesn't land on target" results (Valor/Brilliance/Bear/Insight, SoW/Thorns,
bystander) were all the **same false alarm**: `server.log` shows every one rejected with
`spell cast rejected — insufficient mana` (caster pinned at mp=15). The stacking + group
fixes are still **unverified** — no peer buff actually fired.

Root cause of the unescapable drought: **Test Panel → Full Heal restored MP client-side
only** (`test_panel.gd::_full_heal` set `PlayerStats.mp` locally with a comment claiming MP
"isn't server-driven"). But the server gates mana independently, so the bar looked full
while the server still rejected every cast. Three fixes:

- **`handlers.rs` `HealSelf`** now also restores MP + stamina to full (it backs the dev Full
  Heal button; server-authoritative, so this is the only path that unblocks casting).
- **`test_panel.gd::_full_heal`** always sends `broadcast_heal_self` in launcher mode (even
  at full HP) so a drained-mana top-off reaches the server.
- **`tick.rs`** now fans a `CastFail("Not enough mana.")` on the insufficient-mana rejection
  (was a silent `continue`). The caster sees "Cast failed: Not enough mana." in the combat
  log instead of just the optimistic "You cast X". (Player-reported: the client showed "You
  cast X" with no error because Full Heal had lied the local MP to full, passing the local
  mana check while the server silently rejected.)

Server rebuilds clean. Re-export + restart, click Full Heal (now actually restores castable
mana), and the buff stacking / group-buff / SoW / Thorns rows are finally testable.

## Playtest round 3 (2026-06-10) — green, verified

Full checklist passed (`docs/playtest_notes/ally_buff_routing_checklist.md`, all `[x]`), and
`server.log` corroborates every claim:

- All 10 buffs logged `ALLY buff applied caster=47 target=48` — routed to the **target**,
  not the caster. Bless→Valor→Strength→Brilliance→Bear→Insight→Clarity→Haste all applied to
  target=48 in sequence: **multiple stat buffs stacking on one ally** (the fix).
- Self-cast: `caster=47 target=47` lines. Group-buff under PvP: both 47 & 48 `/pvp on`, then
  `ALLY buff applied caster=47 target=48 Valor` — the **`same_group` exemption** lets a
  grouped buff land while flagged. Third-client dispel via `PvP spell applied … Expose`.
- The insufficient-mana alert fired once for real (char 49 Antimagic Ward, mp=20 < 35) — the
  tester pivoted to Expose. The mana tooling + CastFail alert are working.

ALLY-target buff routing for player peers is **done and verified**. Remaining: pet buffs
(deferred — no replicated `active_buffs` on the server `Entity`). Nothing committed yet.

The ALLY-buff work was then committed to `feature/ally-buff-routing` (client `074b6de`,
server `99884e1`), excluding the user's unrelated in-progress files.

---

# Pet buffs (2026-06-10) — pets get a primary-stat model + ALLY buff routing

Player feedback: a player buffing their pet expects the same results as buffing themselves
(Strength → more pet melee DPS), so cosmetic-only stat buffs were rejected. Instead of a
pet-combat rebalance, used a **buff-delta** approach so existing pet balance is untouched.

**Layer 1 — pet stat model** (`entity.rs`): `Entity` gains `PrimaryStats` (`stats` +
`base_stats`); pets get a level-derived base on summon, world enemies stay at zero. Pet
melee = `mob.dmg + (stats.strength − base_stats.strength) / 5` (`melee_swing_damage()`) —
mirrors the player `calc_swing` STR bonus but as a *delta*, so an unbuffed pet deals exactly
its authored damage (no rebalance) and a +12 STR buff adds +2/hit just like a player. Two
unit tests cover it.

**Layer 2 — pet buff plumbing:**
- `Entity` gains `active_buffs` + `apply_stat_deltas` / `undo_stat_deltas` / `apply_buff`
  (StatBuff mutates pet stats + max_hp; refresh-by-name) + `tick_buffs` (HoT heal, decrement,
  stat-undo on expire). `attack_interval()` now divides by `(1 + haste)` and movement uses
  `move_speed()` (× highest Speed buff) — pet attack/movement are server-driven, so Haste /
  SoW have real effect (unlike players, who pace locally).
- `tick.rs`: `apply_pet_spell_buffs` applies the pet-relevant set (HoT / speed / haste /
  damage shield / stat) — skips mp-regen / accuracy / absorb (no pet mana / crit). ALLY pet
  arm calls it + fans a pet `BuffSnapshot` (target = pet id) + logs `"ALLY pet buff applied"`.
  New per-tick pet buff pass fans HealthUpdate / BuffSnapshot on HoT / expiry.
- Client: `RemotePet` mirrors RemotePlayer's buff surface (`buff_names` / `buff_durations` /
  `apply_buff_snapshot` / `buffs_changed` + countdown); `RemotePetManager` routes pet-id
  `BuffSnapshot`s; `hud.gd` shows the pet's buffs in the target frame when a pet is targeted.

**Deferred (v1 boundary):**
- **Thorns reflect on a pet** — the buff is tracked + shown, but reflecting damage onto the
  attacking enemy is deferred (a reflect-kill would need kill-credit / loot routing to the
  pet's owner — its own slice).
- **AGI / INT / WIS combat effect** — stored + buffable, but no dodge / pet-spell-power model
  yet, so they're display-only. STR (damage) and max_hp (Valor) are the live mappings.

Server compiles clean (only the pre-existing `BuffSnapshotFanOut` warning); 118/118 lib
tests pass.

**Playtested green (2026-06-10).** `server.log` confirms `ALLY pet buff applied` for
Strength / Spirit of the Bear / Valor / Haste / Spirit of Wolf / Regrowth (HoT) / Thorns,
all to pet ids (`3000000000+`) across two owners. Skeleton hit 8→10 with Strength; Valor
grew max HP; Haste/SoW changed cadence/speed; HoT ticked; expiry reverted; target-frame buff
list works; Thorns shows but doesn't reflect (expected). Full checklist `[x]` in
`docs/playtest_notes/pet_buffs_checklist.md`.

Confirmed edge: **Clarity on a pet is a no-op** — the pet path skips mp-regen (no pet mana),
so nothing applies and **no icon shows** (the server fans no snapshot). The caster's mana is
still spent (a wasted cast) — acceptable for a nonsensical cast; a client guard could be
added later. Follow-up logged: target-frame buff *icons* (currently a text list).

Not committed yet.
