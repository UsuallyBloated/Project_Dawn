# Session Notes

Developer changelogs written after each work session. Each file records what was built, which files were touched, and implementation notes or caveats. Use these to reconstruct what changed and why without digging through git blame.

Files named `session_YYYY_MM_DD.md`. Where a session produced separate lore/content and code passes, a `_code` suffix distinguishes the code-focused file.

---

## Index

| Date | File | Topics |
|------|------|--------|
| 2026-08-17 | [session_2026_08_17.md](session_2026_08_17.md) | **Phase 3 opens: the bank was never eating anything.** The highest perceived-severity item on the friends-build list, deposited items vanishing from the bank Items tab, was **not a refresh bug** despite its name. Traced the full chain and every link was already correct: the server sends a `BankItemSnapshot` after every store (`tick.rs:6519`), gdext decodes it, `BankerManager` rebuilds the cache and emits `vault_changed`, and `BankWindow._refresh_vault` repaints. The repaint ran every time and drew **nothing** — a vault cell held only an icon and a count label, `ItemData.icon` is null for **all 172** items (declared default, no `.tres` defines one, nothing assigns one at runtime), and the count label is blank for a stack of 1. A null texture over an empty label is pixel-identical to an empty slot, so the item was in the vault, in the cache and on screen the whole time, rendered as nothing. `inventory_window.gd:268-271` has always had the name-label fallback for exactly this, which is why an iconless game still renders readable bags and why only the bank showed the bug. Fix mirrors that fallback into the vault cell plus a tooltip, and clears the label on empty cells so withdrawn items leave no ghost text. Client-only, one file, needs a re-export. Siblings checked: `bag_window.gd` already had it; vendor/loot/inspect render text lists. To-Do renamed so nobody else chases a refresh bug that does not exist. |
| 2026-08-14 | [session_2026_08_14.md](session_2026_08_14.md) | **`/testpanel`, GM state from the handshake, and a build stamp.** Added `/testpanel` (alias `/gm`) as a chat command rather than a hotkey, mounting the panel on demand and **naming the deciding flags when it refuses**. That diagnostic solved the session's real bug: account `tailscale` was `is_gm=1` but character `plump` had no Test Panel while `Pockets` did. Three plausible guesses (different account, GM granted mid-session, stale executable) were all disproved by evidence. Root cause was structural, not per-character: the client cached `is_gm` from the **auth** `LoginOk`, handled only in `login.gd`, so any route into the world that skips the login screen left it `false`. Fix moves the fact onto the **world** handshake, `ConnectOk.is_gm`, wire **PD_W0026**. Also added a `build.rs` stamping `PD_BUILD_COMMIT` onto the boot line (`build="0ff0387" dev_cmds=false`) so a stale deploy is visible at a glance; the client still has no equivalent. Two silent deployment traps recorded: non-interactive `bash -c` can't find `cargo` (so the build silently doesn't run and `cp` ships yesterday's binary), and `cp` onto a running binary needs a `systemctl stop` first. **Then closed the matching client gap:** a new `addons/build_stamp` EditorExportPlugin derives the commit from git in `_export_begin()` and injects it into the PCK with `add_file()`, so **exporting IS the stamping step** and there is nothing to remember. Every on-disk alternative was rejected for reintroducing staleness (gitignored means absent on a fresh clone; tracked means it can be committed stale). Adversarial review caught the real bug pre-ship: a bare `git rev-parse` inherits the caller's CWD, and in this two-repo project an editor launched from `F:\Projects\server` would have stamped **the server's** sha into the client, plausibly and wrongly. Now pinned with `-C <root>` plus a `--show-toplevel` cross-check. Dirty uses `--untracked-files=no` (6 naive changes vs 2 real), failure is a loud `UNSTAMPED` never a blank, `gdext_net.dll` is fingerprinted separately since it is gitignored but carries the wire protocol, and `_rotate()` re-stamps so a long session's log stays identifiable. Surfaces: login footer, `debug.log` header, and `/version`. Verified by a real headless export (`[BuildStamp] stamped 09ad51e DIRTY TREE`) and the editor fallback (`09ad51e-dirty-dev`). |
| 2026-08-12 | [session_2026_08_12.md](session_2026_08_12.md) | **Respawn at bind, a real death state, and the test suite.** Closes the death-loop the first external tester hit (died, respawned on the spot beside the mobs that killed her, died again in 20s). **The root cause was not the missing bind point**: `Respawn` restored HP but never touched *position*, and since the server owns position the client's own respawn move was overridden by the next Position broadcast. Now server-authoritative (`Outcome::RespawnTeleport`): bind point, else `STARTER_SPAWN`, never the death site. Migration `0011` adds `bind_x/y/z`; the dormant `BindAtCurrentLocation` wire variant gained both a sender (gdext `send_bind_at_current_location`) and a handler; **Sister Maelis** (Soul Binder) sets the bind via a `bind_soul` dialogue action. A **death lock** refuses `Move` while dead, and binding is refused while dead. Playtest found two follow-ons: mobs kept beating the corpse (aggro snapshot filtered on `in_world` only, now `hp > 0.0` plus an aggro/threat wipe on death) and the corpse twitched on movement keys (client prediction now returns early while dead). Bind use proven by DB read, since Maelis sits only ~5.4 units from the fallback spawn. **Second thread:** `world_two_clients.rs` was not flaky. It sat at `29 passed; 13 failed` for three weeks because the fixtures predated three gates (CastSpell class/level, swing-rate limit, the `meditate` skill) and asserted pre-gate behaviour. Fixed the fixtures, not the server: 29/42 to 40-42/42. Three genuinely flaky enemy-AI tests remain, and a failure outside those three is now a real regression. |
| 2026-08-11 | [session_2026_08_11.md](session_2026_08_11.md) | **PHASE 2 COMPLETE — the game is hosted and someone else has played it.** Covers 2026-08-05 to 08-11, mostly not game code. A physical **Dell PowerEdge R720** was built from bare metal (it arrived unbootable: the PERC had no virtual disk, so the BIOS had nothing to boot and the "Windows Boot Manager" entry was a stale UEFI pointer) into RAID 10 + RAID 5 + hot spare, Ubuntu Server 26.04, key-only SSH, UFW default-deny, unattended security upgrades. Deployed to `/opt/projectdawn` under a service account via systemd, reached over **Tailscale** (`100.93.108.112`) rather than port forwarding, with UFW scoped to `tailscale0`. **The big catch: the deployed binary was six weeks stale** — `git clone` took the default branch `main` (`98728ea`, 26 Jun) while work was on `fix/xp-leveling-overflow`, 30 commits ahead, so the running server had **none of the Phase 1 exploit gates**. The only symptom was a missing `dev_cmds=` field in one log line. Both repos were also far ahead of GitHub (server 30, client 59); both pushed. Discovered mid-deploy that `server/docs/deployment_linux.md` already existed and was better informed (it carries the NTP/ConnectToken clock requirement); scoped the two docs rather than merging. Backups: nightly to a second physical array, weekly off-site to Google Drive, **restore verified** by reading a character back out. Client fixes found by running the exported build from a console: `global_position` set before `add_child` in three remote managers, and two missing `has_signal()` guards in `hud.gd`. Discord bug-report invite had expired; replaced in code, not just the README. **First external tester 2026-08-11** closed the phase and immediately found three bugs, none findable solo: the **respawn death-loop** (died, respawned on the spot beside the same mobs, died again in 20 s), the **login rate limiter forcing a duplicate account** (5/60 s tripped a real human, who made a second account rather than wait), and the **group panel showing one player's stats under another's name** (`GroupManager` still on Godot's vestigial ENet stack; fixed same day, including wiring it to the server's existing `HealthUpdate` fan-out, which needed no wire change). New docs: `docs/deployment/{r720_host_setup,server_operations,inviting_a_player}.md`, `README_FOR_TESTERS.md` rewritten. |
| 2026-07-31 | [session_2026_07_31.md](session_2026_07_31.md) | **PHASE 1 (exploit gate) COMPLETE** — the login rate-limit + auth-timing gate playtested + ticked, closing all seven gates on schedule. Verified beyond the tick: `server.log` showed no auth activity and the DB no new accounts, both reconciled (throttle rejections were debug-level; `NameTaken` consumes a slot before `create_account`, so re-used names throttle without writing rows). **Gap found + fixed** (server `339ea97`): throttles now log at INFO with the client IP, so brute-force is visible in `server.log` for the hosting phase. Two playtest-only bugs closed this cycle: the loopback exemption (untestable + unprotected locally, `6cddff2`) and Register never throttling (launcher auto-login cleared the shared counter; now per-`(IP, AuthKind)` budgets, `fcfdf9a`). Also wrote `handoff_account_admin.md`: `admin_report` already lists accounts; the real gap is write tooling (`is_banned` is enforced but unsettable, no account delete/purge). **Then server-authoritative procs PLAYTESTED + TICKED** (PD_W0025): proc + fire flash + named line, real damage, proc killing blow grants loot/XP once (verified: 64 loot bags, all `bag_id`s unique, none in the same second — a double cascade would show as either), spells/non-proc weapons unaffected. Tester's "hitting and proc-ing at the same time" is the intended design (the proc rides the swing: two numbers, ONE combined HP drop; `proc_dmg` subtracts exactly once at tick.rs:3331). **0 swing-rate rejections this run vs 6 last time** proves the client's old proc Attack is gone. To-Do item retired, replaced by the two deferred follow-ups (proc resist, PvP procs). |
| 2026-07-30 | [session_2026_07_30.md](session_2026_07_30.md) | **Server-authoritative weapon procs (PD_W0025).** Procs moved server-side (was client-driven: a second Attack that double-hit and the swing-rate limit now drops). Cross-boundary build (protocol + gdext GDExtension + server + client + data): the server rolls `proc_chance` on a landed swing and folds `proc_damage` into that swing's HP/aggro/death cascade (proc killing blow credited + looted once), sending a private `ProcTriggered` the client renders as the named "Flaming Strike for N (Critical!)" hit; client-side roll removed. Fixed a live data bug (Flamebrand was tagged ICE via the SpellData enum offset — now FIRE). Reads the server-equipment weapon (unspoofable). Adversarially reviewed (correctness + regression clean; one LOW deploy note re min_client_version). +2 tests → 178 lib; headless import clean; gdext rebuilt/deployed. **Needs a client re-export** — `server_procs_checklist.md`. Server `b0bdbdd` / client `ad145c0`. |
| 2026-07-29 | [session_2026_07_29.md](session_2026_07_29.md) | **Skill-display fix ticked; melee swing-rate limit built (adversarially reviewed).** Skill-display-on-relog playtested clean (client `0f0489b`). Then the last combat gate: a per-hand server swing-rate limit (server `335b5b1`), built via two workflows — an *understand* survey (server resolver / weapon-delay-haste model / client cadence) and a 3-lens *adversarial review*. New `conn.last_swing_at[2]`, per-hand min interval from the equipped weapon's `weapon_delay`, assumes max haste, drops "too fast" same-hand swings silently, stamps only on accept. Review (6 findings) folded in: **HIGH** — reject an off-hand Attack with an empty slot 1 (forged free fist damage stream); grace 0.25→0.35 for internet jitter; extracted `swing_too_fast()` + tests (175 lib). Proc interaction scoped to a new **server-authoritative procs** To-Do (pre-change procs double-hit as a full weapon roll; the gate drops the client's proc Attack). **PLAYTESTED + TICKED 2026-07-29**: regression sweep all `[x]`; server.log had exactly 6 rejections, all Flamebrand (delay 2.5) main-hand = the proc's second Attack, i.e. zero base-swing false positives, dual-wield clean. Phase 1: **7 gates ticked, only login rate-limit remains.** **Then built the login rate-limit + auth-timing gate** (server `a75d9d0`, the LAST exploit gate): per-IP attempt cap on Login+Register (`LoginRateLimiter`, 5/60s, atomic check-and-reserve, loopback-exempt, success clears) + a dummy Argon2 verify on the no-user path so timing can't leak account existence. Adversarial review (3 lenses, 9 findings) caught a concurrency TOCTOU (fixed → atomic `try_acquire`) and that Register was an unthrottled enumeration oracle (fixed); documented the friends-build residuals. +1 test → 176 lib + 3 auth-smoke. Pending the **final Phase 1 playtest** (`login_rate_limit_checklist.md` — rate-limit needs a non-loopback IP — plus the deferred haste spot-check). **All Phase 1 gates now built.** |
| 2026-07-23 | [session_2026_07_23.md](session_2026_07_23.md) | **`Attack` weapon_path gate ticked + skill-display-on-relog fix.** Processed `attack_weapon_path_checklist.md`: sections 1-5 (equipped melee + swap, unarmed, dual-wield + off-hand removal, ranged reach, out-of-range Miss) all `[x]`, forged-cast `[-]` as expected. **Ticked** finding 5: moved to the Security "done" blockquote (server `9089b4b`), un-struck the "Assorted trust gaps" bundle, updated schedule §7 + status row + a `systems_overview.md` note. **Bug the tester flagged, fixed:** weapon/armor/casting skills showed `1 / x` after relog until the next advance. Diagnosed (against source) as a stale DISPLAY, not lost data — server loads + snapshots the correct scores, and the client applies them, but the character window paints skills in `_ready` at world-load (before the `SkillProgressSnapshot` arrives) and `apply_remote_snapshot` updates the cache silently (no `skill_advanced` emit, on purpose — CombatLog would log a bogus "skill increased" ×3 per login). Fix (client `0f0489b`): a dedicated `snapshot_applied` signal the character window repaints on, which CombatLog doesn't subscribe to. Headless import clean; `skill_display_relog_checklist.md` (needs re-export). Phase 1: 5 gates ticked; remaining: melee swing-rate limit, login rate-limit. |
| 2026-07-22 | [session_2026_07_22.md](session_2026_07_22.md) | **Cast gate + regen model playtested; spell-drift fix.** Playtest-review of two filled checklists + `server.log`. **CastSpell class/level gate PLAYTESTED + TICKED** (`cast_class_level_gate_checklist.md`): every regression row `[x]`, forged-cast `[-]` (stock client can't send it), and the log confirms **zero** `class/level not eligible` lines all session (no misfire on any legit cast). Moved to the Security "done" blockquote (server `a96826d`) + `systems_overview.md` note. **EQ regen model (incl. Meditate) PLAYTESTED** (`regen_model_checklist.md`): standing/sitting MP, Meditate training + climb + seated MP scaling, persist-across-relog, Troll HP column all `[x]`. Two non-defect rows run down: the combat gate (seated regen suppressed 6s after damage) is correct in code (`last_damaged_at` stamped on all 5 player-damage sites + unit-tested) but the effect is 1-2 HP/tick and taking damage doesn't yet stand you up (separate To-Do); stamina untestable (nothing consumes it yet); the "grep meditate in log" row was a bad diagnostic (skill gains are wire messages, not logged). Design doc → BUILT + PLAYTESTED. **Bug surfaced + fixed: client/server spell drift** — Blood Mage Life Drain + Shadow Knight Dark Shroud did nothing (mana spent, no effect); root cause confirmed in `server.log` (5 "unknown spell" drops): both were client-only, missing from `spells.toml`. Dropped by `lookup` **before** the cast gate (so unrelated to it). Ported both as base-class ENEMY spells (server `e6a3542`; Life Drain lifetap already supported; DISCIPLINE_MAP already had them), +1 test → 172 lib. Re-test: `spell_drift_life_drain_dark_shroud_checklist.md` (server restart only). **Wider finding tracked:** ~32 spells remain client-only (9 PORT, prestige-class tags, Warder's Mend PET_HEAL need server support first); added a "Client-only spell backlog" To-Do + updated the "Known drift" note. **Also built (pending playtest): `Attack` weapon_path trust gate** (finding 5, server `9089b4b`) — the resolver now derives the swung weapon from the SERVER's equipment map (slot 0 main / 1 off) via `PerConnection::equipped_weapon_path`, ignoring the client's claimed `weapon_path`; all five consumers (damage, PvP+enemy range, log, weapon-skill) use it; empty slot = unarmed fists. Verified safe (equipping only routes through the server `EquipItem` intent; new chars start bare). +1 test → 173 lib; wire unchanged (no re-export); `attack_weapon_path_checklist.md`. Phase 1: keystone/Respawn/attack-while-seated/**cast gate** ticked, weapon_path built-pending-playtest; remaining: swing-rate limit, login rate-limit. |
| 2026-07-20 | [session_2026_07_20.md](session_2026_07_20.md) | **Respawn dead-check playtested + TICKED** (Phase 1, exploit-audit finding 3; built 2026-07-19, server `0d908d1`). `respawn_deadcheck_checklist.md`: normal death→respawn regression all `[x]`; the exploit side (`[-]`, needs a modified client) is covered by the green integration test `respawn_requires_being_dead`. Server log reviewed: repeated clean death→corpse→respawn cycles, no errors. Ticked: retired the `Respawn` To-Do item into the Security blockquote, updated `schedule.md` §7 + `schedule.html`, added a "Respawn (client-driven, gated)" note to `systems_overview.md` (and fixed a stale `CORPSE_LINGER_SECS`=300s bullet there → 7 days + the `ENEMY_DESPAWN_LINGER_SECS` rename). Three playtest observations captured: no server bind / no death lock (folded into the "Respawn at bind" To-Do; own redesign project, not a regression), HP/MP regen currently disabled (tester says fine), and a new bug — Test Panel "Despawn All Enemies" leaves enemies invisible not gone (added to To-Do). Phase 1: keystone + Respawn done; next is attack-while-seated. |
| 2026-07-19 | [session_2026_07_19.md](session_2026_07_19.md) | **GM keystone (per-account `is_gm`) proven + TICKED** — Phase 1's keystone, built 2026-07-17. **Integration test** (server `312fe07`): `world_two_clients.rs::is_gm_gates_dev_commands` runs the real token flow (register → flip `accounts.is_gm` → mint a fresh connect token → connect) and asserts a GM account's `GrantQuestXp` applies while a plain account's is a no-op; filters on the probe amount so the connect-time `XpGained{0}` bar-seed isn't a false positive; skips under `PD_DEV_CMDS=1`. Added `db::set_account_gm` + test helpers. **Playtest** (`gm_access_checklist.md`, all `[x]`): reviewed the saved server logs (UTF-16→UTF-8, ANSI-stripped) — run 1 shows bungus (acct 9) connecting pre-grant with no GM log, reconnecting post-grant WITH `GM account connected` and dev-spawning; the non-GM (acct 8) does nothing dev; run 3 shows the revoke. **Two flagged notes run down as NON-issues:** "Full Heal works for non-GM" = `_full_heal` optimistic client bar-fill (server `HealSelf` refused, self-corrects); "Trigger Death works" = legitimate (`_trigger_death` sets local HP 0; dying isn't a dev power). **Tick:** retired the `is_gm` To-Do item (folded into the Security blockquote), `schedule.md` §7 Phase 1 → In progress, new `systems_overview.md` "GM access + dev tooling" section, both handoffs marked TICKED. Next: Respawn dead-check. One middle server log was lost to `Tee-Object` overwrite (non-critical). |
| 2026-07-17 | [session_2026_07_17.md](session_2026_07_17.md) | **Phase 0 — Reconcile the picture** (friends-build schedule). Doc reconciliation + one server constant; worked from `handoff_phase0_reconcile.md`. **Verified 3 flagged checklists in source, not on faith:** `pet_pvp_inheritance:30-31` Inferno-AOE failure is STALE (git-blamed `3d5e05e` 2026-06-11 — pre-fix AOE scan `continue`d on ids `>= LOOT_BAG_ID_BASE` so pets, which sit above, could never be hit; the fix added pet inclusion + the `can_attack` owner gate, `tick.rs:4337-4370`); `corpse_slice3` "failed" is STALE (shipped clean 06-26 per `session_2026_06_25.md:72`; the `[-]` rows were the too-low-Cleric mana gate, the file's own "TEST-SETUP GOTCHA"); `quest_phase2_sliceB:35` is PARTIAL (orb + kill-credit complaints closed by `5ae6808`, but the Hunter's Medal item was never re-tested → left open). **Server:** `corpses::CORPSE_LINGER_SECS` **300s → 604800s (7 days)** — user's design call, EQ-authentic, off the 5-min test value that lost gear on a first bad death; comment flags the duplicate-name trap (`world/mod.rs` 5.0 = dead-enemy despawn, untouched); `cargo check` clean; wants a playtest before it ticks. **`CLAUDE.md` To-Do:** retired quest phase 2 (shipped A/B/D; "what exists" moved to `systems_overview.md`), closed the "CompleteQuest doesn't verify objectives" security item (slice A gates it), added the schedule's orphans (`is_gm` keystone, a new Hosting/deployment category, the 4 Phase-3 bugs), plus 2 bugs found while counting rows (Tarnished Silver Ring AGI+1 doesn't apply on equip; Hunter's Medal re-test). **Docs:** `systems_overview.md` Quests bullet rewritten to server-authoritative; 3 checklists banner-annotated `RECONCILED`; `schedule.md` §7 Phase 0 → In progress (HTML is a Gantt with no status column — no change needed). Not committed. Phase 0 closes when the timer is playtested; **do not start Phase 1**. |
| 2026-07-11 | [session_2026_07_11.md](session_2026_07_11.md) | **Exploit audit (read-only security review).** Both repos surveyed on `fix/xp-leveling-overflow`; **no code changed**. Deliverable: [../security/exploit_audit_2026-07-08.md](../security/exploit_audit_2026-07-08.md) (file's date label reads 07-08; review ran 07-10 into 07-11). Method: five parallel read-only passes (economy; item dupe/theft; combat/progression; session/auth/movement; full client-to-server trust-boundary enumeration), each re-verified against source before writing; all five converged on `/give`. **Findings (nothing fixed):** Critical ungated `/give` (GmCommand mints any item free + persisted, also unlimited coin via resale; dev command missing its gate; reachable from the stock client). High: no server swing-rate limit (attack-speed hack), `Respawn` floors HP to 25% while alive. Medium: `CastSpell` class/level ungated except resurrection, `Attack` trusts client `weapon_path`, no login rate limiting, `BuyItem` ignores vendor_id. Low: non-atomic cross-store persistence (crash-window dupe), username enumeration, minor cosmetic/info-disclosure. Also confirmed (adjacent to quest work, kept out of the doc): `CompleteQuest` doesn't verify objectives (server checks id-exists + not-already-done only). Dev tools flagged for separate triage; operational caveat that `is_dev` is process-wide `PD_DEV_CMDS`. Confirmed server-authoritative: identity, auth crypto, movement, damage/heal magnitudes, XP totals, economy math, loot rights, resurrection, duplicate-login refusal, camp/linkdead. Not committed. **Also (2): first per-race character model** — Gnome `gnome.glb` blockout wired via new `scripts/race_model.gd` (`RaceModel.apply` on local + remote players; cosmetic only, no rig/anims yet; camera/collision still capsule-sized). **Also (3): race expansion design pass (docs only)** — three in-design playable races framed with race files + draft stat blocks (`race_stats.md` In Design section): Aerathi "the Stoop-kin" (cliffside raptor martial, AGI 99), Vesperin (canopy songbird bard/caster, CHA 95), Sylphari (plant-blooded grove keepers, WIS 95); NPC factions greenlit (Sporeborn, Saurian, bog-stalkers, canopy-dwellers); faction-driven appearance-change idea captured (mushroom features at high Sporeborn standing, per-faction commitment markers). Working doc `docs/design/race_expansion_brainstorm.md`. |
| 2026-07-10 | [session_2026_07_10.md](session_2026_07_10.md) | **Quest phase 2 slice B — server-granted item rewards.** Both repos, **no wire change** (item grants ride the existing `InventoryDelta` + `LootGranted`; a full bag reuses `QuestRejected`, so no protocol bump / no DLL rebuild). Makes the 3 authored reward items real + server-granted (closes plan root-problem #3). Client: authored `tarnished_silver_ring` / `scouts_leather_boots` / `hunters_medal` `.tres` (stats mirror `quest_definitions.gd`) + regenerated the server `items.toml` (hand-reduced to the 3 lean entries to avoid pre-existing exporter drift); the reward arrives via the loot `InventoryDelta` path with NO launcher-mode client code (the Test-Room `_make_reward_item` grant stays gated, so no double-grant). Server: `Quest.item_rewards` (parser rejects a path not in the registry), `PlayerInventory::can_accept` (non-mutating pre-flight mirroring `add_item_locating`), and the turn-in grant — **capacity pre-check BEFORE the once-ever `record_quest_completion`** (a full bag refuses the whole turn-in: no completion, no XP, no item, quest stays Ready), then grant + fan deltas + a **forced `save_inventory`** so a crash after the durable completion can only LOSE the item, never dup it. 163 lib tests (+3). Focused adversarial review of the grant/dup/capacity surface = clean. **Playtested clean** (`quest_phase2_sliceB_checklist.md`, both logs preserved): ring grants, full-inventory rejection fires + recovers, survives relog + server restart, no dup. Triage of 3 tester findings confirmed all are **pre-existing / dev-tool, not slice-B bugs**: (1) gear stat bonuses don't display (server applies AGI but no primary-stat wire message + `apply_remote_equip` skips local recompute — hits all gear), (2) Test Panel "ghost items" (client-only `Inventory.add_item`, server sees slots empty), (3) Test Panel Rotfang spawns client-local (no `DevSpawnMob` → no credit / golden orb). **Committed.** **Next:** gear-stat display fix, then dev-panel modernization; slice D (Brom's dialogue) still open. |
| 2026-07-09/10 | [session_2026_07_09.md](session_2026_07_09.md) | **Quest phase 2 slice A — server-side objective tracking.** Both repos, **PD_W0023→PD_W0024**, one server + one client commit. Moves quest objective STATE off the client: the server counts quest kills itself (`active_quests` table, migration 0010; `award_kill` increments per credited member), seeds the journal from a private `QuestSnapshot` on EnterWorld (**survives relog AND server restart** — the phase-2 headline, playtest-proven across a real restart), drives progress via `QuestProgress`, and REJECTS a turn-in until every objective is met (closes the phase-1 forged-`CompleteQuest` hole — pays nothing without the kills). Wire: reuse the dormant scaffold `AcceptQuest`/`AbandonQuest`; append `QuestSnapshot`/`QuestProgress`/`QuestRejected{rollback}`/`QuestCompleted`; `KillCredit` retired (kept in enum). Classic-EQ **NPC turn-in** (user's fork pick): killing the last mob marks the quest **READY**, reward pays at the giver (dialogue gated READY); journal gains a "(Ready)" callout + an **Abandon Quest** button. Closes 5 of the plan's 7 root problems (journal wipe, invisible completed-set, field auto-complete, no-abandon, forged-completion). Adversarial review (4 finder dims, verify stage cut short by a session limit → adjudicated manually): fixed a fragile reason-string rollback (→ explicit server `rollback` bool) and a per-message DB-write/ordering hole (→ one arrival-order accept/abandon queue reconciled by a once-per-tick dirty flush). Playtest (two runs, both logs preserved) passed every quest path; folded 3 follow-ups (completed quest shows N/N not 0/N after relog; re-complete gives visible feedback; turn-in XP line reads "quest experience"). 161 lib tests. **Open: slice B** (server-granted item rewards — 3 `.tres` + items.toml) **and slice D** (wire Brom's authored rat/gnoll dialogue into the scene). Bank item-deposit display bug found during regression spot-check = **pre-existing, not a quest regression** (server persists the item; client BankWindow Items-tab refresh), deferred. |
| 2026-07-07/08 | [session_2026_07_08.md](session_2026_07_08.md) | **Quests go online + the quest-XP exploit fix.** Both repos, **PD_W0022→PD_W0023**, one changeset of four features. (1) **Kill credit online**: killer-private `KillCredit{mob_name}` → `QuestManager.notify_kill`, so "kill N X" objectives finally advance in launcher mode (witnesses excluded by design). (2) **Dev world-spawns**: Test Panel spawns go through the server (`DevSpawnMob`, PD_DEV_CMDS-gated) — real combat/XP/loot/corpse/quest-credit mobs, one-shot; wolf renamed **"Grey Wolf"** + warder-template names rejected (a charmed bare "Wolf" armed the free warder-respawn loop); NaN guards. (3) **Group-split unification**: one `award_kill` for all 3 kill paths — grouped spell/pet kills now split XP + credit like melee (playtest caught melee-only); **owned victims award NOTHING** (pet kills paying XP = infinite 15s-respawn faucet). (4) **User-flagged exploit KILLED**: `GrantQuestXp{amount}` (client names its own XP — one packet = level cap; + the relog re-turn-in farm) replaced by `CompleteQuest{quest_id}` — server computes the reward from its own `quests.toml` (tier% × band(level_req), level_req enforced), persists to a new `completed_quests` table (migration 0009) BEFORE paying: **once per character, ever**, proven across relog AND server restart in the log; `GrantQuestXp` now dev-gated (Test Panel buttons). Quest item rewards gated to Test Room (were ghost items online). 3 adversarial review workflows folded; full integration suite run (fixed a stale `mob.xp=10` assertion → `kill_xp(1)=263`; 40/40 solo); real-world.db boot smoke (0009 confirmed; backup kept). Playtested fully (`quest_kill_credit_checklist.md`), storm-interrupted and resumed. Also 07-06: **quest XP tier re-tune committed** (`315a156` — tier% × band(level_req) model, REWARD_TIERS knobs) + EQ reference doc research (`6b2f029`: quest/faction two-track + racial XP/regen trade-offs). Open: quest phase 2 (server objective tracking — journal persistence, completed-set sync, item rewards, NPC turn-in gating), bug triage queue (sell-dupe, corpse despawn, crit-for-3). |
| 2026-06-26 | [session_2026_06_26.md](session_2026_06_26.md) | **EverQuest-authentic XP leveling overhaul.** Both repos, `fix/xp-leveling-overflow`, **no wire change** (xp stays i32, PD_W0022, no DLL rebuild). Fixed the Slice-0 bug where the geometric **×1.5** curve saturated `i32::MAX` (~2.1B) at ~level 43 (the giant res-refund numbers). Went through an interim **×1.25 flatten** (playtested clean) then **superseded by the full EQ model** after a 3-vote-verified research pass (`everquest_xp_curve_reference.md`): **per-level cost CUBIC + hell levels** (`total(L)=L³×1000×hell_mod(L)`, reproduces the P99 table exactly — band(1)=1,000, band(30)=5,311,000, band(60)=53,463,000; bands **non-monotonic by design**, band(59)>band(60) = the L59 triple-hell) and **per-kill reward QUADRATIC** (`kill_xp = mob_level²×ZEM×3.5`, ZEM 75; the 3 `tick.rs` kill sites derive from mob level, not the flat `mob.xp`, now legacy). Kills/level ~11 normal, spikes on hell levels. `MAX_LEVEL=60` cap + `saturating_add` + `db::load_character` reconciliation of old-curve saves; client `player_stats.gd` cubic mirror (**lockstep verified byte-identical**). 152 lib tests, clean boot, **2 adversarial review workflows** (the ×1.25 + the cubic pass), all findings folded in. Playtested clean (kept EQ-exact ZEM 75). Also: branch hygiene (FF'd the stale `feature/ally-buff-routing` onto trunk, started this fresh branch). Open: **quest `xp_reward` re-tune** (now under-rewards), per-zone ZEM. Checklist `eq_leveling_checklist.md`. |
| 2026-06-25 | [session_2026_06_25.md](session_2026_06_25.md) | **Corpse epic Slice 3 — Cleric/Paladin resurrection (PD_W0021→PD_W0022).** A res spell targets a corpse; the owner (must be online) gets an **Accept/Decline** offer; on accept they're **teleported to the corpse + refunded a %** of that death's lost xp (Cleric `Resurrection (Minor)`/`Resurrection`/`Resurrection II` = 25/50/75%, Paladin `Reclaim Soul` = 20%). No res-sickness v1; **one-res-per-corpse** (persisted `resurrected` flag); lost xp captured as **ACTUAL removed** (closes a level-5-floor over-refund exploit) onto `Corpse.lost_xp` + migration `0008`. New `CORPSE` cast arm + `ResurrectAccept`/`ResurrectOffer`/`Teleport` wire (gdext rebuilt); Test Panel **"Level Up"** button. 4-dimension adversarial review (5 minor, all fixed); playtest confirmed all 4 tiers + every guard + persistence. **Committed** (server `98728ea` / client `a04ed07`). Completes the corpse epic core. |
| 2026-06-24 | [session_2026_06_24.md](session_2026_06_24.md) | **Corpse epic Slice 2 committed** (server `403ed9d` / client `286e2a4`) after playtest + a pre-commit review fix (folded the corpse delete into `apply_corpse_loot`'s tx, revert-on-failure, +3 tests) — detail in `session_2026_06_23.md`. **Monster orb to corpse — BUILT, awaiting playtest (PD_W0020→PD_W0021).** Slain creatures now leave a **body** ("<creature>'s corpse", white nameplate) instead of a golden orb, keeping all loot-bag GROUP mechanics. Scoped via 3-agent recon: VISUAL unification only (loot mechanics stay on `LootBag`). **Server:** appended `creature_name: String` to the END of `LootBagSpawn` (append-only-safe), plumbed through `LootBag`+`new`+`fan_out`, the 4 kill sites (mob name) + player-drop site (empty) + test helper, and gdext (signal 9 args / Incoming / decode / emit). **Client:** new shared `scripts/corpse_body.gd` (`CorpseBody.build`) builds the gray capsule + **white** "<name>'s corpse" nameplate, used by both `corpse.gd` (player corpse, was reddish) and `loot_bag.gd`; `loot_bag.gd::_ready` branches body (name present) vs golden sack (empty = dropped item); `net.gd` + `remote_loot_bag_manager.gd` carry the name (re-snapshot leaves the built visual alone). 146 lib tests, clean boot, **2-dimension adversarial review = 0 findings**. Per-creature model/scale still deferred. **Not committed** (`monster_corpse_checklist.md`). |
| 2026-06-23 | [session_2026_06_23.md](session_2026_06_23.md) | **Corpse epic, Slice 1 — the corpse run (corpse entity).** Cross-wire, both repos, **PD_W0018→PD_W0019**. Dying now leaves a **persisted, owner-only corpse** holding all gear + carried coin; you respawn **naked**; the corpse renders with a "<name>'s corpse" nameplate, survives relog + server restart, and **decays harshly** (gear gone for good) after `CORPSE_LINGER_SECS` (300s test value). A corpse = "a `LootBag` that persists + decays harshly"; opened with a 3-agent recon, then mirrored. **Server:** migration `0007_corpses.sql` (`corpses` + `corpse_items`); `world/corpses.rs` (`Corpse`); `db::{save_corpse,load_corpses,delete_corpse}`; `inventory::{all_stacks,clear_all}`; corpses mint from the loot-bag id partition with `loot::reserve_bag_ids_through` guarding post-restart id reuse; `tick.rs` boot-loads corpses before any login, and the death sweep does the **atomic gear move** (save_corpse FIRST so a crash can't lose gear → on failure skip the strip; then clear+recompute+zero-coins+save_inventory(empty)+save_coins(0), fan the emptied snapshot/gear-free resources/zeroed wallet, insert corpse map+AOI, fan `CorpseSpawn`); corpse decay sweep (despawn+map/AOI remove+`delete_corpse`); corpses also seed on EnterWorld + AOI cell-entry and despawn on AOI exit. Wire: `CorpseSpawn{corpse_id,owner_id,owner_name,pos}` + `fan_out_corpse_spawn`; gdext decode + `corpse_spawn` signal; DLL rebuilt (4.09 MB). **Client:** `scripts/corpse.gd` (`Corpse` body+nameplate, render-only), `autoloads/remote_corpse_manager.gd` (mirrors RemoteLootBagManager, shared-partition despawn guard, "Your corpse rests where you fell." to the owner), `net.gd` `world_corpse_spawn` wiring, `combat_log` "You have died.", autoload registered; naked respawn needs no new client code (emptied snapshot + CoinsUpdate(0) already clear it). Verified: 143 lib tests + migration integration test green, clean editor boot. **Not committed — awaiting playtest** (`corpse_slice1_checklist.md`). Caveat: cross-zone-bind corpse rendering (tied to deferred server respawn). Slice 2 = loot your own corpse. |
| 2026-06-22 | [session_2026_06_22.md](session_2026_06_22.md) | **Corpse epic, Slice 0 — server-authoritative XP / leveling + death penalty.** Cross-wire, both repos, **PD_W0017→PD_W0018**. The foundation for the corpse run: move XP, level-ups, AND the death penalty server-side so an authoritative XP loss + de-level can't be rolled back (closes [[project-level-up-server-desync]]). Fixed the real bug after verifying the code (a recon agent wrongly claimed the server awarded no XP): the server **did** award kill XP but `send_xp_gained` was a pure notification (`current`/`to_next` hardcoded 0, never touched `conn.xp`/`level`), so the server's level stayed frozen while the **client** drove `_level_up` — then the server's `HealthUpdate` (max stats from the stale server level) rolled it back. Verified the client `CLASS_LEVEL_GAINS` and server `char_data::level_gains` are in full lockstep (all 18 classes + structure + 1.5x curve, anchor-tested) so the authority move shifts no stats. **Server:** new `world/progression.rs` — `award_xp` choke point (mutates `conn.xp`, resolves level up/down via a pure unit-tested `resolve`, applies the per-level **intrinsic delta** add-only so gear/buffs survive), `apply_death_penalty` (5% of band, cascade, **grace + floor at level 5**, locked 06-22), `kill_player`; 7 unit tests. New `char_data::xp_to_next_for`. `send_xp_gained` carries real `current`/`to_next`; new `send_level_up`; `GrantQuestXp` handler routes client-tracked quest XP through `award_xp`. **Post-combat death sweep** (`tick.rs` step 5a-bis) + `death_processed` flag = server-side death detection; `DeathBroadcast`/`Respawn` rewired (the client broadcast now only covers non-simulated deaths). The three kill-credit sites call `award_xp`. No resource-fan on level-up (client applies the max delta itself; `HealthUpdate` stays the idempotent backstop, avoids a double-count). **Wire:** inert `LevelUp` reshaped to `{new_level, xp, xp_to_next}` (up on gain, DOWN on penalty); `GrantQuestXp{amount}` appended; gdext decode/`level_up` signal/`send_grant_quest_xp`; DLL rebuilt. **Client:** `player_stats.gd` `_level_up` split into `_apply_level_gain`/`_apply_level_loss` + new `apply_server_level` (intrinsic delta up/down, base+public, no local heal) + `apply_remote_xp`; `net.gd` mirrors XpGained/level_up; `quest_manager` sends `GrantQuestXp` in launcher; `player_death` lost its 5% local loss; Test Panel **Grant 250 XP** aid. Verified: 143 lib tests + kill-credit integration test green, clean editor boot. **Not committed — awaiting playtest** (`corpse_slice0_checklist.md`). Migration note: existing chars show their server-stored (likely low) level on first PD_W0018 login. |
| 2026-06-19/20 | [session_2026_06_19_camp_linkdead.md](session_2026_06_19_camp_linkdead.md) | **Camp + Linkdead (BOTH slices, EQ logout model).** Both repos. **Slice A (06-19, no wire change):** an unclean drop (crash/killed client) leaves the body **linkdead** — lingers ~30 s in-world, still targetable + **killable**, frozen — and refuses a same-account relogin ("You already have a character in this world." + countdown) until it reaps; a clean **Quit Game** reaps immediately. Server `reap_connection` split + linkdead reaper sweep + `linkdead_since`/`clean_disconnect`; client window **X button** rewired to a hard self-kill (`OS.kill` own PID, kills only that instance) to drive linkdead; `server_design.md` reconciled. **Slice B `/camp` (06-20, PD_W0016→PD_W0017):** the voluntary sit-gated ~30 s logout — `/camp` (gated on seated) starts an amber countdown, cancelled by stand/move/damage (server camp sweep: `!is_sitting` or `last_damaged_at >= camp_since` via 5 hooked damage sites; death clears it too), `Camp`/`CancelCamp`/`CampUpdate` wire + gdext `send_camp`/`camp_update` (DLL rebuilt). **Completion is client-driven** (client runs the Quit-Game clean logout at 0; exits to desktop — no in-game return-to-lobby yet, so a server kick would strand the player). Recon + adversarial review both run as multi-agent workflows; review caught 3 real bugs (same-tick `>`→`>=`, death-not-aborting-camp, the completion handoff), all fixed. Verified: 136 lib tests, clean editor compile, **two-client playtest both slices**. Checklists: `linkdead_camp` rows; `camp_checklist.md`. | Both repos, **no wire change**. The EQ involuntary-logout half of `docs/design/camp_and_linkdead.md`: an unclean drop (crash/killed client) now leaves the body **linkdead** — lingers ~30 s in-world, still targetable and **killable**, frozen — and refuses a same-account relogin ("You already have a character in this world." + `reconnect_after_secs` countdown) until it reaps; a clean **Quit Game** still reaps immediately. **Server:** `LINKDEAD_SECS` + `linkdead_since`/`clean_disconnect` on `PerConnection` (+ `linkdead_expired`, 2 reaper-timing tests); old inline disconnect cleanup factored into a `reap_connection` helper; `ClientDisconnected` branches clean→reap vs unclean→linkdead (pet despawns now, group kept for the window); per-tick reaper sweep; deny-login fills `reconnect_after_secs`. Caught + fixed a self-inflicted bug (a refused same-char relogin shares `client_id`==`char_id`, so its stray disconnect reset the reap timer → now ignore disconnects for already-linkdead conns). **Client:** the window **X button** is rewired to a hard self-kill (`OS.kill(OS.get_process_id())`, no clean `Disconnect`) so it drives the linkdead path and kills only that instance; **Quit Game** is unchanged = clean logout. `server_design.md` reconciled (stale 60 s seamless-resume section replaced). Verified: 136 lib tests, clean boot, **two-client playtest** (X→linkdead, enemy hit the body 87→54 hp, reaped at 30.0 s; Quit Game reaped instantly). **Slice B `/camp` still open.** |
| 2026-06-19 | [session_2026_06_19.md](session_2026_06_19.md) | **Banker NPC slice 2 — item storage.** Cross-wire, both repos, **PD_W0015→PD_W0016**. Adds a 10-slot per-character item vault (char-keyed `bank_items`) + a 2-slot account-shared vault (account-keyed `account_bank_items`, EQ shared bank), one reusable `ItemVault` struct (deposit stack-merge/capacity/take/restore; 4 tests). **One character per account is now enforced** (a duplicate login is **refused** — the session already playing is never booted, avoiding the Lineage II re-login force-off exploit; this replaced an initial kick-the-older-session model after playtest feedback), which keeps the shared vault single-owner. Deposit gesture = right-click an inventory/bag item while the bank is open (the quick-transfer from the new `inventory_interaction_grammar.md`); withdraw = right-click a vault slot; both move whole stacks. Server store sizes by vault room then debits inventory by the actual removed count (no dup); withdraw refunds overflow straight back to the source slot (no loss); bag items rejected. Wire: `BankStoreItem`/`BankWithdrawItem` (string-location, not SlotRef — gdext can't encode tagged enums) + `BankItemSnapshot` (full vault per op). Client: `BankWindow` gained Coins/Items tabs (10 personal + 2 cool-blue shared slots, Personal/Shared toggle); coins also got per-tier clickable chips for right-click quick-transfer (slice-1 buttons kept). Adversarial review caught **1 real bug** (enforcement clobbered the new session's shared vault if the pre-kick flush hit a DB error) plus hardening nits, all fixed. Verified: 134 lib tests, clean boot, `CURRENCY_SMOKE_PASS`. Checklist: `banker_slice2_checklist.md`. Full cursor grammar (partial grabs, split, drag) is a separate deferred track. |
| 2026-06-17 | [session_2026_06_17.md](session_2026_06_17.md) | **Banker NPC slice 1 — coin banking + tier exchange.** Cross-wire, both repos, **PD_W0014→PD_W0015**. The Banker (Thalia Mourne, town hub) gives **zero-weight coin storage** (deposit/withdraw) + **tier exchange** (convert coin between tiers) — the relief valve for coin weight. Coins-only MVP (item storage + the 2 account-shared slots are slice 2); **0% fee** + **free** banking; exchange on the carried wallet. **Server:** `0005_bank.sql` (four `bank_*` columns) + `bank_coins`/`bank_dirty` on `PerConnection`, persisted via checkpoint + disconnect flush + enter-world `BankSnapshot` seed; new `Coins` math (`exchange`/`has_at_least`/`add_each`/`sub_each`/`has_negative`, saturating, value-preserving, 6 new tests); PD_W0015 wire (`Bank{Deposit,Withdraw}Coins`/`BankExchange` + `BankSnapshot`/`BankRejected`, appended at enum ends) + tick apply loops that guard against minting and fan `CoinsUpdate`+`BankSnapshot`. **gdext** senders/signals + DLL rebuilt. **Client:** `net.gd` broadcasts/signals; `BankerManager` autoload (caches bank balance) + `BankerNPC`/scene + `targeting.gd`/`hud.gd` routing; new `BankWindow` (wallet + bank rows, per-tier deposit/withdraw, exchange), server-authoritative. **Adversarial review** (4-dimension finder→verify workflow) caught **1 real bug** — `targeting.gd` never targeted `banker_npcs`, making the BankWindow unreachable — plus 4 minor (all fixed bar a documented proximity-gate nit). Verified: 130 lib + 14 protocol tests, clean boot, `CURRENCY_SMOKE_PASS`. Checklist: `banker_slice1_checklist.md`. |
| 2026-06-16 | [session_2026_06_16.md](session_2026_06_16.md) | **Loot/group polish — 3 deferred Round-2 items** (from `handoff_loot_group_polish.md`). Both repos `feature/ally-buff-routing`; wire stays **PD_W0014**. **(1) Drop item to ground** — wired the existing-but-callerless `Net.broadcast_drop_item` via **two** gestures: a ⬇ **Drop cell** beside the Trash cell + **drag-out-to-world** release, both confirm-dialog'd and server-authoritative (no optimistic clear). Server `DropItem` handler extended from base-only to **bag slots** via the shared bag-aware `inventory.destroy_at` (no wire change); `drop_base` thereby became dead → removed + its 5 tests migrated to `destroy_at` (130 lib tests, unchanged). **(2) Defender sees incoming misses (PvP)** — client-only: server already fanned `Miss` to the defender; `remote_player_manager._on_miss` now logs "X misses you." symmetric to `_on_hit`'s incoming "X hits you" (new `_attacker_display_name` helper). **(3) `/autosplit` notifies the group** — new `ServerWorldMsg::GroupNotice { text }` (PD_W0014 append) + `send_group_notice`; inline `SetAutosplit` handler no-op-guards and returns `Outcome::AutosplitNoticeIntent`, a tick drain loop (mirrors 4hbb) fans "X set auto-split on/off." to the toggler's **other** online group-mates (toggler keeps the `hud.gd` local echo, no double-log; solo fans nothing); gdext `group_notice` signal + `net.gd world_group_notice` + `group_manager` log. **DLL rebuilt** (4.01 MB). Verified: server clean build + 130 lib tests, headless boot clean, `CURRENCY_SMOKE_PASS`. Two-client rows added to `group_loot_coin_checklist.md` ("Round 2 — Deferred follow-ups"). |
| 2026-06-15 | [session_2026_06_15.md](session_2026_06_15.md) | **Group loot rights + coin drops (the economy faucet).** Server-authoritative, both repos, wire **PD_W0013→PD_W0014**. Coins now drop from mobs by tier (`loot::roll_coin_for_mob` per `currency.md`; beasts 0) and show as a gold row in the loot window. Corpses are **owned by the kill-creditor's group** (`LootBag.owner_killer`/`can_loot`) — strangers refused ("That isn't your loot."). Per-group **loot mode** (`LootMode`, leader-set, default Round Robin): RR rotates item turns per corpse (lazy `next_loot_turn` claim; "Not your turn to loot.") + **auto-splits coin to group members within 30m** of the corpse (remainder→looter); **FFA** = anyone loots, coin to the looter. Per-player **`/autosplit on\|off`** (off keeps coin you loot). Built in layers L1 ownership+gate (`e779b17`) → L2 coin drops+split (`78ca35e`) → L3 RR turns (`3178588`) → L4 wire+client 3 slices (`622fd33`/`4534aa1` /autosplit; `f7c190b`/`f439361` loot-mode + `/loot` + panel `[RR]/[FFA]`; `7873527`/`7bcb011` corpse coin + `LootRejected`). Client cmds in `hud.gd`; gdext-net DLL rebuilt to PD_W0014 each wire slice. XP split stays distance-agnostic (deliberate vs coin's proximity gate). Verified: 129 server lib tests, clean client boot, `CURRENCY_SMOKE_PASS` per slice. Also **fixed the long-stale CLAUDE.md "server is auth-only" line**. Checklist: `group_loot_coin_checklist.md` (two-client). `world_two_clients.rs` flakiness investigated + documented (pre-existing, not this work). |
| 2026-06-12 | [session_2026_06_12.md](session_2026_06_12.md) | **Currency & encumbrance visibility UI.** Client-only, from `handoff_currency_ui.md` — the systems worked but players couldn't see them. Wallet line in the inventory window (`Currency.format_coins` from **actual stacks**, never reduced; live on `PlayerStats.coins_changed`; matches the vendor footer, both now on `UITheme.C_COINS`). Character window AttribGrid gained the deferred `WT` row — `Encumbrance.total_weight / capacity`, yellow (`C_ENCUMBERED`) over capacity, red (`C_OVERLOADED`) at ≥ 2× (`OVERLOADED_RATIO`), threshold-explaining tooltip. Stretch 1: HUD warning label under the stat panel, visible only while over capacity ("Encumbered 24.3 / 20.0" / "Overloaded!"). Stretch 2: item-weight starter — 20 of ~169 `.tres` items tagged (weapons 2–8, armor 1–10, potions 0.3, reagents 0.1); full pass stays backlog #1 (user in loop). Verified: `CURRENCY_SMOKE_PASS` + clean headless boot. In-game eyeball of the three UI pieces is the next playtest's job. **Then (cont.): full item-weight content pass** — all 169 `.tres` items tagged (149 were untagged). New `tools/item_weight_audit.gd` (headless coverage check). Proposal doc `docs/design/item_weight_proposal.md` built per-category against the 20 anchors, adversarially reviewed (recipe graph, gameplay-pressure math vs creation stats, hostile-tooltip pass), then user-ruled: **chain armor rescaled ~45%** incl. three starter anchors (iron vest 10→6, leggings 8→5, copper coif 4→2.5 — a fresh Human Warrior was encumbered standing still at the old values); ore 3.0 / ingot 1.5 confirmed then the whole smithing family rescaled ÷12 later same day (ore 0.25 / ingot 0.1, smelt compression preserved); universal 0.1 floor, no zero-weight items; **Arrow Bundle `stack_size` 200→20** (was 4,000 arrows = 200.0 per stack); gold ingot 2.0, steed whistle 0.1, meats 0.25. Audit: 169/0 untagged; boot + smoke clean. Checklist: `item_weights_checklist.md`. Follow-ups: **Fae gear line** (user direction — Fae are smaller than gnomes, need their own clothing category; related: no creation-stat floor → Fae caster capacity 5.0 < the 5.5 cloth kit). |
| 2026-06-11 | [session_2026_06_11.md](session_2026_06_11.md) | **Atlas binding pass — maps review + art-commission prep.** Docs/lore only. Full review of the atlas + twelve-plate annex, then applied everything: errata (Second/fourth edition contradiction fixed; **Bone Citadel adjudicated ✦→✧** on Atlas/Plate I/Plate III — network stays 11 confirmed nodes; stale "when issued" cross-refs; Plate VII's Maren pointer IX→X; Plate VIII prefatory counts A–E; glyph hygiene incl. IX-D `®®` corruption); cartouches (six-fingered rose + scale) on all 12 plates; **Visual Character blocks for Plates I–VIII**; Plate VII lane schematic (lanes finally drawn port-to-port); **vignette volumes IX-B.i/C.i/D.i** (6 each, matching Greenshire's IX-A.i). New docs: `maps/drafting_standards.md` (Hall style sheet; six-fingered rose + quartered seal canonized), `maps/appendix_league_table.md` (travel paces + hub distances), `maps/corrections_847.md` (in-world errata record; the 4 cross-race name collisions were ruled same-session: **Vrekka & Threkka canonized as a standing dispute** — `vrekka_and_threkka.md`, Year 839 CR ledger-error origin, the pairs never meet on-screen; Mireth + Kethri ruled coincidence, closed), `docs/design/map_commission_briefs.md` (per-plate Gemini/Nano Banana briefs; art doubles as in-game map assets), `greenwarden_veneration.md` (the Greenshire regional deity, deliberately unresolved). |
| 2026-06-09 | [session_2026_06_09.md](session_2026_06_09.md) | **CLAUDE.md rewrite + systems overview.** Documentation-only. Rewrote root `CLAUDE.md`: fixed Godot 4.3→4.4 doc rot; added a Commands section for both repos (client run/debug + Rust server `cargo run/test/build`, SQLite `world.db`); added a two-repo architecture section (authoritative server, pre-alpha auth-only, multiplayer-only, pointer to `server/docs/server_design.md`) with the known client↔server drift list; added a Debugging section on the in-game debug console (`scripts/debug_console.gd`, F2/backtick/`/console`, in-memory `DebugLog` tail); regrouped the autoload reference (old table listed ~30, project registers 49 — the whole client networking layer was missing); trimmed the To-Do to open items only; loosened the comment rule to welcome file-header/orientation comments while keeping anti-rot guards; tightened touch hygiene with an adjacency guardrail. Created `docs/concepts/architecture/systems_overview.md` as the new home for the ~60 completed `[x]` items' "what exists" knowledge, organized by domain. **Then (cont.): ALLY-target buff routing** — made 10 curated buffs (Bless/Valor/Brilliance/Clarity/Strength/Gift of Insight/Spirit of the Bear/Spirit of Wolf/Haste/Thorns) ally-castable. They were all authored `SELF` (so not a routing bug — a feature; class docs tag each buff Self/Target/Group). Server `tick.rs` shares `apply_player_spell_buffs` across the SELF + ALLY arms and fans `BuffSnapshot` to the buffed peer; client `spells.gd` `_cast_target_id` routes the cast to the peer + skips the local caster mutation; `buff_manager.gd` `reconcile_with_server_snapshot` is now **additive** so the buffed ally reconstructs the buff locally (PlayerStats + bar). Pet buffs deferred. |
| 2026-05-21 | [session_2026_05_21_currency.md](session_2026_05_21_currency.md) | **Four-tier currency + encumbrance v1.** Single `coins` int → four independent stacks (copper/silver/gold/platinum at **100:1** per tier) end to end, in three gated stages. **Server**: new `Coins` struct in `protocol/world.rs` is the single home for tier math — `spend` makes change low-coins-first (a copper hoard survives unrelated purchases), `add_payout` deposits reduced payouts without re-reducing held stacks; `CoinsUpdate` carries the wallet (protocol bump **PD_W0012→PD_W0013**); migration `0004_currency.sql` carries legacy `coins`→`copper`; vendor Buy/Sell in tick.rs use the helpers; 6 new unit + 3 integration tests green. **Client**: new `Currency` autoload mirrors Rust `Coins`; `PlayerStats` holds the four stacks (legacy save `"coins"` loads into copper — no wipe); `net.gd`/gdext 4-arg `coins_update`; vendor window shows prices reduced ("2s 50c") but the wallet footer raw ("350c" stays "350c"); DLL rebuilt; `tools/currency_smoke.gd` 12 assertions PASS. **Encumbrance v1**: new `Encumbrance` autoload — weight = coins (0.02/coin **flat across tiers**) + inventory + worn gear (`ItemData.weight`, default 0) vs capacity `10 + STR`; over-cap slows movement linearly (×0.25 floor, after mount mult) and gates stamina regen (×0.5, ×0 at 2× cap); CombatLog "You are encumbered!" on crossings. Decisions: auto-make-change (EQ refusal rejected); encumbrance applies even mounted. Also fixed `addons/gdext_net/build.ps1` (BOM-less UTF-8 broke stock PowerShell). Playtest: `docs/playtest_notes/currency_encumbrance_checklist.md`. Follow-ups: weight readout UI, item-weight content pass, moneychanger NPCs/banks/Citizen exchange. |
| 2026-05-26 | [session_2026_05_26_track22G_chunk4_tab_docking.md](session_2026_05_26_track22G_chunk4_tab_docking.md) | **Track 22.G chunk 4 — Tab docking.** Every `ChatWindow` carries a `group_id`; solo windows have a group of 1 keyed by their own `window_id`, multi-tab groups share a position/size with the active member rendering panel+tab strip and non-active members `visible=false`. New `ChatWindowManager._groups: {group_id: {members, active}}` with `dock` / `undock` / `set_active_tab` / `_refresh_group` ops. Drag UX: `ChatWindow._stop_interaction` override emits `drag_ended(id, drop_pos)` after a window drag; manager hit-tests other windows' `get_drop_target_rect()` to find a dock target. Tab buttons distinguish click (activate) from drag (undock) by motion distance — drag-out emits `tab_dragged_out(member_id, drop_pos)` routed to `undock`. Hit-testing iterates `_windows` directly rather than `gui_get_hovered_control` because the dragged window draws on top of any potential target. `group_id` persists on the per-window layout dict; restore rebuilds `_groups` and picks an active that's actually visible. Carry-forward: persist explicit active tab per group, visual drop-zone highlight, intra-strip reorder, optional context-menu fallback for accessibility. |
| 2026-05-26 | [session_2026_05_26_track22G_chunk3.md](session_2026_05_26_track22G_chunk3.md) | **Track 22.G chunk 3 — Per-window display settings.** Each `ChatWindow` gains `bg_alpha` (0–100), `font_alpha` (10–100), `font_size` (9–21), `default_channel` ("", "say", "shout", "ooc", "group"). Right-click → **Display...** opens an `AcceptDialog` with two `HSlider`s, a `SpinBox`, and an `OptionButton`. `apply_display_settings` re-applies panel alpha and walks `_msg_vbox` labels to update font_size and re-derive color at the new alpha — each label tags itself with `msg_type` meta at `add_line` time so the base MsgType color is recoverable. `default_channel` prepends `/<channel> ` to non-slash input in `_on_chat_text_submitted`; the LineEdit placeholder shows `[Channel] Press Enter to type...` for visual confirmation. All four fields persist on the existing layout dict. Closes the three chunks of the original multi-window chat to-do entry; tab docking still pending as separate work. |
| 2026-05-26 | [session_2026_05_26_track22G_chunk2.md](session_2026_05_26_track22G_chunk2.md) | **Track 22.G chunk 2 — Per-window message filters.** Each `ChatWindow` gains a `filters: Dictionary` keyed by `FILTER_KEYS` (string-per-`MsgType`) → bool; `add_line` short-circuits before label creation when the incoming type is filtered out. Right-click → **Filters...** opens an `AcceptDialog` with 14 checkboxes grouped under Combat / Chat / System. Filter state persists on the existing layout dict in `GameSettings.chat_windows`. Pre-chunk-2 layouts (no `filters` key) load with all categories enabled — `_merge_filters_with_defaults` ensures a freshly-added `MsgType` defaults to visible. Index-of-`FILTER_KEYS` must match `MsgType` declaration order; load-bearing comment in `chat_window.gd`. Chunk 3 (alpha / font size / default channel) and tab docking still carry forward. |
| 2026-05-25 | [session_2026_05_25_track22G_chunk1.md](session_2026_05_25_track22G_chunk1.md) | **Track 22.G chunk 1 — Multi-window chat framework.** `CombatLog` refactored from `CanvasLayer` (data + UI) to plain `Node` broker emitting `line_added(text, type)`; public API (`add_line`, `chat_submitted`, `MsgType`, `show_chat_input`, `is_chat_input_focused`) preserved bit-for-bit so the ~30 caller files were unaffected. New `scripts/chat_window.gd` (`ChatWindow` extends `DraggablePanel` — title bar, scroll, per-window LineEdit) and new `autoloads/chat_window_manager.gd` autoload owns N windows, builds the right-click PopupMenu (New / Rename / Delete; Delete disabled when only one window remains), persists layout through `GameSettings.chat_windows`. `player.gd` + `character_creation.gd` retargeted from `CombatLog.visible` to `ChatWindowManager.visible`. Chunks 2 (per-window filters) and 3 (per-window display settings) carry forward; tab docking deferred. Godot not on PATH so no automated checks — manual verification checklist in the session note. |
| 2026-04-26 | [session_2026_04_26.md](session_2026_04_26.md) | Project bootstrap, player movement, HUD bars |
| 2026-04-27 | [session_2026_04_27.md](session_2026_04_27.md) | Combat autoload, player stats, character creation, enemy AI, terrain |
| 2026-04-28 | [session_2026_04_28.md](session_2026_04_28.md) | Inventory, equipment, skills, spells, regen, damage numbers, loot, networking, hotbar, paperdoll, crafting scaffold, loot tables, 22 starter items |
| 2026-04-29 | [session_2026_04_29.md](session_2026_04_29.md) | Zone system, buff manager, group manager, pet manager, settings persistence, social commands, weapon skills, CharacterData refactor, drag-drop inventory, class/race docs (all 18 classes, all 17 races), tradeskill docs, transformation docs, alignment docs |
| 2026-04-30 | [session_2026_04_30.md](session_2026_04_30.md) | World lore (deities, dominion war, architects, languages, calendar), factions, 8 NPC profiles, zone expansions, ASCII maps; day/night mobs, food & drink, enemy hit reactions, spell VFX, crits, stun/root/slow/silence, elemental resists, damage shield, alignment extraction, WarderAI split, HUD split (907→478 lines), buff types, new spell definitions, bard twist, race vision, Feign Death/Sneak/Track, hotbar cooldowns, CombatLog decoupling, spell book, floating combat text |
| 2026-05-01 | [session_2026_05_01.md](session_2026_05_01.md) | Character window combat skills + armor skills sections, QuestManager, quest journal (J key), pet idle facing fix, caster/healer enemy types, mobile_character.gd + passive_skill_tracker.gd base classes, bard twist songs, level-gated spells, group XP sharing, spell ranks, proc weapons, bindpoint death respawn, vendor NPC bug fixes, NPC dialogue system; crafting stations, mining system, fall damage; named/boss mobs (5 bosses, enrage, gold nameplate, guaranteed + rare loot); full quest loop (QuestDefinitions, dialogue give/complete actions, quest_condition filters, item rewards); code review: fixed @onready null crash in EnemySpawner.apply_named(), fixed notify_collect() Crafting cross-coupling; loot bug fix (ResourceLoader.exists() false positive), inventory redesign (8 flat base slots, bags-as-items), item stacking fix, ToT frame positioning fix, paperdoll item name display |
| 2026-05-08 | [audit_2026_05_08.md](audit_2026_05_08.md) | Cross-repo health audit (read-only). 12 findings: SaveManager OS-close save is a no-op (same Godot-4 `_notification` pitfall as Track 1 hit), Net/SaveManager both own `auto_accept_quit`, lobby UI ungated in launcher mode, `dungeon_world.gd` ignores multiplayer, addon vs standalone procedural_dungeon drift, doc rot (Godot 4.3 → 4.4), stray build temp file, dead `terrain_zone.gd`, plugin.gd no-op shell, 5 trivial clippy warnings. No fixes applied. |
| 2026-05-24 | [session_2026_05_24_track22_EFHI.md](session_2026_05_24_track22_EFHI.md) | **Track 22.E / 22.F / 22.H / 22.I batch.** E: spell-damage cast-interrupt — extended Track 19A's `roll_cast_interrupt` to the PvP spell damage path (target's cast interrupted on hit) plus shield-reflect arms (PvP melee + spell). F: skill cap rebalance — `starting_value` is now `cap(L1)/4` with a floor of 1 (was equal to cap, so L1 chars couldn't advance any skill). Mirrored in server skills.rs + the three GDScript definition files. H: peer→peer target broadcast — wired the existing `ClientWorldMsg::SetTarget` (scaffolded but never handled); new `Outcome::PlayerTargetFanOut` + post-dispatch fan-out queue; client broadcasts on `Combat.set_target`; RemotePlayer gained `target_id` + `target_changed` signal mirroring RemoteEnemy; RemotePlayerManager subscribes to `Net.world_entity_target` filtered by id partition; ToT HUD resolver extended to remote_players group. I: player portrait HUD slot — 96×96 TextureRect right of the stat panel; slugifies race + class to load `assets/sprites/portraits/portrait_<race>_<class>.png`; hidden when file missing. Naming matches `docs/concepts/lore/portraits/prompts.md`. DLL rebuilt (3.96 MB). 116/116 lib + 40/40 integration tests pass. G (multi-window chat) deferred to its own track. |
| 2026-05-24 | [session_2026_05_24_track22C.md](session_2026_05_24_track22C.md) | **Track 22.C — Mount system (client-side v1).** Design call locked: item-based whistles (right-click summon), per-zone `NO_MOUNT_ZONES` blacklist, any incoming damage dismounts, mount speed multiplier overrides every other speed source. New `autoloads/mount_manager.gd` with `current_mount: ItemData`, `summon/dismount/is_mounted`, and `get_effective_speed_mult()` that player.gd now reads in place of `BuffManager.get_speed_mult()` directly. Three new `@export` fields on ItemData: `is_mount`, `mount_speed_mult`, `mount_name`. Right-click branches added to inventory_window + bag_window; toggling re-clicks dismount, different-whistle clicks swap. Dismount triggers: damage (combat.receive_player_damage), death (player_death._die safety net), manual `/dismount` chat command, zone-change auto-dismount via ZoneLoader.zone_changed subscription. `data/zone_data.gd` gained `NO_MOUNT_ZONES: Array[String]` mirroring `NON_BINDABLE_ZONES`. New `brown_steed_whistle.tres` (1.6× speed mult, 350gp vendor price); items.toml regenerated (169 entries). Server unchanged — MAX_MOVE_SPEED still clamps wire integration; mount fields are client-only metadata. 116/116 lib tests pass. |
| 2026-05-22 | [session_2026_05_22_track21B.md](session_2026_05_22_track21B.md) | **Track 21B — Target-of-target HUD frame.** The hud already had a `_tot_frame` widget (hidden) and a `_refresh_tot()` that hard-coded "you" as the target-of-target. Rewrote the resolver to actually compute the tracked target's current target. New `_resolve_tracked_target_target_id` reads from `RemoteEnemy.target_id` (server `EntityTarget` broadcast) or derives "you" from local Enemy state (CHASE/ATTACK). New `_resolve_remote_entity(id)` walks the id partition (`< 1B` → RemotePlayer, `1B-2B` → RemoteEnemy, `≥ 3B` → RemotePet) via new public `get_by_id()` accessors on the three remote-entity managers. New `_bind_tot_entity` / `_clear_tot_entity_binding` subscribe + cleanup hp_changed + died signals so the bar tracks live. `_on_target_changed` enemies branch now subscribes to `enemy.target_changed` when present so ToT follows the tracked enemy across target switches (mob switching from tank to healer). `_on_hp_changed` (PlayerStats) gated on `_tot_entity == null` so a peer-on-peer ToT isn't clobbered by local HP changes. No automated tests — Godot not in PATH on the workstation; manual verification deferred to next playtest. No server / protocol changes. |
| 2026-05-22 | [session_2026_05_22_track20D.md](session_2026_05_22_track20D.md) | **Track 20D — Cross-cutting cleanups.** Three spells were missing from `crates/projectdawn-server/data/spells.toml` — Lifetap, Soul Drain (both base spells; the Rk. II variants were already there from Track 9), and Bind Affinity — so client casts logged `unknown spell name` server-side while still rendering phantom damage. All three added with fields mirroring the GDScript definitions; Bind Affinity uses target_type `"BIND"` which falls through to the "not yet processed" default arm (mana deduction only — the bind point is still client-side `PlayerStats` state). 10 new `.tres` files in `data/loot/items/` for every named-mob drop (Rotfang's Fang, Predator's Collar, Gnoll Chief's Seal, Bonecrusher's War Axe, Pristine Venom Sac, Chitinous Ring, Sable Wing Membrane, Shadow Signet, Undying Marrow, Cursed Femur). `named_mob_definitions.gd` rewritten — `guaranteed_loot: Array[String]` of resource paths + `rare_loot: Array[{path, drop_chance}]`; new `load_item(path)` helper. `enemy.gd::apply_named` consumes the new shape. `items.toml` regenerated via `tools/export_items_oneshot.py` (158 → 168 entries). With paths in the server registry, equip / sell / destroy on named-boss loot now resolves end-to-end (was silently rejected as `unknown item path`). 116/116 lib + 40/40 integration tests pass in isolation (pre-existing pet AI flake under parallel load). |
| 2026-05-22 | [session_2026_05_22_track19A.md](session_2026_05_22_track19A.md) | **Track 19A — Server-authoritative cast interrupt.** Closes the Track 18 carry-forward note for channeling-on-interrupt. New `skills::channeling_interrupt_chance(score, cap)` mirrors GDScript `CastingSkills.get_interrupt_chance` (cap=0 → 1.0; trained → `max(0.10, 0.70 - ratio*0.60)`). New `tick::roll_cast_interrupt(conn)` helper returns `InterruptOutcome::{NotCasting, Interrupted{spell_name}, Survived{advanced_to}}` — wired into both the enemy-on-player damage path (alongside Track 18.1e's armor advance) and the PvP attack arm. On interrupt: clears cast cache + fans `CastFail("interrupted (hit during cast)")`. On survive: advances channeling. `autoloads/spells.gd::try_interrupt_cast` now early-returns in launcher mode (server owns the roll); existing `RemotePlayerManager._on_cast_fail` self-caster handler already routes own CastFails through `Spells.cancel_cast`. New integration test `cast_interrupted_by_incoming_pvp_hit` uses two Warriors over PvP (channeling cap=0 → deterministic 100% interrupt). 116/116 lib + 40/40 integration tests pass (+1 unit, +1 integration). |
| 2026-05-22 | [session_2026_05_22_track18.md](session_2026_05_22_track18.md) | **Track 18 — Server-authoritative skill leveling + memorize-gated hotkeys.** Protocol bumped `PD_W0009 → PD_W0010` for two new server messages: `SkillProgressUpdate { kind, key, new_score }` and `SkillProgressSnapshot { weapon, armor, casting }`. New migration `0003_character_skills.sql` (composite PK on `char_id, kind, key`); new `db::SkillRow` + `load_skills`/`save_skills`. New `world::skills` module ports the three GDScript cap tables (WeaponSkillDefinitions / ArmorSkillDefinitions / CastingSkillDefinitions) as embedded const slices, plus the `try_advance(0.2 * (1 - cur/cap))` math and `discipline_for_spell` lookup (HashMap built once at startup; strips Rk. II/III suffixes). `PerConnection` gained three `HashMap<String, i32>` skill maps + `skills_dirty`; EnterWorld seeds with `seed_starting_scores` then overlays DB rows. Server wires advances at three sites: weapon skill (+ defense) on landed Attack, casting skill on successful CastSpell, armor skill (+ dodge) on enemy-on-player hit. Checkpoint cadence flushes `skills_dirty`. gdext-net gained two new `#[signal]`s + `Incoming` variants + classify/fire arms; DLL rebuilt (3.95 MB) and deployed. Client `passive_skill_tracker.gd::try_advance` early-returns in launcher mode; new `apply_remote_score` + `apply_remote_snapshot` setters; `weapon_skills.gd`/`armor_skills.gd`/`casting_skills.gd` subscribe to `Net.world_skill_progress_update/snapshot` and filter by `kind`. 18.2: `social_hotkeys._cast_spell` now refuses to cast if the spell isn't memorized in SpellBar — closes the Track 17 carry-forward gap. 115/115 lib + 39/39 integration tests pass (+4 unit, +2 integration). |
| 2026-05-22 | [session_2026_05_22_track17.md](session_2026_05_22_track17.md) | **Track 17 — Memorize cost/cap + cast cooldown & movement server-auth.** 17.1 — `SpellBar.max_slots_for_level` (MIN_SLOTS=3, +1 per 5 levels, cap 10 at L35) with `is_slot_unlocked` rejection on `set_slot`; new `cap_changed` signal. `Memorize.commit(slot)` runs a 2 s cast bar, debits half the spell's mana, cancels on stand / >0.5 m movement / candidate change; emits `memorize_started/progress/cancelled/completed`. Hotbar repaints candidate icon onto the target slot during commit + lavender progress overlay + "Lv N" lock badges on capped slots; hotkey-bar candidate route removed (memorize resolves only through the spell bar). 17.2 — server `PerConnection.spell_cooldowns: HashMap` + `cast_start_pos: Vec3f`; CastStartBroadcast stamps the position; `Outcome::CastSpellIntent` extended; tick gate runs movement check (>5 m fails `interrupted (moved)`) + cooldown check (`Spell is on cooldown.`) before the existing cast-time gate's downstream apply. Successful casts insert `cooldown[name] = now + cooldown_secs`. 2 new integration tests (`cast_spell_rejected_during_cooldown`, `cast_spell_rejected_when_caster_moved_during_cast`) pass; 111/111 lib tests pass. |
| 2026-05-22 | [session_2026_05_22_track16.md](session_2026_05_22_track16.md) | **Track 16 — Spell book memorize workflow + social/macro library.** 16.0 fixed five Track 15 regressions from the 2026-05-22 playtest: stackable-count visible during drag (inventory_window), launcher-mode `_return_drag_to_source` didn't repaint source cell, paperdoll left-click equip never landed (Node `_input` order vs Control `_gui_input`), pet target stomped every tick by inheritance pre-pass (server `tick.rs` — keep current target if it's still alive), and text-focus walk only recursing one viewport level. 16.1 — new Memorize + SpellBar autoloads; spell-book row click no longer casts (sit-gated select sets a Memorize candidate + highlights row); spell bar rebuilt from auto-populated to player-configurable, click-with-candidate assigns memorize, click-without-candidate casts, right-click clears, Alt+digit reroutes through SpellBar; hotkey bar also accepts candidate clicks. 16.2 — SocialHotkeys gained a per-character `library` of macros; slot type SOCIAL stores a `library_id` ref; legacy inline-lines slots auto-migrate on load. Context menu has Assign Social (pick mode) + Manage Socials (browse mode) opening the same two-pane window. |
| 2026-05-20 | [session_2026_05_20_track15.md](session_2026_05_20_track15.md) | **Track 15 — right-click lift + UseConsumable + PetCommand GUARD/SIT.** Closes the carry-forward from Track 14.4. **Client (Equipment.gd)**: extracted pure `_pick_slot`; new `request_equip_from(src_location, src_slot, item)` handles base + bag sources, sends `Net.broadcast_equip_item` in launcher mode with a 2H-clears-offhand pre-step that parks the displaced offhand via `Net.broadcast_unequip_item` (rejects with combat log on inventory-full); `request_unequip(slot)` picks first-free base slot. Right-click in `inventory_window` / `bag_window` / `paperdoll_window` routes through the new entry points. **Server inventory.rs**: generalised `equip_from_base` → `equip_from_location` (accepts `bag_<i>` sources, returns `Vec<(String, u32)>` so the apply phase fans deltas at the right location); old `equip_from_base` removed and tests migrated. New `destroy_at(loc, slot, count)` and `decrement_at(loc, slot)`. **Protocol**: new `DestroyItem { location, slot, count }` variant. **Server tick.rs**: equip apply switched to `equip_from_location`; new apply phases for `DestroyItem` (decrement + InventoryDelta, no loot bag) and `UseConsumable` (peek + validate is_food/is_drink/heal_on_use, decrement, fan InventoryDelta + HealthUpdate/ManaUpdate + BuffSnapshot with `"Food: "`/`"Drink: "` name prefixes matching the client's BuffManager naming). **PetCommand GUARD/SIT**: new `PetStance` enum on `Entity` (`Follow`/`Guard`/`Sit`); `tick_pet_ai` branches — `Sit` drops target + stands still, `Guard` engages target if any else holds position. Pet inheritance pre-pass filters by `stance == Follow`. Guard pets retaliate on incoming damage (set target = attacker); Sit ignores. PetCommand dispatch sets stance + clears target + stamps `command_at`; ATTACK/BACK/FOLLOW also force-reset to Follow. **gdext-net**: new `send_destroy_item` + `send_use_consumable` `#[func]` exports; DLL rebuilt with static-CRT release (4.0 MB) and copied. **pet_manager.gd**: `command_guard` / `command_passive` route through wire; `/pet sit` chat alias added in hud.gd. 154/154 server tests pass. After Track 15 the inventory + equipment + vendor authority lift is complete; cooldown / movement-during-cast / skill leveling carry forward. |
| 2026-05-20 | [session_2026_05_20_client_authority_lift.md](session_2026_05_20_client_authority_lift.md) | **Client drag-and-drop + vendor authority lift.** Closes the carry-forward from Tracks 13.2 / 13.3 / 14.3 / 14.4: client UI now routes mutations through `Net.broadcast_*` in launcher mode. **gdext-net**: new `coins_update` signal + classify arm decodes `ServerWorldMsg::CoinsUpdate` (variant has existed since Track 6, finally consumed); new `send_buy_item` / `send_sell_item` `#[func]` exports — the latter parses the GDScript-friendly `"base"` / `"bag_<i>"` string into the protocol's typed `SlotRef`. **DLL rebuilt** with static-CRT release profile (4.0 MB). **Net.gd**: `world_coins_update` signal + `_on_coins_update` routes through `PlayerStats.apply_remote_coins` (new setter that overwrites + emits `coins_changed`); new `broadcast_buy_item` / `broadcast_sell_item` wrappers. **inventory_window.gd / bag_window.gd**: launcher-mode pickup skips the local `clear_*_slot` (source visually stays filled until server confirms); drop routes `Net.broadcast_move_item(src_loc, src_slot, dst_loc, dst_slot)` and ends the drag without local mutation. The swap-then-continue-carrying UX is dropped — drop-on-occupied fires a server swap and ends the drag. **vendor_window.gd**: `_populate_list` walks base + bag slots directly to tag each sell entry with `(location, slot_idx)`; `_do_buy` / `_do_sell` route through `Net.broadcast_*` in launcher mode; solo paths preserved under the gate. **Carry-forward**: right-click flows (equip / unequip / consumable / destroy) still local — natural next step. |
| 2026-05-20 | [session_2026_05_20_track14_followups.md](session_2026_05_20_track14_followups.md) | **Track 14 follow-ups — Lifesteal + Vendor server-side.** Two cross-cutting handoff items shipped server-side. (1) **Lifesteal**: `apply_spell_damage_to_enemy` now applies `caster.hp += min(spell.heal_amount, damage_done)` for spells like Lifetap Rk. II / Soul Drain (signature widened to `&mut connections`; both call sites updated). (2) **Vendor BuyItem / SellItem**: new `items::lookup_by_name`; new `Outcome::BuyItemIntent` / `SellItemIntent` + handler arms; new tick.rs phases 4hi/4hj mutate `conn.coins` + inventory, fan `CoinsUpdate` (the scaffolded variant since Track 6, finally emitted) + `InventoryDelta`. Sell price = `vendor_price / 2` per unit, matches GDScript. Server NPCs don't exist yet so stock-by-vendor validation is deferred; server enforces price + coin balance + inventory cap (vendor_id is informational). Client `vendor_window.gd` still mutates locally — client lift is the next step alongside the carried-forward inventory/bag drag-and-drop lift. Lib unchanged at 111; integration 32→35 (+1 lifesteal_spell_heals_caster, +1 buy_item_charges_coins_and_grants_stack, +1 buy_item_rejects_insufficient_coins, +1 sell_item_credits_coins_and_removes_stack). Track 14's original sub-tasks + the two follow-ups are all done. |
| 2026-05-19 | [session_2026_05_19_track14_3.md](session_2026_05_19_track14_3.md) | **Track 14.3 — Bag locations.** Lifts the last `from_rows` exclusion: `bag_<i>` rows now load, persist, snapshot, and move-item-between. New `PlayerInventory.bags: HashMap<u8, Vec<Option<InventoryEntry>>>` per-bag contents map; `ensure_bag_init` allocates / drops the Vec at every mutation site (from_rows, add_item_locating, move_*, split_base, drop_base, equip/unequip). New `move_across` dispatch resolves any (location, slot) pair and routes to `move_base` / `move_bag` / `move_bag_to_base` / `move_base_to_bag` / `move_bag_to_bag`. Mutation rules: bag must be empty to move OUT of base or be dropped; no bag-in-bag (rejected at both src and swap-dst); unequip swap-back rejects if dst base isn't equippable in the slot (Track 14.3 hardening — closes the Track 13.3 latent bug where a bag could be swapped INTO the paperdoll). New `small_pouch.tres` fixture (BAG, 4 inner slots) seeds the test set; items.toml regenerated (158 entries). Client `autoloads/inventory.gd` extends `_on_inventory_snapshot` (two-pass: base first to init bag_contents, then bag_<i>) and `_on_inventory_delta` (bag_<i> branch). 111 lib + 31 integration tests pass (+13 unit, +1 integration `bag_contents_persist_across_reconnect`). Closes Track 14's original sub-task list (14.1 + 14.2 + 14.3). |
| 2026-05-19 | [session_2026_05_19_track14_2.md](session_2026_05_19_track14_2.md) | **Track 14.2 — Stat recompute on equip.** New `EquipStatBonuses` cache on `PerConnection` tracks the running gear-bonus total so `inventory::recompute_equipped_stats` can apply signed deltas to `conn.{strength, ..., max_hp, max_mp, max_stamina, equipped_armor}` without re-deriving the race+class base. Mirrors GDScript: equip bumps max only (current unchanged), unequip drops max and clamps current down if needed. Wired at three sites — equip dispatch, unequip dispatch, and post-`load_inventory` — fans `HealthUpdate` / `ManaUpdate` / `StaminaUpdate` on max change so the equipping player and AOI peers see the new bar immediately. `EquipUpdate` handler demoted to logged no-op (server now derives armor + stats from the registry; the message stays for one track so old clients don't error). Orthogonal to buff stat deltas — Bless + equip cycles compose cleanly. 98 lib tests + 30 integration tests pass. |
| 2026-05-19 | [session_2026_05_19_track14_1.md](session_2026_05_19_track14_1.md) | **Track 14.1 — Server-side item registry + equip validation.** Generalised `items::Weapon` to `items::Item` (13-variant `ItemType` enum); regenerated `items.toml` from all 157 `.tres` files via new `tools/export_items.gd` (canonical, EditorScript) + `tools/export_items_oneshot.py` (bootstrap). `equip_from_base` now rejects mismatched slot via `is_equippable_in_slot`; `add_item_locating` returns `(Vec<slots>, leftover)` and caps stacks at per-item `max_stack`, with overflow spilling into new slots; loot dispatch refunds leftover stacks to the bag instead of dropping. 99 lib tests pass (+8). Closes three Track 13 deferrals; 14.2 (stat recompute) and 14.3 (bag locations) carry forward on the new registry. |
| 2026-05-19 | [session_2026_05_19_track13.md](session_2026_05_19_track13.md) | **Track 13 — Server-side inventory (1, 2, 2.b, 3).** Four sub-tasks shipped: 13.1 schema + load/save + PlayerInventory + loot mutation foundation; 13.2 InventorySnapshot/InventoryDelta + MoveItem (protocol bump PD_W0008→PD_W0009, client lifts to render-from-server-state); 13.2.b SplitStack + DropItem (drop spawns server-owned LootBag at player's pos); 13.3 EquipItem/UnequipItem + paperdoll persistence via 'equip' rows in character_items. Every gameplay-relevant piece of player state now server-authoritative. 117 tests pass (was 84; +24 unit + integration). Documented deferrals: bag_<i> locations, item-vs-slot validation, stat recompute on equip, lifesteal heal_amount — all gated on the server-side item registry. Track 14 handoff lays that in. |
| 2026-05-19 | [session_2026_05_19_track12.md](session_2026_05_19_track12.md) | **Track 12 — Pet polish (commands, threat, warder, charm).** Three pieces across six commits. **A1**: PetCommand wire variant (PD_W0007 → PD_W0008) with FOLLOW/GUARD/ATTACK/BACK/SIT codes; Entity.command_at sticky timer; PetManager.command_attack/back route through Net in launcher mode; /pet attack /pet back chat commands. **A2**: separate Entity.threat map (per actual attacker, kept distinct from aggro/credit map); maybe_switch_target_by_threat re-targets enemy onto pet at 1.3× threat threshold; threat clears on leash-home. **B**: Wolf warder template; Beast Master auto-summon on EnterWorld; warder death sets warder_respawn_at on owner, pre-AI sweep respawns at 30% HP after 15 s; new `summon_pet_for_owner` helper consolidates PET_SUMMON + auto-summon + respawn. **C**: PET_CHARM via id-partition re-key (EntityDespawn old enemy + PetSpawn fresh pet, copied hp/max_hp/yaw, owner=caster, charm_expires_at set); charm decay sweep silently despawns ("mob runs away"); spawner.on_enemy_died arms the camp's respawn timer so charmed mobs don't block spawns. 84 tests pass (+4 integration, +1 unit). GUARD/SIT, multi-pet, lifesteal heal_amount, in-combat retreat / fury still deferred. |
| 2026-05-19 | [session_2026_05_19_track11.md](session_2026_05_19_track11.md) | **Track 11 — Server-side pet system (1.1–1.5).** Five sub-tasks, six commits across server/client/launcher. Necromancer's Summon Skeleton is end-to-end authoritative: server spawns the pet entity (Entity extended with owner: Option<EntityId>, PET_ID_BASE 3B partition), pet follows owner via tick_pet_ai (PET_FOLLOW_DISTANCE 3 m), inherits owner's last_attacked_enemy with 10 s decay and chases + swings; enemies aggro on pets (targets pool extended); pets take damage and die through the existing corpse cleanup; HUD pet panel reads from RemotePet via PetManager.register_remote_pet. Protocol PD_W0006 → PD_W0007 (new PetSpawn variant). Charm + warder retreat/fury + multi-pet + pet commands + re-aggro on damage deferred to Track 12. 78 server tests pass (+6: pet_templates ×2 unit, pet_summon_visible_to_peer, pet_follows_owner, pet_attacks_owners_target, enemy_aggros_on_pet_when_no_player_nearby). AI-walks-into-melee timeouts bumped 20 s → 35 s across 5 tests. |
| 2026-05-19 | [session_2026_05_19_track10.md](session_2026_05_19_track10.md) | **Track 10 — Server-side cast-time gate.** Closes the last anti-cheat hole in the spell pipeline: forged `CastStartBroadcast` + immediate `CastSpell` no longer instant-casts long-cast spells. Gate snapshots cast cache state at `handle_message` time (CastComplete in the same batch would otherwise blank it before the post-dispatch intent loop fires), then requires matching spell name + elapsed wall-time ≥ cast_time − 100 ms. Rejection fans `CastFail("cast not ready")`. Cache also clears on successful cast. Two pre-existing tests fixed to send proper CastStart → wait → CastSpell flow (caught a test-only gotcha: `tokio::time::sleep` doesn't advance the renet transport). 72 tests pass (+2 gate tests). |
| 2026-05-18 | [session_2026_05_18_track9.md](session_2026_05_18_track9.md) | **spells.toml Rank II/III fill-in + Track 9 — Server-side AOE.** Added 36 missing Rank II/III spell entries to the server table (hand-write, not via stale `export_spells.gd`). Track 9: new `"AOE"` arm in CastSpell resolution uses AOI 3×3 neighbourhood + radius filter; shared `apply_spell_damage_to_enemy` helper now powers both ENEMY and AOE arms (death / kill-credit / loot / CC). Helper extraction also closes a Track 7 carry-over: spell-kill loot bags now register into AOI grid + AOI-filter LootBagSpawn fan-out. 4 AOE spells (Inferno / Nature's Wrath / Ice Storm / Arcane Nova) added to `spells.toml`. 70 tests pass (+1 `aoe_spell_damages_nearby_enemies`). |
| 2026-05-18 | [session_2026_05_18_track8.md](session_2026_05_18_track8.md) | **Track 8 — Name recycling fix, ALLY peer healing, enemy CC/mez.** Deleted-character name freed via `_del_{id}` suffix on soft-delete. 13 heal spells changed SELF→ALLY; server ALLY arm heals targeted peer + fans HealthUpdate/BuffSnapshot; client guards against double-apply in launcher mode. Enemy CC: `CcKind`/`ActiveCc` on `Entity`; Mez/Root/Snare/AttackSlow apply server-side from spell fields; damage breaks mez. 69 tests pass. |
| 2026-05-18 | [session_2026_05_18.md](session_2026_05_18.md) | **Track 7 — AOI grid partitioning.** Pre-audit fixes (HealSelf/DamageSelf gated behind `PD_DEV_CMDS=1`, Y-axis drift). New `world/aoi.rs` (120 m cells, 3×3 neighbourhood, 21 unit tests). All broadcasts — spawn, despawn, position, resources, loot bags — filtered to visible neighbourhood. 2 new integration tests: far-apart clients don't see each other; approaching client triggers EntitySpawn on cell crossing. 69 tests pass. |
| 2026-05-14 | [session_2026_05_14.md](session_2026_05_14.md) | **Track 6 — Server-authoritative stats / PvP / buffs / groups.** Five sub-tasks plus three buff-state subdivisions (4a/4b/4c/4d) landed across two days; ~22 functional commits across server (12), launcher (1), Project_Dawn (9). `WORLD_PROTOCOL_ID` bumped `PD_W0004 → PD_W0005`. **Sub-task 1** lifted HP/MP/Stamina + stats to server, ported regen math to `world/regen.rs`, added enemy-on-player HP application; removed `ClientWorldMsg::ResourceUpdate`, added `Respawn`. **Sub-task 2** ported `CharacterData` (race/class/level → stats) + `Combat.calc_damage` to Rust; added `items.toml` weapon table (7 weapons). `Attack` carries `weapon_path` + `is_offhand` now; client-supplied amount ignored. Bow fix (RANGED_ATTACK_RANGE = 25.0). Dev tool `/give <name>` + `/items <substring>` backed by `ItemRegistry` autoload (157 items indexed; handles `.tres` / `.res` / `.remap` in PCK). **Sub-task 3a** wired PvP HP application + `combat::can_attack` chokepoint (returns `attacker.pvp_override_on && target.pvp_override_on`); armor reduction (AC/(AC+100)) applied in PvP melee + enemy-on-player paths; new dev intents `EquipUpdate`/`PvpToggle`/`DamageSelf`/`HealSelf`; closes fall-damage peer-view desync (route through DamageSelf). **Sub-task 3b** added server-side `CastSpell` handler + `spells.toml` (~40 spells, heals + damage across every caster class) + `tools/export_spells.gd` for canonical-from-GDScript regen; `CastSpell` shape `spell_id` → `spell_name`. **Sub-task 4a** introduced `world/buffs.rs` with `ActiveBuff` + `BuffEffect` enum; HoT (Healing Wave, Regrowth), MP regen (Clarity, Breeze), Lich Form (toggle, infinite duration) ticking server-side; server originates `BuffSnapshot` from `active_buffs` (client BuffSnapshotBroadcast deprecated). Lich tuned 15→8 MP/s after playtest. **Sub-task 4b** added `StatBuff` variant — delta-based, apply on cast / undo on expire; refresh semantics (Bless re-cast doesn't double-stack); 6 spells (Bless, Valor, Spirit of the Bear, Gift of Insight, Strength, Brilliance). **Sub-task 4c** added Speed (movement integration), Haste (tracked only), DamageShield (reflects to attacker in all damage paths), Absorb (pool consumed before HP), AccuracyCrit (folds into `calc_swing` crit chance); 7 spells (Spirit of Wolf, Haste, Thorns, Spellshield, Rune, Primal Bond, Hunter's Eye). **Sub-task 4d** added Mez/Root/Snare/AttackSlow/Silence + dispel action; server gates Move (mez/root) / Attack (mez) / CastSpell (mez/silence) intents from CC'd players; snare folds into movement integration `(1 - snare) * speed`, floored at 10%; 9 spells (Mesmerize, Ensnare, Ensnaring Roots, Immobilize, Snare, Slow, Spellbreak, Antimagic Ward, Expose). **Sub-task 5** added `world/groups.rs` (in-memory `GroupManager` with HashMap rosters + pending invites + server-mediated handshake); kill-credit XP splits among killer's online group with +20% bonus; new ServerWorldMsg variants `GroupInvited` + `GroupRoster`; disconnect cleanup leaves the leaver and dissolves single-member groups; client GroupManager autoload routes through Net in launcher mode (legacy enet paths stay for Test Room); chat commands `/invite`, `/accept`, `/leave`, `/kick`. Manual verification done by user through sub-task 4a; 4b/4c/4d/5 pending user's return. Deferred to follow-ups: enemy CC (need `Entity.active_buffs`), damage-breaks-mez, ALLY target_type for peer healing, server cast-time gating, Rank II/III spell variants (rerun export_spells.gd), AOE server-side, pet system, server-side inventory, AOI partitioning (Track 7). |
| 2026-05-13 | [session_2026_05_13.md](session_2026_05_13.md) | **Track 5 — Server-authoritative enemies.** Four sub-tasks across two repos. Server owns enemy spawn / AI / HP / death lifecycle and loot bag rolling + arbitration; clients render via broadcasts and route player attacks + loot pickups through intent messages. `WORLD_PROTOCOL_ID` bumped `PD_W0003 → PD_W0004` covering five new variants (`EnemySpawn`, `EntityTarget`, `LootBagSpawn`, `LootGranted`) and one shape change (`Attack { target_id, amount, crit, dmg_type }`). New server modules: `zones.rs` (TOML camp loader, 9 camps / 27 spawn points / 9 archetypes ported from `Project_Dawn/data/zone_data.gd`), `entity.rs` (Entity + EnemyState + tick_ai for Idle/Chase/Attack/Leash transitions), `spawn_points.rs` (Spawner with backdated first-fire + on_enemy_died respawn arm), `loot.rs` (11 mob loot tables ported from `MobLootTables.TABLES`, roll_for_mob with partial-name match). Id partitions: enemies start at `ENEMY_ID_BASE = 1_000_000_000`, bags at `LOOT_BAG_ID_BASE = 2_000_000_000`. Constants added: `CORPSE_LINGER_SECS = 5.0`, `ATTACK_RANGE_TOLERANCE = 1.5`, `LOOT_BAG_LINGER_SECS = 120.0`, `LOOT_PICKUP_RANGE = 6.0`. Tick loop gained six new phases: 4g spawner, 4h apply attack intents (Hit/HealthUpdate/EntityDied fan-out + loot roll on death), 4i AI tick, 4j corpse cleanup + spawner.on_enemy_died, 4ka apply loot intents (LootGranted private + LootBagSpawn re-snapshot or EntityDespawn), 4k bag expiry; step 6b for enemy position fan-out alongside players. **Sub-task 2 fix mid-track**: RemoteEnemy was a plain Node3D so Targeting._click_target's PhysicsRayQueryParameters3D fell through it; refactored to CharacterBody3D with CollisionShape3D mirroring remote_player.tscn. **Track 4 regression fix**: step 4a seed was asymmetric — new joiner got each existing peer's full state (EntitySpawn + resources + cast + buffs) but existing peers only got the new joiner's EntitySpawn, so if A entered the world before B's initial ResourceUpdate landed, A saw B's HP/MP/Stamina as 0/0/0 until B triggered a state change. Closed the symmetric half. **Client autoloads**: `RemoteEnemyManager` and `RemoteLootBagManager` follow the same `_spawn_data` (autoload-scoped) / `_by_id` (scene-scoped) + scene-swap-rehydrate pattern as `RemotePlayerManager`. Routing by id partition lets the three managers coexist on shared signals (Position / HealthUpdate / EntityDespawn) without arguing. **LootBag refactor**: `bag_id: int = -1` field discriminates local single-player bags (legacy 120s despawn timer, in-place `Inventory.add_item`) from server-owned bags (timer skipped, `LootWindow.Take` routes through `Net.broadcast_loot_item`, server's `LootGranted` handler does the Inventory.add_item). LootWindow connects to `bag.items_changed` so server re-snapshots refresh the open window. **Combat refactor**: `combat.gd` gains `_apply_damage_to_node` chokepoint picking the right authority per target type (RemotePlayer → broadcast_hit visual-only, RemoteEnemy → broadcast_attack server-resolves, local Enemy → take_damage); all four damage call sites (melee, single-target spell, AoE spell, weapon proc) converge on it. `_spell_to_net_damage_type` maps SpellData's authoring enum onto NetProtocol.DamageType. `zone_manager.gd` early-returns in launcher mode so the local EnemySpawner instantiation that built the 27 starter-zone camps is skipped — Test Room single-player flow keeps working unchanged. **Trust model carried forward**: Track 4's client-authoritative-own-HP / client-authoritative-own-inventory still holds; only enemy state moves fully server-side. Damage formula computed client-side and trusted (Track 6 lifts to fully server-authoritative combat math). Inventory-full on loot pickup currently loses the item — server is authoritative on bag contents, doesn't know client inventory state. **Tests**: `cargo test --release` 27/27 stable (8 lib unit + 3 auth + 1 world_smoke + 9 world_two_clients + 2 protocol), 4 new lib unit tests across loot/zones/spawn_points, 2 new integration tests (`enemies_visible_after_enter_world`, `player_attack_kills_enemy_and_corpse_despawns`). AI-wait timeouts bumped 10s→20s on the two AI-driven tests after parallel-cargo CPU contention made them flaky under full-suite runs (isolated runs still 5-6s). cargo clippy clean. 17 functional commits + 1 test-stability commit across 3 repos. **Sub-task 5 — kill credit + XP** (later same day): server picks top damager from each enemy's `aggro` table in step 4h's on-death branch, sends a private `XpGained` to that client; client calls `PlayerStats.gain_xp(amount)` so the existing level-up cascade fires unchanged. Reuses the pre-existing `XpGained` variant (Track 4 scaffolding) — no protocol bump, still PD_W0004. Solo only — legacy enet `GroupManager` path stays for in-launcher group XP since renet only sees server-connected clients. `current/to_next` ride as placeholder zeros (Track 6 lifts player XP state server-side). Integration test extended to assert XpGained{amount=10} arrives between EntityDied and EntityDespawn. User-verified two-`.exe` with `server.log` showing `kill credit granted killer=12 mob="Bandit Scout" xp=50` on each kill; non-killer client received no XP. 3 commits. Track 5 complete (5/5 sub-tasks). |
| 2026-05-12 | [session_2026_05_12.md](session_2026_05_12.md) | **Track 4 — Player state replication.** Five sub-tasks plus two follow-ups (EnterWorld gate + test-panel access in launcher mode); spans 2026-05-11 → 2026-05-12. `WORLD_PROTOCOL_ID` bump `PD_W0002 → PD_W0003` covers the whole batch. **Sub-task 1**: `ClientWorldMsg::ResourceUpdate { hp, max_hp, mp, max_mp, stamina, max_stamina }` appended; throttled broadcaster autoload (`NetCombatBroadcaster`) hooks PlayerStats signals — broadcast on any single resource moving >5% of max OR after 250 ms of pent-up small changes. Server expands one ResourceUpdate into three ServerWorldMsg fan-outs (`HealthUpdate`/`ManaUpdate`/`StaminaUpdate`) with per-sender dedup; `PerConnection` caches last values so new joiners get seeded in step 4a. `ServerWorldMsg::ConnectOk` extended with `name/race/class/level` so launcher-mode `lobby._on_app_connected` can call `PlayerStats.apply_character` before flipping the Enter World button — without this fix the character entered world.tscn classless with default 100/100/100 stats because EntitySpawn fan-out skips self by design and the launcher doesn't relay character data. **Mid-sub-task render pivot**: initial implementation put three overhead bars above the peer's capsule; user feedback redirected to the target-frame approach (matches the game's enemy-targeting pattern). Wire infrastructure kept; only the render moved. `remote_player.tscn` reverted to capsule + NameLabel; bar shader deleted; RemotePlayer joined `remote_players` group with `mob_name/is_dead/set_targeted` accessors so existing Targeting + Combat plumbing accepts it; Combat.set_target falls into the non-enemy else branch (auto-attack stays off — PvP is Track 6). `TargetFrame/VBox` gained `MPBar` + `StaminaBar` (hidden by default); HUD's `_on_target_changed` routes remote_players through `_setup_peer_target` and subscribes to per-resource signals. Level label cleared on peer targets per user feedback. **Follow-up E** (EnterWorld gate): new `ClientWorldMsg::EnterWorld` variant sent by lobby's `_on_enter_world_pressed`; server tracks `conn.in_world` separately from `conn.ready`; spawn / position / resource / buff / cast / death / despawn fan-outs all gate on `in_world` (peers don't see ghost bodies for clients still at the Enter World screen). `JustConnected` outcome renamed to `JustEnteredWorld` and re-pointed at the EnterWorld arrival. Tests in `world_two_clients.rs` + `world_smoke.rs` send EnterWorld immediately after ConnectOk and pump the transport four ticks so the bytes leave the buffer before `start()` returns. **Follow-up F** (test panel access): "Take 20 dmg" button in COMBAT FLAGS routes through `Combat.receive_player_damage`; test panel mount gate in `world.gd` / `dungeon_world.gd` extended to also fire when `Net.is_launcher_mode()` (was previously only when `Network.is_test_room`). Also bundled: defensive cleanup in `remote_player_manager.gd` after a transient duplicate-RemotePlayer artifact (race in scene-change detection: `_by_id.clear()` dropped references without freeing the nodes; a transient `current_scene` null during `change_scene_to_file` could trip `scene != _last_scene` without actually swapping). Two belt-and-suspenders fixes: queue_free every known node before clear, and scan-and-free orphans in `_on_entity_spawn` before instantiating. **Sub-task 2** (cast bar, option 2b per user — target frame, not overhead): three new `ClientWorldMsg` broadcasts (`CastStartBroadcast`/`Complete`/`Fail`); existing `ServerWorldMsg::CastStart`/`Complete` switched from `spell_id: u32` to `spell_name: String` per Q1; `CastFail` gained `caster: EntityId`. `NetCombatBroadcaster` hooks Spells.casting_started → broadcast Start, spell_cast → Complete *only if cast_time > 0* (spell_cast fires for instant casts too), casting_cancelled → Fail("interrupted"). `PerConnection.cast_spell_name/cast_total_duration/cast_set_at` caches in-flight cast; step 4a seeds new joiners with remaining duration (`total - elapsed`, clamped > 0). RemotePlayer `cast_started/cast_ended` signals, `cast_progress_ratio()` helper reading `Time.get_unix_time_from_system()`. HUD's CastBar (gold/XP color) + CastLabel ticks via `cast_progress_ratio()` in `_process` — server sends Start + duration once; client interpolates progress locally. Defensive duration-elapsed timeout (0.5 s grace) auto-hides if the casting peer crashed before sending Complete/Fail. **Sub-task 3** (buff snapshot, per Q2 — whole-list model not deltas): `ClientWorldMsg::BuffSnapshotBroadcast { buffs: Vec<(String, f32)> }` and `ServerWorldMsg::BuffSnapshot { target, buffs }`. `BuffManager.get_snapshot_arrays()` walks every buff source the local buff bar surfaces (HoTs, Absorb, Evade, Food, Drink, Speed, Haste, MpRegen, Stat, PrimaryStat, DamageShield, Stealth, Lich) and emits parallel `PackedStringArray names` + `PackedFloat32Array durations`. 0.0 duration means "indefinite/toggle" (Lich, raw Absorb); receiver displays just the name. `NetCombatBroadcaster` hooks `BuffManager.buffs_changed` and pushes through. `PerConnection.buff_snapshot` cached for new-joiner seed. HUD's TargetFrame gained `BuffsLabel` (wrap-enabled multi-line); renders one line per buff: `"<name>  <duration>s"` or just `<name>` for 0-duration entries. **Sub-task 4** (hit/miss/evade floating text): three new `ClientWorldMsg` broadcasts; existing `Hit`/`Miss`/`Evade` `ServerWorldMsg` variants reused. `Combat.deal_damage_to_target` detects RemotePlayer target → broadcasts Hit, skips `take_damage` (peer authority over own HP). `combat_fanouts` queue NOT deduped (one-shot ordered events). RemotePlayerManager `_on_hit/_miss/_evade`: target == own_id → `DamageNumbers.spawn_incoming` near local player; else `spawn_damage/spawn_miss` over the targeted RemotePlayer. Evade reuses spawn_miss (DamageNumbers has no distinct evade visual yet). Test panel gained Hit Target (20) / Miss Target / Evade Target buttons in COMBAT FLAGS with `_resolve_peer_target` group-check helper that reports via CombatLog if it no-ops. **Diagnostic side-track**: first test showed no floating text on either side despite broadcast lines being received end-to-end. Added test-panel and RemotePlayerManager._on_hit instrumentation; confirmed chain fired. Plain Label3D bypass confirmed rendering worked. Root cause: `GameSettings.floating_text_enabled` was off in user's save (Options → Interface toggle from a prior session). Once re-enabled, system worked. Diagnostic stripped before commit. PvP HP application deferred to Track 6 — Track 4 broadcasts are visual-only. **Sub-task 5** (death/respawn, per Q3 option a — no separate Respawn variant): `ClientWorldMsg::DeathBroadcast` (no payload — dying client's char_id implicit from sender); existing `ServerWorldMsg::EntityDied { id }` reused. `PlayerDeath._die()` calls `Net.broadcast_death()` before scheduling `_respawn`. RemotePlayer `apply_death` tweens `rotation.x` to PI/2 over 0.5 s (falls onto its side), dims nameplate, sets `is_dead = true`. `apply_health_update` detects "was_dead and new_hp > 0" → `_apply_respawn`: clears snapshot buffer + `_last_seq` so the next Position broadcast snaps via cold-start (no slide-from-death-spot to bind-point), tweens rotation back to 0. `on_position_update` drops snapshots while `is_dead` so stray Position updates don't slide the corpse. `_physics_process` early-returns while dead. **Tests**: `cargo test --release` 12/12 (auth 3 + protocol 2 + world_smoke 1 + world_two_clients 6 — added five new integration tests, one per sub-task). 18 functional commits across three repos. |
| 2026-05-11 | [session_2026_05_11.md](session_2026_05_11.md) | **Track 3 — Multi-player replication.** Five sub-tasks bringing the server-authoritative loop from one-player to many-player; after this track, two `ProjectDawn.exe` instances on one server see each other walk around in world.tscn. **Sub-task 1** — wire format: `EntitySpawn { id, name, race, class, level, pos, yaw }` variant added to `ServerWorldMsg` (placed after `EntityDespawn` so the bincode discriminant order stays append-only); `WORLD_PROTOCOL_ID` bumped `PD_W0001 → PD_W0002`. Server-side: `CharacterSpawn` / `SpawnRow` / `load_character` SELECT extended with `race` + `class` (columns existed in `0001_init.sql` from day one, just unread); `PerConnection.race` / `.class` added. GDScript: `SW_ENTITY_SPAWN` tag mirrored 1:1 across `Project_Dawn` and `launcher` copies of `scripts/net/protocol.gd`. Post-hoc finding: the protocol-id check is server-side only — clients relay the opaque ConnectToken and never compare locally, so an exported `.exe` built against PD_W0001 still connects to a PD_W0002 server. The bump remains correct for future client-side mints but doesn't act as a kill-switch today. **Sub-task 2** — GDExtension: `entity_spawn(id, name, race, class, level, pos, yaw)` and `entity_despawn(id)` signals on `NetClient`; new `Incoming` variants + `classify` / `fire` arms following the existing `Position` pattern, with deferred-emit through the `Pending` struct preserved so handlers can re-enter `NetClient` methods. Top-of-file doc comment refreshed (six message types now decoded, was four). DLL rebuilt 3.62 MB with `+crt-static`; copied to `addons/gdext_net/gdext_net.dll`. `addons/gdext_net/build.ps1` has a long-standing UTF-8 BOM / em-dash parser issue in PowerShell 5.1; ran cargo + Copy-Item inline. **Sub-task 3** — server-side fan-out (biggest server change of the track). `Outcome::JustConnected` returned from `handle_message`'s Connect arm when `conn.ready` flips false→true, so `tick.rs` can run the EntitySpawn fan-out at app-Connect time (not transport-connect — identity isn't settled until app-Connect). New step 4a: new client → existing peers, existing peers → new client; subject skipped both ways (ConnectOk is the new client's own-spawn signal); `to_disconnect.contains(new_id)` skip handles the rare connect-then-kick-same-tick case. `ServerEvent::ClientDisconnected` arm broadcasts `EntityDespawn` to ready peers BEFORE removing the leaver from `connections` (gated on the leaver having been `ready`, so no orphan despawns reach peers who never had a matching spawn). Step 6 (position broadcast) became a true fan-out: every ready client's position to every ready client INCLUDING themselves (Track 2's snap-or-lerp depends on the owner receiving own broadcasts). `handlers.rs` refactor: `send_position` split into `build_position_msg` (pure encode) + thin sender; `send_entity_spawn` / `send_entity_despawn` helpers on the reliable system channel; `broadcast_position` wrapper deleted. AOI: per handoff Q1 (user-confirmed) no spatial filter — "everyone in zone sees everyone," ~4 MB/s worst case at MAX_CLIENTS=64. Two-client integration test `world_two_clients.rs` (~280 lines, adapted from `world_smoke.rs`) drives two renet clients against in-process auth+world; asserts both directions of EntitySpawn fan-out (with name/race/class/level intact), Position broadcasts carrying the other's id at both clients, and `EntityDespawn{B}` arriving at A on clean disconnect. Test suite now 7/7. Crate-level `#![allow(clippy::too_many_arguments)]` added to `gdext-net/src/lib.rs` — `entity_spawn` has 7 fields by design; `#[godot_api]` expands to an 8-arg fn (with self), tripping the lint at the macro invocation line where item-level allows don't reach. **Sub-task 4** — client-side wiring, three pieces in two commits. (A) `autoloads/net.gd`: `world_entity_spawn` + `world_entity_despawn` coarse signals; parameter renames `name`→`n` (avoids `Node.name` shadow) and `class`→`char_class` (GDScript reserved). (B) `scenes/remote_player.tscn` + `scripts/remote_player.gd` (`class_name RemotePlayer`): CharacterBody3D + 2 m capsule (matches `player.tscn`) + billboarded Label3D (y=2.2, font 36, outline 8); no camera/input/physics; snapshot-interpolation buffer (`BUFFER_CAPACITY = 5`) of `{ time, pos, yaw }` dicts; `_physics_process` renders at `now − INTERP_LAG` (100 ms per handoff Q2, user-confirmed) by linear-searching the buffer for the straddling pair. (C) `autoloads/remote_player_manager.gd`: listens to Net's three world signals, manages `Dictionary[char_id → RemotePlayer]`, registered last in `project.godot` autoloads so `Net._ready` runs first. **Sub-task 5** — manual two-instance verification surfaced an asymmetric-visibility bug: A (first-joined) saw B, but B (second-joined) did NOT see A. Server log clean both directions; integration test still passed. Root cause: server fires `EntitySpawn(A) → B` over the reliable channel immediately when B's app-Connect completes, but B is still on the launcher's lobby scene at that moment (the "Enter World" button just appeared via `Net.app_connected`). Original manager parented RemotePlayers under `get_tree().current_scene` — the lobby — and when B clicked Enter World the lobby was freed, taking the RemotePlayer with it. Server's EntitySpawn is a one-shot, so B was left in world.tscn with no record of A; subsequent Positions for A dropped by the manager's "position before spawn" defensive branch. A didn't hit this because by the time B connected, A was already in world.tscn. The integration test missed it because raw renet clients have no Godot scene tree. Fix in commit `108bcf6`: split state into `_spawn_data` (autoload-scoped, persistent, source of truth) and `_by_id` (scene-scoped, dies with scene). `_process` polls `get_tree().current_scene` for swaps, drops `_by_id` on change, queues a `_needs_rehydrate` flag that fires only when the new scene hosts the local player (membership in the `player` group set by `player.gd._ready`'s `add_to_group("player")`); naturally gates on world.tscn without hard-coding a scene path. Flag persists across frames so player-joins-group on a later tick than scene-becomes-current doesn't fall through the cracks. `_on_entity_spawn` always caches, instantiates only if scene hosts player; rehydrate catches up otherwise. Re-test: both clients see each other regardless of join order, smooth walk (user accepted as "good for now"), instant despawn on close. **Re-export gotcha** (worth carrying forward): user's first manual test used a pre-Track-3 `builds/ProjectDawn.console.exe`. Connected fine (protocol-id is server-side only) but had no Track-3 GDScript — surfaced as "everything looks fine, clients don't render each other." Required Project → Export → Windows Desktop preset to bundle the new GDScript + DLL into the .exe. Eight functional commits across three repos. |
| 2026-05-09 | [session_2026_05_09.md](session_2026_05_09.md) | **Hardening pass.** Five sub-tasks closing audit followups before Track 3. **Sub-task 1** — SaveManager owns window-close: migrated off the broken `_notification(NOTIFICATION_WM_CLOSE_REQUEST)` (audit #1 — same Godot-4 pitfall Track 1 hit) onto `Window.close_requested`, consolidated `auto_accept_quit` ownership so `Net._ready()` no longer races SaveManager (audit #2). Close handler order now matches `options_screen._on_quit_pressed`: save → `Net.leave_session()` → `get_tree().quit()`. **Sub-task 2** — lobby UI gated in launcher mode (audit #3): hides legacy Solo/Host/Join/Test buttons, shows "Connecting to world..." until `Net.app_connected`, then surfaces explicit "Enter World" button (user picked the explicit-button variant over the handoff's default auto-route); on `Net.app_disconnected` re-shows local buttons so offline dev iteration still works. Test Dungeon fall-through (Track 2 surfacing) is unreachable in launcher mode now. **Sub-task 3 (server)** — per-tick movement integration: `handlers.rs` Move arm stops integrating per-message and stores latest direction on `PerConnection`; `tick.rs` integrates once per tick between message-drain and broadcast (`STALE_MOVE_THRESHOLD = 500 ms` zeroes the integrated direction if the client goes silent, so a crashed client doesn't visually run forward until the 10 s heartbeat timeout). Track 2's 20 Hz client throttle stays as bandwidth cap but is no longer load-bearing. `cargo test --release` 6/6 passing; user verified Track 2 walk feel unchanged. **Sub-task 4** — GDScript warning cleanup, two batches: `name` parameter / for-iter shadowing in `hotbar.gd`, `transformations.gd`; `race` parameter shadowing in `character_creation.gd`; integer-division warnings in `combat.gd`/`character_window.gd`/`vendor_window.gd`/`net.gd`/`equipment.gd` and the four skill-cap definition files (`@warning_ignore("integer_division")` for intentional integer division per Godot's documented mechanism); `hud.tscn` ext_resource UID drift (`uid://c8nt3vhqw6y1` in `.tscn` vs `uid://4xqe6iejlere` in the `.gd.uid` sidecar) corrected. Two warnings (1 enum-int, 1 narrowing-conversion) couldn't be located by grep because the type contract is on the target property, not the expression — user clicked them and supplied line numbers; final batch 3 fixed `settings.gd:123` (`ev.keycode = key as Key`) and `combat_log.gd:213` (`int()` wrap of `max_value`). enemy.gd's 9 unused-signal + 2 `var dist` declared-below warnings left intentionally untouched per the no-modify list (documented for next iteration: signals are vestigial because emitters route through `Combat.<signal>.emit` instead). **Sub-task 5** — audit hygiene: `scripts/terrain_zone.gd` (239 lines, dead — superseded by `terrain_mesh.gd`) deleted along with its `.uid` sidecar; server `cargo clippy --workspace --all-targets` now clean (4 `useless_conversion` `String→String .into()` no-ops dropped + 1 `manual_char_comparison` switched to slice pattern). CLAUDE.md `Godot 4.3 → 4.4` (audit #6) deferred to user per no-modify list. Seven commits across two repos (5 in Project_Dawn, 2 in server). |
| 2026-05-08 | [session_2026_05_08.md](session_2026_05_08.md) | **Track 1 — Slice-1 verification.** WASD-Move plumbing (single-line `Net.send_movement(direction, false)` in `player.gd._physics_process` before `move_and_slide()`); 1077 packets verified end-to-end with monotonic sequences and real direction values. Clean window-close disconnect after five rounds: `_notification(NOTIFICATION_WM_CLOSE_REQUEST)` doesn't fire on autoloads in Godot 4 (root Window only, no propagation), so the canonical pattern is `auto_accept_quit = false` in `_ready()` + `get_tree().root.close_requested` signal. Server-side drain-before-evict in `tick.rs` doesn't recover messages that renet 2.0's `transport.update()` has already freed; final fix is client-side — `leave_session()` sends app-Disconnect + 4 polls and skips `disconnect_now()`, letting the OS close the UDP socket on process exit so the server processes the message cleanly (`reason=DisconnectedByServer`). Quit Game button in `options_screen.gd` routed through the same `Net.leave_session()` for parity. Server-side drain kept as defensive insurance. **Track 2 — Server-authoritative position reconciliation** (continuation, same day): client subscribes to `Net.world_position` and honors server's broadcast as truth. Snap-or-lerp variant in `player.gd._on_world_position` — own-player filter, sequence drop for unreliable-channel reorders, unconditional snap on first broadcast (server's DB-loaded spawn can differ from `world.tscn`'s local spawn), snap if horizontal divergence > 1.0 m else update target, time-based smoothing rate of 17.3 /sec (~167 ms to ~95% convergence, framerate-independent via `1 - exp(-rate*delta)`). Y-axis untouched (slice 2 server has no Y physics). Outgoing `Move.direction` scaled by `(current_speed / SERVER_MAX_MOVE_SPEED)` so server's `direction × 7.5 × dt` integration produces local prediction speed (5 m/s base, crouch 2.5, with buff multipliers); server clamp preserves anti-cheat cap. 20 Hz Move throttle (delta-accumulator) fixes the rubber-banding root cause: server applies `TICK_DT` per accepted Move regardless of arrival rate, so 60 Hz sends → 3× server speed; throttle to one Move per 50 ms matches server tick. Audit's "Move-rate optimisation" item was wrong to call this harmless. `save_manager.gd` position-write gate (handoff Q4 option a) unnecessary — `PlayerStats.save_state()` doesn't persist position; server's 60 s checkpoint + disconnect-final-save are the only saver. Verified: local-save regression (Net stays idle, signal never fires, movement identical to pre-Track 2; 34 pre-existing GDScript warnings surfaced in editor Debugger panel — none Track-2-related), persistence loop (launcher → Test Room → walk 15 s → close → relaunch → character returns to last logged-off position via first-tick snap; server log shows two clean session cycles back-to-back). Steady-state SNAP path structurally verified via shared first-tick code; behavioral test deferred until Track 4/7 produces natural divergence > 1 m. Audit #3 (lobby UI ungated in launcher mode) surfaced as Test Dungeon fall-through — out of Track 2 scope; reconciliation is doing exactly what it should. Single commit `9245f6d`, single file (`scripts/player.gd`), no protocol or server changes. |
| 2026-05-06 | [session_2026_05_06.md](session_2026_05_06.md) | Network track A: `scripts/net/protocol.gd` — hand-mirror of `Projects/server/crates/protocol` (auth JSON builders + JSON wire helpers, world enums + tag constants + SlotRef/Vec3 helpers). Variant counts cross-checked 1:1 with Rust source. **Track B — Launcher** (same day, in `F:\Projects\launcher\`): new Godot 4.4 project, single-scene UI with three views (Login/CharSelect/CharCreate) toggled by visibility, drives the auth WS end-to-end (Register auto-promotes to Login, CharCreate/Delete refresh via CharList, Logout tears down cleanly), config persistence to `user://launcher.cfg`, hardcoded race/class lists (16+18), game .exe resolution with three-way fallback, hands off `--auth-token` / `--char-id` / `--world-endpoint` / `--client-version` via `OS.create_process`. Initial commit `4cfde10` on the launcher repo. **Track C pre-flight** (same day): renet 2.0 / MMO persistence research, three-repo secrets audit (clean), security posture review of existing auth service. Handoff plan at [handoff_track_c.md](handoff_track_c.md) — paste-able as a fresh Claude session prompt. Key decisions: renet `Secure` mode from day one (32-byte private key in `.env`), `WorldConnectToken` via temp file not CLI arg, position checkpoint at 60s. **Track C — World UDP server** (same day, in `F:\Projects\server\`): renet 2.0 + renet_netcode 2.0 pinned with `=`; new `world/` module (`mod`/`connection`/`handlers`/`tick`/`persistence`); 20 Hz tick loop owns connection HashMap; secure-mode `ConnectToken` minted by auth handler `RequestWorldToken` (validates session + char ownership, packs `account_id` into 256-byte `user_data`, 30s validity); `Config` adds `netcode_private_key` (REQUIRED — server refuses to start without it) + separate `world_bind` (always `0.0.0.0`) and `world_endpoint` (advertised, defaults `127.0.0.1:7777`); two channels (0=ReliableOrdered system, 1=Unreliable position); server-authoritative speed cap (`MAX_MOVE_SPEED = 7.5 m/s`, clamp before integration); 60s position checkpoint + final save on disconnect; 10s app-layer heartbeat timeout; `tokio::try_join!(auth, world)` spawns both alongside; new protocol messages `RequestWorldToken` / `WorldConnectToken` + `WORLD_PROTOCOL_ID` constant; new DB helpers `verify_char_owned` / `load_character` (CharacterSpawn) / `checkpoint_position` (no schema migration — `pos_x/y/z`/`yaw`/`zone` already in `0001_init.sql`); end-to-end `tests/world_smoke.rs` drives Auth WS → Register/Login/CharCreate/RequestWorldToken → decodes ConnectToken → real `RenetClient` + `NetcodeClientTransport` → renet handshake → app `Connect` / ConnectOk → `Move` / `Position` (asserts `pos.x > 0`) → `Disconnect`. 6/6 tests pass; runtime smoke confirms both listeners bind and refuse-without-key fires before any socket opens. GDScript mirror in `Project_Dawn/scripts/net/protocol.gd` deliberately untouched — it stays in sync with the launcher copy and both update together in track D. **Track D — Game ↔ World net adapter** (same day): GDExtension via gdext (Option A), .dll gitignored + rebuild script. New crate `crates/gdext-net/` in server workspace (cdylib, `=godot 0.5.2 + api-4-4` feature pin, shares the `protocol` crate verbatim — no wire-format drift possible). `NetClient extends Node` exposes `connect_to_server` / `poll` / `send_app_connect` / `send_disconnect` / `send_heartbeat` / `send_move` to GDScript; signals `transport_connected` / `transport_disconnected` / `connect_ok` / `kicked` / `position` / `heartbeat` / `unhandled_server_message`. Channel layout + ConnectionConfig mirrored from `world/mod.rs`. Internal `Pending`-struct pattern lets us drop the `&mut self.client` borrow before firing signals so handlers can re-enter. Game repo: `addons/gdext_net/` (gdextension config + README + build.ps1, .dll gitignored), new `autoloads/net.gd` (registered as `Net`, extends `NetClient` directly, drives the Disconnected → ConnectingTransport → ConnectedTransport → ConnectedApp state machine), new `scripts/cli_args.gd` (parses launcher args, `--world-token-path` is the gate; partial args fall through with warning per handoff Q4); `protocol.gd` gets `+make_request_world_token` / `+AUTH_REQUEST_WORLD_TOKEN` / `+AUTH_WORLD_CONNECT_TOKEN` / `+decode_world_token_bytes` (mirrored 1:1 to the launcher copy, `diff` reports zero). Launcher Play flow extended: `_on_play_pressed` sends `RequestWorldToken` (no longer spawns .exe directly); on `WorldConnectToken` reply, writes raw token bytes to `OS.get_cache_dir() + /pd_world_token_<unixtime>_<rand>.bin` and spawns game with `--world-token-path=<path>` added to the existing args. Game reads file then immediately deletes; 30s server TTL is the secondary safeguard. Server `cargo build -p gdext-net --release` ⇒ 3.7 MB `gdext_net.dll`; existing 6/6 tests still pass (default-members excludes gdext-net so default builds stay fast). Editor + end-to-end runtime verification deferred to user. |
| 2026-05-05 | [session_2026_05_05.md](session_2026_05_05.md) | Designer Debug Panel (sectioned) + autoload flags (god_mode / no_cooldowns / time pause); friendly NPC targeting + F-to-interact (range gate, ProximitySensor split, settings reserved-key sanitizer, dialogue_npc.tscn, Aldric placed); chat input gate + chat input docked to combat-log panel + movement gate via gui_get_focus_owner; loot bag 120 s despawn; inventory trash slot + confirm dialog; armor/casting skill-up chat lines; spell error red colouring; NPC name flash on target; UI scaling (canvas_items 1920×1080) + per-user UI scale slider; Tier 1 saves (PlayerStats / Alignment / quests / skill trackers); Tier 2 saves (M1 base/public stat split, M3 atomic writes + .bak, M2 ItemData dual format, Inventory + Equipment save); Quit Game button + Delete Save button; bulk conversion of 155 ItemData .tres files to script-referenced format; code-review cleanup: FriendlyNPC base class, open_for(npc) API on managers, reserved-key block at rebind commit, SaveManager comment cleanup, lobby parameter rename. **Track A — Alpha distribution** (same day): Report-a-Bug button (Discord invite) on Options screen; Windows Desktop export preset (embed-PCK, 0.1.0, builds/ProjectDawn.exe); .gitignore for builds/ + debug.log; README_FOR_TESTERS.md (system reqs, controls, saving, bug-report flow). **Track B — Server kickoff** (same day, in `Projects/server/`): Cargo workspace pinned to Rust 1.95.0; protocol crate (auth.rs JSON + world.rs bincode types); projectdawn-server binary with auth WS service (Register/Login/CharList/CharCreate/CharDelete/Logout, Argon2id, 30-min session tokens); SQLite + sqlx 0.8 + migrations (accounts/sessions/characters/gm_actions); 5/5 tests passing including end-to-end Register→Logout flow on ephemeral port; ops scripts (backup/dev-run); initial commit c5501e6 |
