# Session 2026-06-24

## Corpse epic Slice 2 — committed (+ post-review atomicity fix)

The "loot your own corpse" slice (built 2026-06-23) was playtested and committed today (server `403ed9d`
/ client `286e2a4`). Full detail is in `session_2026_06_23.md`; the headline is a pre-commit adversarial
review caught a real item-loss window (a failed corpse-loot persist still let a SEPARATE `delete_corpse`
tx drop the body while the looter inventory was never written). Fixed by folding the corpse delete into
`apply_corpse_loot`'s transaction, gating the despawn on persist success, deferring client sends until the
commit, and reverting the in-memory drain from a pre-loot snapshot on failure. +3 regression tests.

## Monster orb to corpse — BUILT, awaiting playtest (NOT committed)

User: "get rid of the golden loot orbs; every creature that dies leaves a corpse body instead. Generic
body for now, eventually the same model/scale as the creature." Scoped via a 3-agent recon: monster loot
is GROUP loot (shared rights, round-robin, coin split, transient/in-memory, 120s linger) and that all
stays on `LootBag`; this is a VISUAL unification (orb -> the same body capsule a player corpse uses) plus
the creature's name on the wire so the body can be labelled. Per-creature model/scale stays deferred
(MobTemplate has no mesh/scale; that's a bigger authored-data job).

**Decision (user):** carry the creature name now (Option B, a small wire change) rather than a client-only
generic body, because the name does double duty: it labels the body AND distinguishes a creature corpse
(name present -> body) from a player-DROPPED item bag (name empty -> keep the golden sack). Nameplate text
is "<creature>'s corpse" mirroring the player corpse, and BOTH nameplates are now **white** (the player
corpse was a reddish tint before).

**Server (PD_W0020 -> PD_W0021):** appended `creature_name: String` to the END of the existing
`LootBagSpawn` variant (append-only-safe; bincode is positional). Plumbed through `LootBag` (struct + `new`
+ `fan_out_loot_bag_spawn`) and all five `LootBag::new` call sites: the spell / melee / pet kill sites pass
the dead mob's name, the player-dropped-item site passes an empty string, and the loot.rs test helper too.
gdext: the `loot_bag_spawn` #[signal] (now 9 args), the `Incoming::LootBagSpawn` mirror, the decode arm,
and the emit arm. 146 lib tests green; DLL rebuilt.

**Client:** new shared `scripts/corpse_body.gd` (`CorpseBody.build(parent, name)`) builds the gray flat
capsule + white billboarded "<name>'s corpse" nameplate; `scripts/corpse.gd` and `scripts/loot_bag.gd`
both call it so the two render paths can't drift. `loot_bag.gd::_ready` now branches: a non-empty
`creature_name` builds a body (0.7 collider, no bob), an empty one keeps the legacy golden sack (0.45
collider, bob tween). `net.gd` signal + handler and `remote_loot_bag_manager.gd` carry `creature_name`
(set before `add_child` so `_ready` branches; the re-snapshot path leaves the already-built visual alone,
so a body never reverts to a sack after a partial loot). Clean editor boot.

**Verified:** server build + 146 tests; clean editor boot; a 2-dimension adversarial pre-playtest review
(wire consistency + client render) returned ZERO findings. Live visual (a mob death leaving a named body,
loot unchanged, dropped items staying sacks, the player corpse now white) is the user's playtest:
`docs/playtest_notes/monster_corpse_checklist.md`. **Not committed — awaiting playtest.** Next: per-creature
model/scale (deferred, needs authored mesh data in the server zone config), then corpse epic Slice 3
(Cleric resurrection).
