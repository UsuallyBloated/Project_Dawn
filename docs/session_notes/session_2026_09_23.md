# Session 2026-09-23: Redeploy, the Login Face-Lift, and the Death That Couldn't Let Go

Machine day two. Morning per plan: R720 redeployed to `3e98553` (provoked-mobs
fix), then section 6 of the machine-day checklist verified: window PASS, Chat 1
PASS, provocation PASS ("appears to work as intended").

- **The login flow got its face-lift** (Monsters & Memories launcher as the
  reference, screenshot provided): all three views now live in a 640-wide
  centered panel via one change to `_make_view` (visibility mirrored up the
  chain), title 48, roomier inputs and buttons. Then the scale work: a global
  1.15 was rejected in favor of **separated dials** (user call) — the window's
  `content_scale_factor` is set per flow, `LOGIN_UI_SCALE` in login.gd vs
  `GAME_UI_SCALE` (1.0) reset by hud.gd on world load. The login dial was
  tuned live 1.15 to 1.4 to 2.0 to 1.8 and settled at **1.6** ("This looks
  good"). Ship zip packed at `45356f2`, then HELD for the finds below.
- **The big find: a corpse could loot itself.** Dying leaves a ~5 s window
  before auto-respawn, and the dead player could loot their own corpse and
  respawn with all the gear — the corpse-run penalty voided. The death lock
  only covered Move/bind/respawn. **Server `59f2fa1`: the dead-state gate** —
  one whitelist at the top of handle_message (talk, respawn, res accept,
  group management, lifecycle, Move's own silent gate, dev tools); everything
  else answers "You cannot do that while dead." Integration test replays the
  exact exploit sequence; 220 unit + 45/45.
- **The EQ death, client half** (user quote-driven: no menu, camera pulls
  back, watch your body): chat says "You have died. Returning to bind point."
  at the instant of death, the camera eases out to 9 m and tilts down (look
  around stays live, movement stays dead), dead right-clicks interact with
  nothing, camera eases home on respawn. The 5 s auto-respawn already existed;
  it just had no theater.
- **Chat numbering**: new windows now take the lowest free "Chat N" (close
  Chat 2, the next new window is Chat 2 again); names scan live windows,
  internal ids stay monotonic.
- Exports: `ae48504` / `a61d34d` / `b01fbc2` / `26c6d05` / `45356f2` (scale
  ladder), `6156d05` (death), `016a3db` (chat numbering, current builds/).
  **Pending verification (machine-day checklist §7)**: redeploy to `59f2fa1`
  + one deliberate death on `016a3db`; then the zip packs and ships.
