# Session 2026-09-17: Wipe Decision, Loose Ends, and the Pet Research

Phone session. No gameplay code changed; the server tip moved only by a comment
fix and a doc sync (build sha for the pending deploy is now `a34af47`).

- **Fresh-world wipe decided and documented.** The user called for erasing all
  accounts and characters alongside the phase 4 deploy ("we are not connected"
  to old positions in a replaced world; also retires the August duplicate
  accounts). Procedure written into `docs/deployment/server_operations.md`
  ("Fresh world reset"): snapshot, stop, retire `world.db`, start, sqlx
  rebuilds from migrations; GM regrant + tester re-registration + stale local
  hotbar-file cleanup are the recorded follow-ons. `docs/NEXT_DESK_SESSION.md`
  created as the ordered desk queue.
- **Loose ends closed**: `server/docs/flaky_integration_tests.md` now records
  the 09-16 resolution (no known-flaky list; any failure is real), and the kill
  test's walk comment matches its code (the earlier accidental denial was
  cleared by the user).
- **An ASCII map** of the phase 4 world was drawn for the user from the shipped
  camp coordinates (chat-only; the plan doc remains the source of truth).
- **Decisions recorded** (user, from the phone): the login rate limiter stays
  disabled indefinitely (tailnet-only; mandatory re-enable if ever exposed —
  caveat kept in the To-Do), and the full-bags scope is settled: the cursor may
  hold a non-empty bag (build queued behind the phase 4 playtest).
- **Pet-levels research commissioned and delivered**:
  `docs/design/pet_levels.md` — EQ classic spell-owned pets with the per-summon
  level roll and max-pet ritual, EQ Legends' 1:1 rework, WoW's owner-tracking,
  and SWG's Creature Handler taming/maturity system. Options A (owner-derived
  now) / B (EQ spell line later) / C (SWG taming epic for the Beast Master).
  Awaiting the pick. The user also set a standing direction, saved to memory:
  **"this game needs more influence from SWG."**
