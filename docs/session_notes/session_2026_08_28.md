# Session 2026-08-28 — The cursor slot playtest, and the death dupe

Client `f538b54` export from last night; server `73074a8` live, `f8c609b` pushed this morning.

## 1. Slice 1 playtest: 13 of 14 rows pass

`cursor_slot_checklist.md`: ground pickup with every refusal honest, the full moves law
(place / capped merge / swap), equip-from-hand, ESC discipline, Drop/Trash escape hatches,
logout persistence, weight, and the round-robin refusal on the partner's bag — all clean on
the first run of a brand-new server system.

## 2. The one failure was the worst kind: an item dupe

Dying while holding left the item on the cursor after respawn AND on the corpse. Diagnosis:
the DB strip was correct (DELETE by char_id takes the cursor row), but the in-memory
`clear_all()` predated the cursor — cleared base/equipment/bags, left the hand full — so the
post-death snapshot re-sent the held item while the corpse carried the copy, and the next
checkpoint made it durable. Fixed same morning (`f8c609b`): one line in `clear_all` plus a
regression test running the exact death sequence (fill hand, clear, assert hand and
all_stacks empty). 211/211. The playtest character may hold a leftover duplicate from the
test — destroy one copy by hand.

## 3. Slice 1.5, queued from the feedback notes

- Left-click a kill-loot corpse/bag should TARGET it (left-click = target, everywhere; also
  activates the standing hud target-frame guard item — loot bags as targets need the
  has_signal guards).
- Left-click a WORN paperdoll item with an empty hand lifts it to the cursor (UnequipItem to
  "cursor"; small server addition).
- Clicking the WORLD while holding = drop (with confirm); the inventory's Drop cell goes away
  entirely at user direction.
- **Every pickup attaches to the cursor** regardless of source: retire the local visual drag
  in launcher mode (its icon ghost was invisible anyway — `item.icon` is null for all 172
  items) and route all lifts through the real cursor. One system, one look, one law.
