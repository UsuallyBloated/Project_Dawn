# Session — 2026-05-08

**Track 1 — Finish slice-1 verification.** Closes the open verification step from track D's handoff: WASD updates server position, and window-close produces a clean app-layer disconnect on the server log instead of a 10 s heartbeat-timeout kick. Touches three files in the game repo, one in the server repo. No protocol changes.

---

## Move intents on the wire (`scripts/player.gd`)

Single-line addition in `_physics_process()`, between the velocity-tween block and `move_and_slide()`:

```gdscript
if Net.is_app_ready():
    Net.send_movement(direction, false)
```

`direction` is the local unit vector built from WASD input (`transform.basis.x/z`-relative), normalised on line 188 or `Vector3.ZERO` when idle / chat-focused. Both states are valid intents — server clamps via `Vec3f::clamp_length(1.0)` so zero passes through as a stop intent.

`Net.is_app_ready()` returns false in local-save mode, so the call is a no-op there. Local-save Test Room flow is unaffected.

### Verification

Re-export, launcher → Test Room, walk for ~17 s, close window. Added a temporary `tracing::debug!` to the server's `Move` arm in `world/handlers.rs` (since reverted) under `RUST_LOG=projectdawn_server=debug,sqlx=warn` to inspect packet bodies. First test came back with all-zero directions (lobby vs. world-scene timing); second test with the same code produced 1077 Move packets, sequences 1–1077 monotonic, real direction values from sequence 54 onwards (e.g. `dir_x=-0.7615 dir_z=0.6481` while diagonal-strafing). No out-of-order rejects, no rate-limit kicks, no decode errors. Ratio of ~63 packets/sec matches the client's physics tick.

The `tracing::debug!` was a one-shot diagnostic and has been reverted; the noise wasn't worth keeping past verification.

---

## Clean window-close disconnect — five rounds to find the right shape

The handoff plan called for `_notification(NOTIFICATION_WM_CLOSE_REQUEST)` calling `leave_session()` so the server logs `client requested disconnect` instead of a 10 s heartbeat-timeout kick. The straightforward implementation hit three different bugs in sequence; this section records each so the next person doesn't re-derive them.

### Round 1: `_notification` doesn't fire on autoload nodes in Godot 4

Initial implementation:
```gdscript
func _notification(what: int) -> void:
    if what == NOTIFICATION_WM_CLOSE_REQUEST:
        leave_session()
```

Test result: 10 s heartbeat-timeout kick. The handler never ran. In Godot 3 the close-request notification propagated down the scene tree; in Godot 4 it is delivered only to the root `Window` node and does not propagate to children (autoloads sit under the root viewport, so they're children).

### Round 2: `auto_accept_quit = false` set inside the handler is too late

Tried setting `get_tree().auto_accept_quit = false` inside `_notification` and calling `get_tree().quit()` at the end. Same result — the engine commits to quitting based on the value at the time the close request fires, not at the time the handler reads it.

### Round 3 (works): `Window.close_requested` signal

In Godot 4, the canonical pattern is:

```gdscript
func _ready() -> void:
    get_tree().auto_accept_quit = false
    get_tree().root.close_requested.connect(_on_close_requested)
    ...
```

Set `auto_accept_quit` *before* any close request can fire, connect to the root window's `close_requested` signal (which the engine emits regardless of the auto-quit setting), and call `get_tree().quit()` ourselves at the end of the handler. `_notification` was removed entirely — it's the wrong path for this in Godot 4.

After this fix, the handler ran and the server log changed: heartbeat timeout disappeared, replaced by a fast `client disconnected (transport) reason=Transport`. Progress, but still not the goal — `client requested disconnect` was still missing.

### Round 4 (didn't help): server-side drain-before-evict

Hypothesis: when the client calls `disconnect_now()` after `send_disconnect()` + a few polls, the netcode-level disconnect packet races the app-layer `Disconnect` message at the server. Both arrive in the same UDP burst; renet's `transport.update()` queues both a `ClientDisconnected` event AND the channel-0 message; tick.rs processes events before draining channels, so the `ClientDisconnected` event handler removes the connection from `connections` HashMap, then the channel-drain phase skips the disconnected client (`if !connections.contains_key(&client_id) { continue; }`), losing our app message.

Server-side fix attempt at [crates/projectdawn-server/src/world/tick.rs:86-113](F:/Projects/server/crates/projectdawn-server/src/world/tick.rs#L86-L113): drain pending channel messages *inside* the `ClientDisconnected` event handler before removing from `connections`.

Test result: `client requested disconnect` still missing. The drain inside the disconnect handler always finds nothing, because `RenetServer` evicts per-client channel buffers as part of `transport.update()` itself — by the time the `ClientDisconnected` event reaches our `match` arm, `server.receive_message(client_id, channel)` already returns `None`. The drain is structurally correct logic but doesn't recover what's already been freed.

The drain is **kept** as defensive code with a "why" comment — if a future renet version preserves channel buffers across disconnect, or if there's any scenario where messages persist past the event, it'll catch them. Cheap to leave in place; expensive to re-derive if removed.

### Round 5 (works): client skips `disconnect_now()`, OS closes the socket on exit

The actual fix is on the client side: stop sending the netcode-level disconnect at all. `Net.leave_session()` now sends the app-layer `Disconnect` and drives `poll(0.05)` 4× (~200 ms) to flush the message to the wire — and that's it. No `disconnect_now()`. The OS closes the UDP socket when the process exits a few milliseconds later.

Server side then sees only the app-layer message (no concurrent netcode disconnect packet), processes it cleanly:

```
15:21:41  client requested disconnect          char_id=8                         ← handlers::handle_message
15:21:41  client disconnected (transport)      client_id=8 reason=DisconnectedByServer
```

`reason=DisconnectedByServer` because tick.rs handles `Outcome::Disconnect` from the message handler by adding the client to `to_disconnect` and calling `server.disconnect(client_id)` itself — that's the right cause.

The downside: `leave_session()` no longer cleanly tears down the renet client object. For the current callers (window-close, in-game Quit) the process exits immediately after, so the GC handles it. A future return-to-lobby flow that needs to keep the process alive would need a different teardown path — `send_disconnect`, poll long enough for the message to be processed (~50–100 ms is plenty given a 50 ms server tick), *then* `disconnect_now()`. Documented in the function's comment.

---

## Quit Game button (`scripts/options_screen.gd`)

The in-game Quit Game button at [options_screen.gd:109-112](scripts/options_screen.gd#L109-L112) was calling `SaveManager.save() ; get_tree().quit()` directly, which bypasses `Window.close_requested` entirely (that signal only fires for OS-initiated closes). Routed it through the same cleanup:

```gdscript
func _on_quit_pressed() -> void:
    SaveManager.save()
    Net.leave_session()
    get_tree().quit()
```

Both quit paths (OS X button, in-game Quit Game) now share `Net.leave_session()` as the single source of truth for network teardown. `leave_session()` early-outs gracefully when `_state != CONNECTED_APP`, so it's safe to call in local-save mode.

---

## Files touched

### Project_Dawn (`F:\Projects\Project_Dawn\`)

**Modified**
- [scripts/player.gd](scripts/player.gd) — `+Net.send_movement(direction, false)` in `_physics_process` before `move_and_slide()`.
- [autoloads/net.gd](autoloads/net.gd) — `_ready()` sets `auto_accept_quit = false` and connects to `get_tree().root.close_requested`; new `_on_close_requested()` calls `leave_session()` then `get_tree().quit()`; `leave_session()` rewritten to send app-Disconnect + 4 polls and skip `disconnect_now()` (with a long comment explaining why).
- [scripts/options_screen.gd](scripts/options_screen.gd) — Quit Game button calls `Net.leave_session()` between `SaveManager.save()` and `get_tree().quit()`.

### server (`F:\Projects\server\`)

**Modified**
- `crates/projectdawn-server/src/world/tick.rs` — `ClientDisconnected` event handler drains pending channel messages before removing the connection from the local HashMap. Currently dead code in practice (renet 2.0's `transport.update()` evicts the buffers earlier), kept as defensive insurance with a comment explaining the race it's meant to catch.

---

## Pending / deferred

- **Move-rate optimisation** — client currently sends 1 Move per `_physics_process` (~60 Hz) for a 20 Hz server tick. Wasteful but harmless; server already drops out-of-order packets via `last_move_seq`. Worth throttling to ~20 Hz client-side once Track 2 (server-authoritative movement) lands and we care about bandwidth in earnest.
- **Track 2** — server-authoritative position. Server is already broadcasting `Position` on channel 1; client subscribes to `Net.world_position` signal but doesn't reconcile yet. Owner is the next session.
- **Server-side drain-before-evict** in tick.rs is dead code in current renet 2.0. If we never hit a case where it fires after Track 2/3 lands, candidate for removal.

---

## Verification trail

Server log across the five test rounds (timestamps trimmed):

| Round | Close path | Server log result |
|---|---|---|
| 1 | OS X button, `_notification` handler | `app-layer heartbeat timeout` (handler never ran) |
| 2 | OS X button, `_notification` + late `auto_accept_quit` | `app-layer heartbeat timeout` (still didn't run) |
| 3 | OS X button, `close_requested` signal + `disconnect_now()` | `client disconnected (transport) reason=Transport` (no app-Disconnect) |
| 4 | OS X button, server-side drain-before-evict | `client disconnected (transport) reason=Transport` (drain found nothing) |
| 5 | OS X button, no `disconnect_now()` | `client requested disconnect` + `reason=DisconnectedByServer` ✓ |

Final state at session close: server log records both lines on window close, sub-100 ms apart. Track 1 closed.

---

# Track 2 — Server-authoritative position reconciliation (continuation, same day)

User pasted [handoff_track_2.md](handoff_track_2.md) into a fresh Claude session and answered the four open decisions: snap threshold 1.0 m, lerp factor 0.25, unconditional snap on first broadcast, save_manager gate strategy option (a). Track 2 wires the downlink half of the server-authoritative loop: client subscribes to `Net.world_position` and honors the server's broadcast position as truth.

## What landed

Single commit, single file ([scripts/player.gd](../../scripts/player.gd)). No protocol or server changes — Track 2 stays client-side per the handoff.

### Reconciliation (snap-or-lerp)

`_ready` connects `Net.world_position` → `_on_world_position`. Handler filters to own `player_id` (other-player replication is Track 3), drops out-of-order packets via `_last_server_seq` (channel 1 is Unreliable), and:

- **First broadcast:** unconditional snap. Server's DB-loaded spawn can differ from `world.tscn`'s local spawn (especially after a logout/login cycle where the server saved a non-default position). First-tick snap eliminates the visible scene-in lerp.
- **Subsequent broadcasts:** snap if horizontal distance > 1.0 m, else update `_server_pos_target`. Each physics frame after `move_and_slide`, blend horizontal `global_position` toward target with a time-based smoothing rate (17.3 /sec ≈ 167 ms to ~95% convergence, framerate-independent via `1 - exp(-rate*delta)`).
- **Y-axis is left untouched.** Slice 2 server has no Y physics (no gravity, no jumping); reconciling Y would fight local fall damage / jump physics.

### Outgoing speed scaling

Server integrates `Move.direction` as `direction * MAX_MOVE_SPEED * TICK_DT`, treating the unit vector as "go in this direction at max speed." Client speed is base 5 m/s, server max is 7.5 m/s. Without scaling, the server would always run 50% faster than client prediction.

Fix: scale outgoing direction by `(current_speed / SERVER_MAX_MOVE_SPEED)` before sending. Server clamps direction length to 1.0, so any speed-buff pushing past the cap is enforced server-side — the cap is preserved as anti-cheat.

### 20 Hz Move throttle — the rubber-banding fix

First smoke test felt like "moving too fast" with bad rubber-banding. Cause: the server's `Move` handler in [handlers.rs:67-93](../../../server/crates/projectdawn-server/src/world/handlers.rs#L67-L93) integrates a fixed `TICK_DT` (50 ms) per *accepted Move*, regardless of arrival rate. Client was sending at physics-tick rate (~60 Hz), so the server processed all of them and effectively moved at `60 × 0.25 m = 15 m/s` instead of the intended 5 m/s.

The audit's "Move-rate optimisation" item ([audit_2026_05_08.md #11](audit_2026_05_08.md)) had described this as "harmless." It's not — at default frame rate, server speed is tripled.

Fix: throttle Move sends to one per 50 ms (matches server tick). Delta accumulator subtracts the interval rather than zeroing, so the long-run average stays exact across variable frame times. After throttle: server processes 1 Move per tick × 0.25 m/Move = 5 m/s, matching client prediction. Lerp has minimal work to do.

The proper server-side fix (accumulate latest direction, integrate once per real-time elapsed delta) is left for a separate change. Track 2 stayed no-server-changes per the handoff.

## What didn't need changing

`save_manager.gd` position-write gate (handoff open question 4 option a) was **unnecessary**. Confirmed via grep:
- `PlayerStats.save_state()` ([player_stats.gd:230-249](../../autoloads/player_stats.gd#L230-L249)) lists `player_name, race, class, level, xp, hp/mp/stamina, stats, bind_zone, transformation` — no position field.
- No other autoload writes player position to disk. `player_death.gd` holds `_respawn_position` in-memory only; `zone_loader.gd` reads spawn from in-scene Entry nodes, not save data.

Server's 60 s DB checkpoint (`world/persistence.rs`) plus the disconnect-final-save are already the only persisted source. Track 2 simplifies to a one-file change.

## Five rounds, like Track 1

The shape of Track 2 evolved through smoke tests:

| Round | Symptom | Diagnosis | Fix |
|---|---|---|---|
| 1 | Editor parse errors on `var blend := lerp(...)` | `lerp()` returns Variant; project treats `untyped_declaration` as error | Use `lerpf()` (float-specific overload) |
| 2 | "Moving too fast", bad rubber-banding | Server applies `TICK_DT` per accepted Move, not per real time | Client throttle to 20 Hz (one Move per server tick) |
| 3 | Smoother but still tunable feel | Per-frame lerp factor isn't framerate-independent | Time-based smoothing rate via `1 - exp(-rate*delta)` |
| 4 | Falling through floor in Test Dungeon | Audit #3 surfacing — first-tick snap teleports X/Z to world.tscn's saved position while standing on dungeon_world.tscn's floor mesh | Out of scope; documented. Fix is the lobby gate (audit #3) |
| 5 | Persistence regression: walked 15 s, closed, relaunched | Reappeared at last logged-off position. ✓ | n/a — verifies server-as-saver loop end-to-end |

## Verification

- **Local-save regression (F5 from Project_Dawn editor, no launcher):** `Net` stays idle, `world_position` never fires, reconciliation never runs, movement identical to pre-Track 2. 34 pre-existing GDScript warnings showed up in the editor's Debugger panel (shadowing, integer division, narrowing conversions, unused signals) — none from Track 2 changes; all in untouched files. Future hygiene pass.
- **Persistence loop (launcher → Test Room → walk 15 s → close → relaunch → Test Room):** character returns to last logged-off position. Server log shows two clean session cycles back-to-back, both with `client requested disconnect` followed by `reason=DisconnectedByServer`. End-to-end "server is the saver" verified.
- **Steady-state SNAP path** structurally shares the `global_position = Vector3(pos.x, ..., pos.z)` line with the first-tick snap (which is verified). Behavioral test deferred — natural divergence stays below threshold once client/server speeds match. Will see real exercise once Track 4 (combat events) or Track 7 (zone transitions) lands.

## Files touched

### Project_Dawn (`F:\Projects\Project_Dawn\`)

**Modified**
- [scripts/player.gd](../../scripts/player.gd) — Track 2 reconciliation (`_on_world_position` handler, snap-or-lerp branch, time-based smoothing in `_physics_process`), outgoing direction scaling by `current_speed / SERVER_MAX_MOVE_SPEED`, 20 Hz Move throttle via delta accumulator.

### Server / launcher / ProceduralDungeon

No changes.

## Pending / deferred

- **Audit #3 — lobby UI ungated in launcher mode.** Test Dungeon falls through floor when entered from launcher mode. Fix is hiding/auto-routing lobby buttons when `Net.is_launcher_mode()` is true. Should land before more multi-scene work.
- **Server-side per-tick integration bug.** Server uses `TICK_DT` per accepted Move instead of real elapsed time. Client-side 20 Hz throttle is sufficient for slice 2. Proper fix (server accumulates latest direction, integrates once per real-time delta in the tick loop) is server-side and belongs in a separate change.
- **Pre-existing GDScript warnings** (~34 of them) surfaced in the editor's Debugger panel during the local-save regression test. None tied to Track 2; all in pre-existing files. Future hygiene pass alongside the audit's findings #1, #2, #6, #7, #8.
- **Track 3 — multi-player replication.** Server has no AOI fan-out; only the owner sees their own Position. Other-player replication, EntitySpawn/Despawn, and snapshot interpolation between server snapshots all land there.
