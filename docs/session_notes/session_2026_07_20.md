# Session 2026-07-20 — Respawn dead-check playtested + TICKED

Continued Phase 1 (the exploit gate). The Respawn dead-check (built 2026-07-19, server `0d908d1`)
is now playtested and ticked.

## Playtest → tick
`respawn_deadcheck_checklist.md` filled: section 1 (normal death → respawn) and section 2 (regen /
die-again) all `[x]`; section 3 (send a raw `Respawn` while alive) `[-]` as expected — a stock
client can't do it, and the green integration test `respawn_requires_being_dead` covers that side.
Reviewed the server log (cleaned): `dev_cmds=true` boot, then repeated clean `server-detected
player death → corpse created → corpse looted empty, despawned` cycles across many deaths (levels 4
and 6), no errors or panics. So the fix did not break legitimate respawn. **Ticked**: retired the
`Respawn` To-Do item (folded into the Security blockquote), `schedule.md` §7 + `schedule.html`
updated, and a "Respawn (client-driven, gated)" note added to `systems_overview.md`.

While there I also fixed a stale `systems_overview.md` "Harsh decay" bullet (still claimed
`CORPSE_LINGER_SECS` = 300 s and the old `mod.rs::CORPSE_LINGER_SECS` name; now 7 days /
`ENEMY_DESPAWN_LINGER_SECS`, per Phase 0).

## Three playtest observations captured (none block the tick)
1. **No bind + no death lock.** With no server bind you respawn exactly where you died, and there's
   no death state — you can move the character straight through death. Folded into the existing
   "Respawn at bind / Soul Binder NPC" To-Do (the whole death→bind→respawn UX wants a redesign
   pass; its own project). NOT a regression from this fix.
2. **HP/MP regen currently disabled** — the tester notes it's fine and helps testing. No action.
3. **New bug: Test Panel "Despawn All Enemies" leaves enemies invisible, not gone** — the client
   visual drops but the enemy appears to persist. Added to the To-Do (World systems); likely the
   despawn needs to route through the server like `DevSpawnMob`, not a client-only removal.

## Also this session: attack-while-seated (BUILT, pending playtest)
A swing now stands a seated attacker, and the seated regen "meditation" bonus is gated on being
out of combat. Server `3bb9b6d`: the attack apply auto-stands the attacker (`conn.is_sitting =
false`) and stamps a new `conn.last_attack_at`; `regen::sitting_bonus_applies` suppresses the
`SITTING_*_MULT` within `COMBAT_REGEN_LOCKOUT = 6s` of dealing or taking damage (split into a pure
`sitting_bonus_from` + unit test; 166 lib tests). Client `9303682`: `combat.gd::_on_auto_attack`
stands the model on a swing (`stand()` also forwards a Stand to the server via regen.gd's
transition watcher). The free-in-combat-regen exploit is currently **latent** (base regen is 0,
disabled by playtest request) but now guarded for when regen is re-enabled. Playtest:
`attack_while_seated_checklist.md`.

## Also: regen re-activated
At the user's request, restored HP/MP/stamina regen to its pre-disable baseline (HP `2.0 + CON×0.15`,
MP `2.0 + WIS×0.20`, ST `3.0 + AGI×0.10`) in BOTH `regen.rs` (server `032d375`) and `regen.gd`
(client), in lockstep — recovered the original values from git (they were zeroed for the "Round-7
playtest request"; server pre-disable `537d4a7`, client `835d442`, values identical). **Final tuned
numbers are pending the user's research; retune both files together.** Knock-ons: the
attack-while-seated sit-regen combat gate is now LIVE (was dormant at base 0), and respawn now
regenerates you back up over time (the earlier respawn playtest saw regen off).

## EQ regen model (a) built + playtested; attack-while-seated TICKED
Superseding the baseline re-activation above: implemented the classic-EQ flat regen model per the
new `docs/design/regen_model.md` (user-approved the 5 decisions). **Commit (a)** — server `fbbfcd3`
+ client `6bb68e8`, lockstep, 168 lib tests: 6s tick; HP by level bracket + posture + Troll bonus;
MP 1 standing / 2 sitting (+ `meditate/12`, meditate 0 until the skill lands); stamina flat 10.
Replaced the stat-scaled `base + stat×scale`. Unit-tested the HP/MP tables. **Playtested — the user
confirmed the (much slower, level-gated) feel.** **Meditate (commit b)** — the trainable skill that
scales sitting MP — is the one open follow-up.

With regen live, **attack-while-seated was re-playtested and TICKED**:
`attack_while_seated_checklist.md` sections 1-2 all `[x]` (a swing stands you; you can't fight
seated), section 3 (the now-live sit-regen-in-combat gate) unit-test-covered, server.log clean.
Ticked in the To-Do, schedule §7 + html. Two EQ-authentic follow-ups the tester flagged, added to
the To-Do: (a) seated players take bonus damage + are a bonus crit target; (b) taking damage should
stand you up.

## EQ regen model (b): Meditate skill — BUILT (pending playtest)
Closed the one open regen follow-up. Meditate is now a server-authoritative **casting skill**:
cap 250 for mana classes / 0 for Warrior-Rogue-Monk, scaled by level like every skill
(`250 × level / 60`). Server `d97031a`: added `meditate` to `skills.rs` `CASTING_CAPS` + `CASTING_KEYS`
(+ a `meditate_cap_mana_classes_only` test → 169 lib tests); a new `conn.last_meditate_at` gates a
~6s med-tick in the regen loop (`tick.rs`) that calls `skills::try_advance` and fans a private
`SkillProgressUpdate` **only while sitting, out of combat, MP < max** (actually meditating); it drives
sitting MP in `regen.rs` (`2 + floor(meditate/12)`) and rides the existing skill persist / sync /
snapshot (no migration — the seed fills the new key). Client `8bc805f` (lockstep): `meditate` added
to `CastingSkillDefinitions` (ALL / DISPLAY / CLASS_CAPS) so it seeds, syncs, and shows in the
character window; `regen.gd` reads `CastingSkills.get_current("meditate")` for the Test-Room mirror.
Design note `regen_model.md` marked BUILT + cap convention corrected. Playtest:
`regen_model_checklist.md` (must run in launcher mode — training is server-side). **This completes the
EQ regen model track (a + b).**

## Phase 1 gate: CastSpell class/level eligibility — BUILT (pending playtest)
Closed the audit's "any class can cast any spell it can name" (Medium). Server `a96826d`: the cast
resolver in `tick.rs` now rejects a `CastSpell` unless the caster's class is in the spell's
`classes` AND their level meets `min_level`, checked **before** mana / cooldown / skill side effects
(a forged cast costs nothing and leaves no state), with a class-vs-level-specific `CastFail` message.
Sits ahead of the target-type branch so SELF / ALLY / PET / CORPSE are untouched; the
resurrection arm keeps its own copy as defense-in-depth. Verified the data supports it: all 122
spells in `spells.toml` are class- and min_level-tagged (no empty `classes`), locked by a new
invariant test; and `conn.class` provably matches the toml class strings incl. multi-word ones
(same field `skills::cap_for` already keys on in this same cast path). +2 lib tests (invariant +
eligibility predicate vs Fireball/Meteor/Cleric rows), 171 pass. Server-only — no client re-export.
Playtest: `cast_class_level_gate_checklist.md` (a regression sweep — confirm legit casts are
unaffected; the reject path needs a forged client, so it's unit-test + log covered).

## Phase 1 status
Keystone (`is_gm`) ✅ ticked, Respawn dead-check ✅ ticked, attack-while-seated ✅ ticked; CastSpell
class/level gate ✅ built (awaiting `cast_class_level_gate_checklist.md`). Remaining gates:
weapon_path trust (`Attack` trusts the client's weapon), melee swing-rate limit, login rate-limit +
auth timing. (The EQ regen model is a separate user-requested track; now fully built — Meditate was
its last piece — and awaiting the `regen_model_checklist.md` playtest.)
