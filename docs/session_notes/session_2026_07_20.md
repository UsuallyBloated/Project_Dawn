# Session 2026-07-20 — Respawn dead-check playtested + TICKED

Continued Phase 1 (the exploit gate). The Respawn dead-check (built 2026-07-19, server `0d908d1`)
is now playtested and ticked.

## Playtest → tick
`respawn_deadcheck_checklist.md` filled: section 1 (normal death → respawn) and section 2 (regen /
die-again) all `[x]`; section 3 (send a raw `Respawn` while alive) `[-]` as expected — a stock
client can't do it, and the green integration test `respawn_requires_being_dead` covers that side.
Reviewed the server log (cleaned): `dev_cmds=true` boot, then repeated clean `server-detected
player death → corpse created → corpse looted empty, despawned` cycles across many deaths (levels 4
and 6), no errors or panics. So the fix did not break legitimate respawn. **Ticked**: retired the
`Respawn` To-Do item (folded into the Security blockquote), `schedule.md` §7 + `schedule.html`
updated, and a "Respawn (client-driven, gated)" note added to `systems_overview.md`.

While there I also fixed a stale `systems_overview.md` "Harsh decay" bullet (still claimed
`CORPSE_LINGER_SECS` = 300 s and the old `mod.rs::CORPSE_LINGER_SECS` name; now 7 days /
`ENEMY_DESPAWN_LINGER_SECS`, per Phase 0).

## Three playtest observations captured (none block the tick)
1. **No bind + no death lock.** With no server bind you respawn exactly where you died, and there's
   no death state — you can move the character straight through death. Folded into the existing
   "Respawn at bind / Soul Binder NPC" To-Do (the whole death→bind→respawn UX wants a redesign
   pass; its own project). NOT a regression from this fix.
2. **HP/MP regen currently disabled** — the tester notes it's fine and helps testing. No action.
3. **New bug: Test Panel "Despawn All Enemies" leaves enemies invisible, not gone** — the client
   visual drops but the enemy appears to persist. Added to the To-Do (World systems); likely the
   despawn needs to route through the server like `DevSpawnMob`, not a client-only removal.

## Phase 1 status
Keystone (`is_gm`) ✅ and Respawn dead-check ✅ done. Remaining gates: attack-while-seated, cast
class/level gate, weapon_path trust, melee swing-rate limit, login rate-limit + auth timing. Next
up (per `handoff_phase1_exploit_gate.md`): **attack-while-seated**.
