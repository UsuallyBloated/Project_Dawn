[plugin]

name="BuildStamp"
description="Injects a git-derived build stamp into every export, so a tester's screenshot or debug.log identifies exactly which build they are running. No human step: exporting is the stamping step."
author="UsuallyBloated"
version="1.0"
script="plugin.gd"
